# Phase 46-01: Backtest Graph Features - Plan

**Created:** 2026-04-02
**Status:** Ready

## Tasks

### T-01: Compute Graph Features for Training Data
- Run graph feature extraction (pandas fallback) for all player-weeks 2020-2025
- Cache to `data/silver/graph_features/` per season
- Verify temporal lag: no future data in any feature
- Verify join rates: what % of player-weeks get non-null graph features?

### T-02: Retrain Per-Position Models with Graph Features
- Add 18 graph features to player feature vector
- Run per-position per-stat XGBoost training (same as Phase 40)
- Walk-forward CV with expanding folds (2020-2022 train → 2023 val, 2020-2023 → 2024 val)
- SHAP feature selection including graph features
- Compare OOF metrics: which graph features survive SHAP selection?

### T-03: Ship/Skip Gate per Position
- Compare holdout MAE per position: baseline vs graph-enhanced
- QB: already on ML — do graph features improve it further?
- RB: currently heuristic (MAE 5.06) — can graph features + ML beat heuristic?
- WR: currently heuristic (MAE 4.85) — WR-CB matchup features are the best hope
- TE: currently heuristic (MAE 3.77) — TE coverage mismatch features
- Decision: SHIP per position if strict MAE improvement, SKIP otherwise

### T-04: Update ML Projection Router
- For positions that SHIP: update ML router to use graph-enhanced models
- For positions that SKIP: keep heuristic fallback
- Update generate_projections.py if routing changes

### T-05: Full Backtest Comparison
- Run backtest_projections.py with updated models
- Report: overall MAE, per-position MAE/RMSE/bias/correlation
- Compare to baseline (4.91 overall MAE)

## Dependencies
- Phase 45 complete (graph features available) ✓
- Player model training infrastructure (Phase 40-41) ✓
