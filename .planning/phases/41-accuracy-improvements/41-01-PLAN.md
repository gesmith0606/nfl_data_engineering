---
phase: 41-accuracy-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/player_feature_engineering.py
  - src/config.py
  - tests/test_player_feature_engineering.py
autonomous: true
requirements: [ACCY-01, ACCY-02, ACCY-03]

must_haves:
  truths:
    - "Efficiency ratio features (yards_per_carry, yards_per_target, yards_per_reception, catch_rate, rush_td_rate, rec_td_rate) with roll3 and roll6 variants are computed from existing rolling columns"
    - "TD regression features (expected_td_pos_avg and expected_td_player) use red zone target share times conversion rates"
    - "Momentum delta features (snap_pct_delta, target_share_delta, carry_share_delta) equal roll3 minus roll6"
    - "All new features use safe division (np.where) and produce NaN for zero denominators"
    - "New features are auto-discovered by get_player_feature_columns() since they are numeric and not in exclusion sets"
  artifacts:
    - path: "src/player_feature_engineering.py"
      provides: "compute_efficiency_features, compute_td_regression_features, compute_momentum_features"
      contains: "def compute_efficiency_features"
    - path: "src/config.py"
      provides: "POSITION_AVG_RZ_TD_RATE constants"
      contains: "POSITION_AVG_RZ_TD_RATE"
    - path: "tests/test_player_feature_engineering.py"
      provides: "Unit tests for all three feature groups"
      contains: "test_efficiency_features"
  key_links:
    - from: "src/player_feature_engineering.py"
      to: "assemble_player_features return"
      via: "called at end of assemble_player_features before return"
      pattern: "compute_efficiency_features|compute_td_regression_features|compute_momentum_features"
---

<objective>
Add derived efficiency, TD regression, and momentum features to the player feature vector.

Purpose: Decompose opportunity from efficiency signal, replace noisy raw TD rolling averages with expected TD rates, and add role momentum detection — giving the model richer inputs to close the OOF gap for RB/WR/TE positions.
Output: Three new feature computation functions in player_feature_engineering.py, position-average RZ TD rate constants in config.py, unit tests, and wiring into assemble_player_features.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-accuracy-improvements/41-CONTEXT.md
@.planning/phases/41-accuracy-improvements/41-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/player_feature_engineering.py:
```python
def assemble_player_features(season: int) -> pd.DataFrame:
    """Assemble player-week feature vector from 9 Silver sources."""
    # ... joins all sources, returns DataFrame at line 346

def get_player_feature_columns(df: pd.DataFrame) -> List[str]:
    """Return sorted list of valid player feature column names.
    Excludes _PLAYER_IDENTIFIER_COLS, _PLAYER_LABEL_COLS, _SAME_WEEK_RAW_STATS.
    Only includes numeric dtypes (float64, int64, float32, int32, bool)."""

def assemble_multiyear_player_features(seasons=None) -> pd.DataFrame:
    """Concatenates assemble_player_features across seasons."""
```

From src/config.py:
```python
LGB_CONSERVATIVE_PARAMS = {
    "objective": "regression", "max_depth": 4, "learning_rate": 0.05,
    "n_estimators": 500, "min_child_samples": 20, "subsample": 0.8,
    "colsample_bytree": 0.7, "reg_alpha": 1.0, "reg_lambda": 5.0,
    "random_state": 42,
}
```

Existing rolling columns available in Silver usage data:
- carries_roll3, carries_roll6, rushing_yards_roll3, rushing_yards_roll6
- targets_roll3, targets_roll6, receiving_yards_roll3, receiving_yards_roll6
- receptions_roll3, receptions_roll6
- rushing_tds_roll3, rushing_tds_roll6, receiving_tds_roll3, receiving_tds_roll6
- snap_pct_roll3, snap_pct_roll6
- target_share_roll3, target_share_roll6
- carry_share_roll3, carry_share_roll6
- rz_target_share (raw, NOT in ROLLING_STAT_COLS — needs ad-hoc rolling in assembly)

_SAME_WEEK_RAW_STATS includes: rz_target_share (so it won't be a feature itself, only rolling versions)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add derived feature functions and config constants</name>
  <files>src/player_feature_engineering.py, src/config.py, tests/test_player_feature_engineering.py</files>
  <read_first>
    - src/player_feature_engineering.py (current state — 488 lines, see assemble_player_features at line 200, get_player_feature_columns at line 354)
    - src/config.py (find LGB_CONSERVATIVE_PARAMS at line 433 — add POSITION_AVG_RZ_TD_RATE nearby)
    - src/player_analytics.py (ROLLING_STAT_COLS list, compute_rolling_averages function for shift(1)->rolling pattern)
    - tests/test_player_feature_engineering.py (existing tests to extend)
  </read_first>
  <behavior>
    - test_efficiency_features_yards_per_carry: DataFrame with carries_roll3=10.0, rushing_yards_roll3=50.0 => yards_per_carry_roll3=5.0
    - test_efficiency_features_zero_denominator: DataFrame with carries_roll3=0.0 => yards_per_carry_roll3 is NaN (not inf)
    - test_efficiency_features_all_ratios: Verify all 12 ratio columns created (6 ratios x 2 windows)
    - test_td_regression_position_avg: rz_target_share_roll3=0.20, position=RB => expected_td_pos_avg = 0.20 * 0.08 = 0.016
    - test_td_regression_player_specific: rz_target_share_roll3=0.20, rec_td_rate_roll6=0.10 => expected_td_player = 0.02
    - test_td_regression_computes_rolling_when_missing: rz_target_share present but rz_target_share_roll3 absent => function computes rolling with groupby shift(1)
    - test_momentum_snap_pct_delta: snap_pct_roll3=0.80, snap_pct_roll6=0.60 => snap_pct_delta=0.20
    - test_momentum_carry_share_delta: carry_share_roll3=0.40, carry_share_roll6=0.50 => carry_share_delta=-0.10
    - test_momentum_missing_columns_skipped: If carry_share_roll3 not in columns, carry_share_delta is not created (no error)
    - test_new_features_auto_discovered: After compute_* functions, get_player_feature_columns returns yards_per_carry_roll3, expected_td_pos_avg, snap_pct_delta
  </behavior>
  <action>
    1. In src/config.py, add after LGB_CONSERVATIVE_PARAMS (around line 445):
       ```python
       POSITION_AVG_RZ_TD_RATE = {
           "RB": 0.08,
           "WR": 0.12,
           "TE": 0.14,
       }
       ```

    2. In src/player_feature_engineering.py, add three functions after the existing helper section (before the Core assembly section around line 195):

       a) `compute_efficiency_features(df: pd.DataFrame) -> pd.DataFrame` — per D-06:
          - For each suffix in ["roll3", "roll6"]:
            - yards_per_carry_{suffix} = rushing_yards_{suffix} / carries_{suffix} (np.where carries > 0, else NaN)
            - yards_per_target_{suffix} = receiving_yards_{suffix} / targets_{suffix} (np.where targets > 0, else NaN)
            - yards_per_reception_{suffix} = receiving_yards_{suffix} / receptions_{suffix} (np.where receptions > 0, else NaN)
            - catch_rate_{suffix} = receptions_{suffix} / targets_{suffix} (np.where targets > 0, else NaN)
            - rush_td_rate_{suffix} = rushing_tds_{suffix} / carries_{suffix} (np.where carries > 0, else NaN)
            - rec_td_rate_{suffix} = receiving_tds_{suffix} / targets_{suffix} (np.where targets > 0, else NaN)
          - Total: 12 new columns (6 ratios x 2 windows)
          - Use np.where for safe division — NaN for zero denominator
          - Column existence check before each computation

       b) `compute_td_regression_features(df: pd.DataFrame) -> pd.DataFrame` — per D-07:
          - If rz_target_share_roll3 not in columns but rz_target_share is:
            compute rz_target_share_roll3 = groupby(["player_id", "season"])["rz_target_share"].transform(lambda s: s.shift(1).rolling(3, min_periods=1).mean())
          - If rz_target_share_roll3 available:
            - expected_td_pos_avg = rz_target_share_roll3 * POSITION_AVG_RZ_TD_RATE.get(row["position"], 0.10)
              Use vectorized approach: map position to rate, then multiply
            - expected_td_player = rz_target_share_roll3 * rec_td_rate_roll6 (if rec_td_rate_roll6 exists from efficiency features)
          - Import POSITION_AVG_RZ_TD_RATE from config

       c) `compute_momentum_features(df: pd.DataFrame) -> pd.DataFrame` — per D-08:
          - snap_pct_delta = snap_pct_roll3 - snap_pct_roll6
          - target_share_delta = target_share_roll3 - target_share_roll6
          - carry_share_delta = carry_share_roll3 - carry_share_roll6
          - Skip each if source columns missing

    3. In assemble_player_features(), call the three functions just before the final return (after line 346, before the logger.info line):
       ```python
       base = compute_efficiency_features(base)
       base = compute_td_regression_features(base)
       base = compute_momentum_features(base)
       ```

    4. Add import of POSITION_AVG_RZ_TD_RATE from config at top of player_feature_engineering.py.

    5. Write tests in tests/test_player_feature_engineering.py following the behavior specs above. Create small synthetic DataFrames with known values. Each test should verify exact numeric output.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_player_feature_engineering.py -x -v -k "efficiency or td_regression or momentum"</automated>
  </verify>
  <acceptance_criteria>
    - src/player_feature_engineering.py contains `def compute_efficiency_features(`
    - src/player_feature_engineering.py contains `def compute_td_regression_features(`
    - src/player_feature_engineering.py contains `def compute_momentum_features(`
    - src/player_feature_engineering.py contains `compute_efficiency_features(base)` inside assemble_player_features
    - src/player_feature_engineering.py contains `compute_td_regression_features(base)` inside assemble_player_features
    - src/player_feature_engineering.py contains `compute_momentum_features(base)` inside assemble_player_features
    - src/config.py contains `POSITION_AVG_RZ_TD_RATE`
    - src/config.py contains `"RB": 0.08`
    - tests/test_player_feature_engineering.py contains `test_efficiency_features`
    - tests/test_player_feature_engineering.py contains `test_td_regression`
    - tests/test_player_feature_engineering.py contains `test_momentum`
    - `python -m pytest tests/test_player_feature_engineering.py -x -v` exits 0
    - `python -m pytest tests/ -v` exits 0 (full suite still green)
  </acceptance_criteria>
  <done>
    All three derived feature functions exist, are wired into assemble_player_features, produce correct numeric output verified by unit tests, and the full test suite passes.
  </done>
</task>

</tasks>

<verification>
- All 3 feature computation functions callable and producing correct output
- get_player_feature_columns auto-discovers new features (no manual additions needed)
- No leakage: efficiency features derived from shift(1) rolling columns; momentum deltas from shift(1) rolling columns; rz_target_share_roll3 computed with shift(1) when needed
- Full test suite green: `python -m pytest tests/ -v`
</verification>

<success_criteria>
- compute_efficiency_features produces 12 ratio columns (6 ratios x 2 windows) with safe division
- compute_td_regression_features produces expected_td_pos_avg and expected_td_player columns
- compute_momentum_features produces 3 delta columns (snap_pct_delta, target_share_delta, carry_share_delta)
- All new features appear in get_player_feature_columns() output
- All unit tests pass
- Full suite (622+ tests) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/41-accuracy-improvements/41-01-SUMMARY.md`
</output>
