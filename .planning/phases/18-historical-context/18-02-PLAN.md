---
phase: 18-historical-context
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - scripts/silver_historical_transformation.py
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "Running silver_historical_transformation.py produces a Parquet file at data/silver/players/historical/"
    - "Output contains combine measurables (forty, bench, vertical, broad_jump, cone, shuttle, ht, wt) AND composite scores (speed_score, bmi, burst_score, catch_radius)"
    - "Output contains draft capital (pick, round, draft_value from Jimmy Johnson chart)"
    - "Output contains position percentile columns for composites"
    - "Output contains gsis_id for downstream roster linkage"
    - "Row count after join equals deduped combine rows + draft-only rows (no explosion)"
    - "Undrafted combine attendees have NaN draft_value; drafted players without combine have NaN measurables"
    - "Match rate and unmatched players logged at INFO/WARNING levels"
  artifacts:
    - path: "scripts/silver_historical_transformation.py"
      provides: "CLI script for combine/draft dimension table generation"
      min_lines: 80
    - path: "data/silver/players/historical/"
      provides: "Output Parquet file with combine+draft profiles"
  key_links:
    - from: "scripts/silver_historical_transformation.py"
      to: "src/historical_profiles.py"
      via: "import build_combine_draft_profiles"
      pattern: "from historical_profiles import"
    - from: "scripts/silver_historical_transformation.py"
      to: "data/bronze/combine/"
      via: "local Bronze read"
      pattern: "_read_local_bronze.*combine"
    - from: "scripts/silver_historical_transformation.py"
      to: "data/bronze/draft_picks/"
      via: "local Bronze read"
      pattern: "_read_local_bronze.*draft_picks"
---

<objective>
Create the CLI script that reads Bronze combine and draft_picks data, calls the historical_profiles compute module, and writes the combined dimension table to the Silver layer.

Purpose: Complete the Phase 18 pipeline so users can generate the combine/draft profiles Parquet file with a single CLI command.
Output: Working CLI script (scripts/silver_historical_transformation.py), Parquet output at data/silver/players/historical/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-historical-context/18-CONTEXT.md
@.planning/phases/18-historical-context/18-RESEARCH.md
@.planning/phases/18-historical-context/18-01-SUMMARY.md

@scripts/silver_advanced_transformation.py
@src/historical_profiles.py
@src/config.py
</context>

<interfaces>
<!-- Key contracts from Plan 01 that this plan depends on -->

From src/historical_profiles.py (created in Plan 01):
```python
def build_combine_draft_profiles(combine_df: pd.DataFrame, draft_df: pd.DataFrame) -> pd.DataFrame:
    """Orchestrate full pipeline: dedup -> join -> composites -> percentiles."""

def parse_height_to_inches(ht_str: str) -> Optional[float]:
def compute_speed_score(wt: pd.Series, forty: pd.Series) -> pd.Series:
def compute_composite_scores(df: pd.DataFrame) -> pd.DataFrame:
def compute_position_percentiles(df: pd.DataFrame, score_cols: list) -> pd.DataFrame:
def build_jimmy_johnson_chart() -> dict:
def dedup_combine(combine_df: pd.DataFrame) -> pd.DataFrame:
def join_combine_draft(combine_df: pd.DataFrame, draft_df: pd.DataFrame) -> pd.DataFrame:
```

From src/config.py:
```python
SILVER_PLAYER_S3_KEYS = {
    ...
    "historical_profiles": "players/historical/combine_draft_profiles_{ts}.parquet",
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create silver_historical_transformation.py CLI script</name>
  <files>scripts/silver_historical_transformation.py</files>
  <action>
  Create scripts/silver_historical_transformation.py following the established Silver CLI pattern from silver_advanced_transformation.py:

  1. Module docstring explaining usage:
     ```
     python scripts/silver_historical_transformation.py
     python scripts/silver_historical_transformation.py --no-s3
     ```
     No --seasons flag per user decision — always processes all available Bronze data (2000-2025).

  2. Imports: sys, os, argparse, glob, logging, datetime, pandas, pyarrow.
     Add src/ to sys.path. Import build_combine_draft_profiles from historical_profiles.

  3. _read_local_bronze(subdir: str) -> pd.DataFrame:
     - Read ALL Parquet files from data/bronze/{subdir}/ recursively (all season dirs).
     - Use glob to find all .parquet files under data/bronze/{subdir}/season=*/*.parquet.
     - pd.concat all files. Log count of files read and total rows.
     - This differs from other Silver CLIs which read per-season — here we read everything since it is a dimension table.

  4. main() function:
     - argparse with --no-s3 flag (skip S3 upload, default behavior given expired creds).
     - Read Bronze combine: _read_local_bronze("combine")
     - Read Bronze draft_picks: _read_local_bronze("draft_picks")
     - Call build_combine_draft_profiles(combine_df, draft_df)
     - Create output dir: data/silver/players/historical/ (os.makedirs exist_ok=True)
     - Write with timestamped filename: combine_draft_profiles_YYYYMMDD_HHMMSS.parquet
     - Log: output path, row count, column count, file size in MB
     - Log NaN rates for key columns (forty, speed_score, draft_value) at INFO level

  5. if __name__ == "__main__": main()

  Follow these patterns from existing Silver CLIs:
  - sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
  - logging.basicConfig with format "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  - load_dotenv() for optional AWS credentials
  - Idempotent: produces new timestamped file each run, download_latest_parquet reads newest
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python scripts/silver_historical_transformation.py --no-s3 && python -c "
import glob, pandas as pd
files = sorted(glob.glob('data/silver/players/historical/combine_draft_profiles_*.parquet'))
assert len(files) >= 1, 'No output file found'
df = pd.read_parquet(files[-1])
print(f'Rows: {len(df)}, Cols: {len(df.columns)}')
assert len(df) > 8000, f'Expected >8000 rows, got {len(df)}'
assert 'speed_score' in df.columns, 'Missing speed_score'
assert 'draft_value' in df.columns, 'Missing draft_value'
assert 'gsis_id' in df.columns, 'Missing gsis_id'
assert 'speed_score_pos_pctl' in df.columns, 'Missing speed_score_pos_pctl'
assert 'has_pfr_id' in df.columns, 'Missing has_pfr_id'
# Check no row explosion
assert len(df) < 12000, f'Possible row explosion: {len(df)} rows'
# Check undrafted have NaN draft_value
undrafted = df[df['draft_value'].isna()]
assert len(undrafted) > 0, 'Expected some undrafted players with NaN draft_value'
# Check drafted without combine have NaN measurables
draft_only = df[df['forty'].isna() & df['draft_value'].notna()]
assert len(draft_only) > 0, 'Expected some drafted players without combine data'
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <done>CLI script produces valid Parquet at data/silver/players/historical/ with >8000 rows, all expected columns (raw measurables + composites + percentiles + draft_value + gsis_id), no row explosion, and proper NaN handling for undrafted/no-combine players.</done>
</task>

<task type="auto">
  <name>Task 2: Run full validation and verify success criteria</name>
  <files>tests/test_historical_profiles.py</files>
  <action>
  1. Run the full test suite to confirm no regressions from the CLI script:
     python -m pytest tests/ -v

  2. Validate the output Parquet against Phase 18 success criteria:
     - Row count after join equals row count before join (no explosion)
     - Combine measurables present (forty, bench, vertical, broad_jump, cone, shuttle, ht, wt)
     - Composite scores present (speed_score, bmi, burst_score, catch_radius)
     - Position percentiles present (*_pos_pctl columns)
     - Draft capital present (pick, round, draft_value)
     - gsis_id present for downstream linkage
     - Match rate logged (check via re-running CLI and observing log output)

  3. If any test fails or validation check fails, fix the issue in the relevant file (historical_profiles.py or the CLI script) and re-run.

  No new test file creation needed — validation uses existing tests plus the automated verify from Task 1.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/ -v --tb=short</automated>
  </verify>
  <done>Full test suite passes (71 existing + new historical tests). Output Parquet validated against all Phase 18 success criteria. No regressions in existing functionality.</done>
</task>

</tasks>

<verification>
1. `python scripts/silver_historical_transformation.py --no-s3` — runs without error, produces Parquet
2. `ls -la data/silver/players/historical/` — timestamped Parquet file exists
3. `python -m pytest tests/ -v` — full suite green
4. Output Parquet has >8000 rows, <12000 rows (no explosion)
5. Output contains speed_score, bmi, burst_score, catch_radius, draft_value, gsis_id columns
6. Output contains *_pos_pctl percentile columns
</verification>

<success_criteria>
- CLI script runs successfully and produces valid Parquet output
- Output file at data/silver/players/historical/combine_draft_profiles_*.parquet
- All combine measurables + composite scores + percentiles + draft capital in output
- gsis_id column present for downstream player matching
- Row count in expected range (8000-11000, no explosion)
- Match rate and unmatched players logged
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18-historical-context/18-02-SUMMARY.md`
</output>
