---
phase: 43-game-level-constraints
plan: 01
type: execute
wave: 1
depends_on: ["42-02"]
files_modified:
  - src/projection_engine.py
  - src/ml_projection_router.py
  - scripts/generate_projections.py
  - tests/test_ml_projection_router.py
autonomous: true
requirements: [CONSTRAIN-01, CONSTRAIN-02, CONSTRAIN-03, CONSTRAIN-04]

must_haves:
  truths:
    - "Calibrated multipliers: half_ppr=3.36, ppr=3.77, standard=2.86 derived from 2020-2024 data"
    - "apply_team_constraints() scales player projections with dampened scaling and dead zone"
    - "--constrain flag on generate_projections.py enables team-level normalization (opt-in)"
    - "Constraint function wired into both heuristic and ML projection paths"
    - "14 new tests covering calibration, scaling, dead zone, and CLI flag"
  artifacts:
    - path: "src/projection_engine.py"
      provides: "apply_team_constraints(), FANTASY_MULTIPLIERS constant"
      exports: ["apply_team_constraints"]
    - path: "scripts/generate_projections.py"
      provides: "--constrain CLI flag"
      contains: "--constrain"
    - path: "tests/test_ml_projection_router.py"
      provides: "Tests for constraint function"
      min_lines: 50
---

<objective>
Implement game-level team constraints that normalize player fantasy projections to implied team totals using calibrated multipliers, with dampened scaling and a dead zone. Wire into both projection paths via an opt-in --constrain flag.

Purpose: Improve team-level coherence of fantasy projections without sacrificing individual accuracy. Opt-in because backtest shows mixed results (better bias, worse MAE).
Output: `apply_team_constraints()` function, CLI flag, and comprehensive tests.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/43-game-level-constraints/43-CONTEXT.md
@.planning/phases/43-game-level-constraints/43-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Calibrate fantasy multipliers</name>
  <files>src/projection_engine.py, tests/test_ml_projection_router.py</files>
  <action>
    Add FANTASY_MULTIPLIERS constant to projection_engine.py with calibrated values (half_ppr=3.36, ppr=3.77, standard=2.86). Add tests verifying the multiplier values exist and are reasonable (> 2.0, < 5.0).
  </action>
  <done>Calibrated multipliers available as a constant for use by the constraint function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement apply_team_constraints() with dampened scaling</name>
  <files>src/projection_engine.py, tests/test_ml_projection_router.py</files>
  <action>
    Implement apply_team_constraints(projections_df, implied_totals, scoring_format, dead_zone=0.10, max_adj=0.20). For each team: compute expected fantasy total = implied_total * multiplier. If actual sum is within dead_zone, skip. Otherwise, compute scale factor capped at +/- max_adj per player. Scale proj_{stat} and projected_points proportionally. Add 10+ tests for: dead zone no-op, scale-down for over-projected teams, scale-up for under-projected, max adjustment cap, missing implied totals handled gracefully.
  </action>
  <done>Core constraint function implemented with dampened scaling, dead zone, and comprehensive tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire --constrain flag into CLI and both projection paths</name>
  <files>scripts/generate_projections.py, src/ml_projection_router.py</files>
  <action>
    Add --constrain flag to generate_projections.py. When set, call apply_team_constraints() after projections are generated (both heuristic and ML paths). Wire into ml_projection_router.py as well. Add CLI tests.
  </action>
  <done>--constrain flag available on CLI, wired into both heuristic and ML projection paths.</done>
</task>

<task type="auto">
  <name>Task 4: Backtest and ship-gate decision</name>
  <files>scripts/backtest_projections.py</files>
  <action>
    Run backtest with and without constraints. Compare MAE, RMSE, bias. Document results in summary. If MAE degrades, confirm opt-in (not default) decision.
  </action>
  <done>Backtest confirms ship-gate decision: constraints stay opt-in due to MAE regression.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_ml_projection_router.py -x -v` -- all tests pass including constraint tests
- `python -m pytest tests/ -v` -- full suite green (669+ tests)
- `grep -q "apply_team_constraints" src/projection_engine.py` -- function exists
- `grep -q "\-\-constrain" scripts/generate_projections.py` -- CLI flag exists
</verification>

<success_criteria>
1. Calibrated multipliers match empirical values (half_ppr=3.36, ppr=3.77, standard=2.86)
2. apply_team_constraints() scales proportionally with dampening and dead zone
3. --constrain flag is opt-in, not default
4. Backtest confirms decision (MAE 4.91->5.12 worse, bias -0.60->-0.33 improved)
5. 14 new tests, 669 total passing
</success_criteria>

<output>
After completion, create `.planning/phases/43-game-level-constraints/43-01-SUMMARY.md`
</output>
