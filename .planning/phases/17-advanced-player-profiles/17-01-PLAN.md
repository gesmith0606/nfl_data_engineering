---
phase: 17-advanced-player-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/player_advanced_analytics.py
  - tests/test_player_advanced_analytics.py
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]

must_haves:
  truths:
    - "NGS receiving metrics (separation, catch probability, intended air yards) are extracted for WR/TE with roll3/roll6 rolling windows"
    - "NGS passing metrics (time-to-throw, aggressiveness, completed air yards) are extracted for QB with rolling windows"
    - "NGS rushing metrics (RYOE, efficiency) are extracted for RB with rolling windows"
    - "PFR pressure rate per QB is computed with rolling windows"
    - "PFR team blitz rate is aggregated from defender-level to team-level with rolling windows"
    - "QBR rolling windows (total QBR, points added) are computed for QB-only rows"
    - "Rolling windows use shift(1), groupby([player_id, season]), min_periods=3"
  artifacts:
    - path: "src/player_advanced_analytics.py"
      provides: "All 6 advanced metric compute functions plus player rolling utility"
      exports: ["apply_player_rolling", "compute_ngs_receiving_profile", "compute_ngs_passing_profile", "compute_ngs_rushing_profile", "compute_pfr_pressure_rate", "compute_pfr_team_blitz_rate", "compute_qbr_profile", "log_nan_coverage"]
    - path: "tests/test_player_advanced_analytics.py"
      provides: "Unit tests for all 6 PROF requirements plus rolling/join behavior"
      min_lines: 100
    - path: "src/config.py"
      provides: "SILVER_PLAYER_S3_KEYS entry for advanced_profiles"
      contains: "advanced_profiles"
  key_links:
    - from: "src/player_advanced_analytics.py"
      to: "src/team_analytics.py"
      via: "apply_player_rolling mirrors apply_team_rolling pattern"
      pattern: "shift\\(1\\).rolling.*min_periods=3"
    - from: "tests/test_player_advanced_analytics.py"
      to: "src/player_advanced_analytics.py"
      via: "pytest imports"
      pattern: "from player_advanced_analytics import"
---

<objective>
Create the player_advanced_analytics.py module with all NGS/PFR/QBR metric computation functions, the player-level rolling window utility, and comprehensive tests.

Purpose: This module provides the core computation logic for Phase 17. Each function takes a pre-filtered DataFrame (from Bronze) and returns processed metrics. The rolling utility adapts the proven team_analytics.apply_team_rolling() pattern for player-level grouping with min_periods=3. Config registration enables health checks and downstream consumption.

Output: src/player_advanced_analytics.py, tests/test_player_advanced_analytics.py, updated src/config.py
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-advanced-player-profiles/17-CONTEXT.md
@.planning/phases/17-advanced-player-profiles/17-RESEARCH.md

@src/team_analytics.py
@src/config.py

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From src/team_analytics.py:
```python
def apply_team_rolling(
    df: pd.DataFrame,
    stat_cols: List[str],
    windows: Optional[List[int]] = None,
) -> pd.DataFrame:
    """Apply shifted rolling averages and STD expanding average by team.
    Groups by ['team', 'season'], uses shift(1), min_periods=1, windows=[3,6].
    Creates {col}_roll{window} and {col}_std columns.
    """
```

From src/config.py:
```python
SILVER_PLAYER_S3_KEYS = {
    "usage_metrics": "players/usage/season={season}/week={week}/usage_{ts}.parquet",
    "opponent_rankings": "defense/positional/season={season}/week={week}/opp_rankings_{ts}.parquet",
    "rolling_averages": "players/rolling/season={season}/week={week}/rolling_{ts}.parquet",
}

SILVER_TEAM_S3_KEYS = {
    "pbp_metrics": "teams/pbp_metrics/season={season}/pbp_metrics_{ts}.parquet",
    "tendencies": "teams/tendencies/season={season}/tendencies_{ts}.parquet",
    "sos": "teams/sos/season={season}/sos_{ts}.parquet",
    "situational": "teams/situational/season={season}/situational_{ts}.parquet",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config registration and player_advanced_analytics.py module</name>
  <files>src/config.py, src/player_advanced_analytics.py, tests/test_player_advanced_analytics.py</files>
  <behavior>
    - apply_player_rolling: given a DataFrame with player_gsis_id/season/week and numeric cols, produces {col}_roll3, {col}_roll6, {col}_std columns grouped by [player_gsis_id, season] with shift(1) and min_periods=3
    - apply_player_rolling: first 3 weeks produce NaN for rolling columns (shift + min_periods=3)
    - apply_player_rolling: does not cross season boundaries (week 18 season N does not bleed into week 1 season N+1)
    - compute_ngs_receiving_profile: extracts avg_separation, catch_percentage, avg_intended_air_yards, avg_cushion, avg_yac, avg_expected_yac, avg_yac_above_expectation; filters week > 0; prefixes columns with ngs_
    - compute_ngs_passing_profile: extracts avg_time_to_throw, aggressiveness, avg_completed_air_yards, avg_intended_air_yards, avg_air_yards_differential, completion_percentage_above_expectation, expected_completion_percentage; filters week > 0; prefixes with ngs_
    - compute_ngs_rushing_profile: extracts rush_yards_over_expected, rush_yards_over_expected_per_att, efficiency, avg_time_to_los, rush_pct_over_expected; filters week > 0; prefixes with ngs_
    - compute_pfr_pressure_rate: extracts times_pressured_pct, times_sacked, times_hurried, times_hit, times_blitzed, passing_bad_throw_pct; prefixes with pfr_
    - compute_pfr_team_blitz_rate: aggregates defender-level rows to team level (sum of def_times_blitzed, def_times_hurried, def_sacks, def_pressures per team/season/week); prefixes with pfr_def_
    - compute_qbr_profile: extracts qbr_total, pts_added, qb_plays, epa_total; prefixes with qbr_
    - log_nan_coverage: logs % non-null for each advanced column at INFO level
    - All compute functions handle missing columns gracefully (return empty DataFrame with expected schema)
    - All compute functions apply rolling windows via apply_player_rolling
  </behavior>
  <action>
    1. In src/config.py, add an "advanced_profiles" entry to SILVER_PLAYER_S3_KEYS:
       ```python
       "advanced_profiles": "players/advanced/season={season}/advanced_profiles_{ts}.parquet",
       ```
       Add it right after the existing "rolling_averages" entry.

    2. Create tests/test_player_advanced_analytics.py with synthetic DataFrames:
       - Fixture: 2 players, 2 seasons, 6 weeks each (enough for rolling verification)
       - Test apply_player_rolling: verify roll3/roll6/std columns created, first 3 weeks are NaN, no cross-season leakage
       - Test each compute_ngs_* function: verify column extraction, ngs_ prefix, week 0 filtered out
       - Test compute_pfr_pressure_rate: verify pfr_ prefix, rolling applied
       - Test compute_pfr_team_blitz_rate: verify aggregation from defender-level to team-level, pfr_def_ prefix
       - Test compute_qbr_profile: verify qbr_ prefix, rolling applied
       - Test log_nan_coverage: verify logging output (use caplog fixture)
       - Test missing column handling: pass DataFrame missing expected columns, verify graceful empty return
       - Test left-join preservation: verify output row count matches input (no silent drops)

    3. Create src/player_advanced_analytics.py:
       - Import pandas, numpy, logging, typing (List, Optional)
       - Define column constants: NGS_RECEIVING_COLS, NGS_PASSING_COLS, NGS_RUSHING_COLS, PFR_PRESSURE_COLS, PFR_DEF_BLITZ_COLS, QBR_COLS
       - Implement apply_player_rolling():
         - Mirrors team_analytics.apply_team_rolling() but groups by [player_gsis_id, season]
         - Uses min_periods=3 (not 1 like team version -- per success criteria #5)
         - Creates {col}_roll3, {col}_roll6, {col}_std with shift(1)
         - Sort by [player_gsis_id, season, week] before rolling
       - Implement compute_ngs_receiving_profile(ngs_recv_df):
         - Filter week > 0 (exclude seasonal aggregates)
         - Select NGS_RECEIVING_COLS that exist in input
         - Rename with ngs_ prefix
         - Apply apply_player_rolling on prefixed columns
         - Return DataFrame with player_gsis_id, season, week, ngs_ columns + rolling
       - Implement compute_ngs_passing_profile(ngs_pass_df): same pattern with NGS_PASSING_COLS
       - Implement compute_ngs_rushing_profile(ngs_rush_df): same pattern with NGS_RUSHING_COLS
       - Implement compute_pfr_pressure_rate(pfr_pass_df):
         - Select PFR_PRESSURE_COLS that exist in input
         - Rename with pfr_ prefix
         - Apply rolling
         - Return with player name, team, season, week keys for later join
       - Implement compute_pfr_team_blitz_rate(pfr_def_df):
         - groupby([team, season, week]).agg(sum) for def_times_blitzed, def_times_hurried, def_sacks, def_pressures
         - Rename with pfr_def_ prefix
         - Apply rolling using team-level groupby (use apply_team_rolling from team_analytics for this)
         - Return team-level DataFrame (not player-level)
       - Implement compute_qbr_profile(qbr_df):
         - Select QBR_COLS that exist
         - Rename with qbr_ prefix
         - Apply rolling
         - Return with player name, team, season, week keys
       - Implement log_nan_coverage(df, advanced_cols):
         - For each col in advanced_cols that exists in df, log % non-null at INFO level
       - All compute functions: if required columns are entirely missing, log warning and return empty DataFrame with expected output columns (NaN-filled schema)

    Run tests RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_player_advanced_analytics.py -x -v</automated>
  </verify>
  <done>
    - src/config.py has "advanced_profiles" in SILVER_PLAYER_S3_KEYS
    - src/player_advanced_analytics.py exports all 8 functions listed in must_haves
    - All tests pass covering PROF-01 through PROF-06, rolling behavior, missing column handling, and NaN logging
    - Rolling windows use shift(1), groupby([player_gsis_id, season]), min_periods=3
    - Existing test suite still passes: python -m pytest tests/ -v
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_player_advanced_analytics.py -x -v` -- all new tests pass
- `python -m pytest tests/ -v` -- full suite still green (no regressions)
- `python -c "from player_advanced_analytics import apply_player_rolling, compute_ngs_receiving_profile, compute_ngs_passing_profile, compute_ngs_rushing_profile, compute_pfr_pressure_rate, compute_pfr_team_blitz_rate, compute_qbr_profile, log_nan_coverage; print('All imports OK')"` -- importable from src/
- `grep 'advanced_profiles' src/config.py` -- config registered
</verification>

<success_criteria>
- All 6 PROF requirements have corresponding compute functions with tests
- apply_player_rolling produces correct rolling columns with min_periods=3 and no cross-season leakage
- Config entry registered for advanced_profiles
- Full test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/17-advanced-player-profiles/17-01-SUMMARY.md`
</output>
