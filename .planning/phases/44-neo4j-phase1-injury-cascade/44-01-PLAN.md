---
phase: 44-neo4j-phase1-injury-cascade
plan: 01
type: execute
wave: 1
depends_on: ["43-01"]
files_modified:
  - docker-compose.yml
  - src/graph_db.py
  - src/graph_injury_cascade.py
  - src/graph_feature_extraction.py
  - src/player_feature_engineering.py
  - scripts/graph_ingestion.py
  - tests/test_graph_features.py
autonomous: true
requirements: [NEO4J-01, NEO4J-02, NEO4J-03, NEO4J-04, NEO4J-05, NEO4J-06]

must_haves:
  truths:
    - "docker-compose.yml starts Neo4j 5 Community with persistent volume and APOC plugin"
    - "GraphDB class connects to Neo4j with retry logic and is_available() health check"
    - "GraphDB gracefully degrades — callers get False from is_available() when Neo4j is down"
    - "identify_significant_injuries() finds starters (snap_pct > 50%) with Out/IR status"
    - "compute_redistribution() calculates target and carry shifts to teammates after starter injury"
    - "extract_graph_features() returns 4 columns: injury_cascade_target_boost, injury_cascade_carry_boost, teammate_injured_starter, historical_absorption_rate"
    - "Dual-path: Neo4j Cypher when available, pure-pandas fallback when not"
    - "graph_ingestion.py CLI loads Bronze data into Neo4j nodes and edges"
    - "player_feature_engineering.py step 11 calls extract_graph_features() with graceful fallback"
    - "29 new tests covering connection, cascade, extraction, fallback, and ingestion"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Neo4j 5 Community container configuration"
    - path: "src/graph_db.py"
      provides: "Connection manager with retry and graceful degradation"
      exports: ["GraphDB"]
    - path: "src/graph_injury_cascade.py"
      provides: "Injury cascade identification and redistribution computation"
      exports: ["identify_significant_injuries", "compute_redistribution"]
    - path: "src/graph_feature_extraction.py"
      provides: "Dual-path feature extraction (Neo4j + pandas fallback)"
      exports: ["extract_graph_features"]
    - path: "scripts/graph_ingestion.py"
      provides: "CLI for Bronze to Neo4j ingestion"
    - path: "tests/test_graph_features.py"
      provides: "Tests for all graph modules"
      min_lines: 200
---

<objective>
Build Neo4j infrastructure (Docker, connection manager) and the first graph-derived features (injury cascade / target redistribution) with dual-path execution (Neo4j + pandas fallback). Integrate into the player feature engineering pipeline.

Purpose: Establish the graph database foundation and deliver the first relational signal (injury-driven volume redistribution) that flat tables cannot capture efficiently.
Output: 5 new source files, 1 Docker config, integration into existing feature pipeline, 29 tests.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-neo4j-phase1-injury-cascade/44-CONTEXT.md
@.planning/phases/44-neo4j-phase1-injury-cascade/44-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Docker Compose and GraphDB connection manager</name>
  <files>docker-compose.yml, src/graph_db.py, tests/test_graph_features.py</files>
  <action>
    Create docker-compose.yml for Neo4j 5 Community (neo4j:5-community image, bolt:7687, browser:7474, APOC plugin, persistent volume, 512MB heap). Create src/graph_db.py with GraphDB class: __init__(uri, user, password), connect(), close(), is_available(), run_query(cypher, params), retry logic (3 attempts with exponential backoff). Tests: connection init, is_available returns False when no server, query execution mock.
  </action>
  <done>Neo4j Docker config and connection manager with retry and graceful degradation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Injury cascade identification and redistribution</name>
  <files>src/graph_injury_cascade.py, tests/test_graph_features.py</files>
  <action>
    Create src/graph_injury_cascade.py with:
    - identify_significant_injuries(injuries_df, snap_df, min_snap_pct=0.50): find players with Out/IR status who had snap_pct > threshold in prior week
    - compute_redistribution(injured_players, weekly_stats_df, rosters_df): for each injured starter, find same-position teammates, calculate target/carry increase in weeks after injury vs weeks before
    Tests: significant injury detection with starter threshold, redistribution calculation with mock data, edge cases (no teammates, mid-season trade).
  </action>
  <done>Injury cascade logic identifies starters and computes volume redistribution to teammates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dual-path graph feature extraction</name>
  <files>src/graph_feature_extraction.py, tests/test_graph_features.py</files>
  <action>
    Create src/graph_feature_extraction.py with extract_graph_features(player_df, season, week, graph_db=None):
    - If graph_db and graph_db.is_available(): use Cypher queries to compute 4 features
    - Else: use pandas fallback computing same 4 features from DataFrames
    - Returns DataFrame with: injury_cascade_target_boost, injury_cascade_carry_boost, teammate_injured_starter, historical_absorption_rate
    Tests: pandas fallback produces valid features, Neo4j path mocked, output schema validation, zero-fill when no injuries.
  </action>
  <done>Dual-path feature extraction with 4 graph features and pandas fallback.</done>
</task>

<task type="auto">
  <name>Task 4: Graph ingestion CLI</name>
  <files>scripts/graph_ingestion.py</files>
  <action>
    Create scripts/graph_ingestion.py with argparse: --season, --weeks (optional, default all). Loads Bronze injuries, rosters, player_weekly. Creates Player nodes (player_id, name, position, team), TEAMMATES_WITH edges, INJURED_REPLACING edges. Uses MERGE for idempotency.
  </action>
  <done>CLI for Bronze to Neo4j data loading.</done>
</task>

<task type="auto">
  <name>Task 5: Integrate into player_feature_engineering.py</name>
  <files>src/player_feature_engineering.py, tests/test_graph_features.py</files>
  <action>
    Add step 11 to assemble_player_features(): call extract_graph_features() and left-join the 4 new columns. Graceful fallback: if extract_graph_features raises any exception, fill 4 columns with 0.0 and log warning.
  </action>
  <done>Graph features integrated into player feature pipeline with graceful fallback.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_graph_features.py -x -v` -- all graph tests pass
- `python -m pytest tests/ -v` -- full suite green (698+ tests)
- `docker compose config` validates docker-compose.yml (if Docker available)
- `grep -q "extract_graph_features" src/player_feature_engineering.py` -- integration exists
</verification>

<success_criteria>
1. Neo4j 5 Community runnable via docker compose up
2. GraphDB class handles connection failures gracefully
3. Injury cascade identifies significant injuries and computes redistribution
4. 4 graph features extracted via Neo4j or pandas fallback
5. Player feature engineering integrates graph features as step 11
6. 29 new tests, 698 total passing
</success_criteria>

<output>
After completion, create `.planning/phases/44-neo4j-phase1-injury-cascade/44-01-SUMMARY.md`
</output>
