---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nfl_data_integration.py
  - scripts/bronze_ingestion_simple.py
  - .planning/phases/02-core-pbp-ingestion/02-01-SUMMARY.md
autonomous: true
requirements: [PBP-01, PBP-02, PBP-03, PBP-04]

must_haves:
  truths:
    - "validate_data() season check is dynamic — accepts 2026 and 2027 without code changes"
    - "bronze_ingestion_simple.py uses format_validation_output() — no inline formatting duplication"
    - "02-01-SUMMARY.md has requirements-completed frontmatter for PBP-01 through PBP-04"
    - "pyarrow is installed and test_generate_inventory.py collects successfully"
  artifacts:
    - path: "src/nfl_data_integration.py"
      provides: "Dynamic season validation via get_max_season()"
      contains: "get_max_season()"
    - path: "scripts/bronze_ingestion_simple.py"
      provides: "DRY validation output using format_validation_output()"
      contains: "format_validation_output"
  key_links:
    - from: "src/nfl_data_integration.py"
      to: "src/config.py"
      via: "import get_max_season"
      pattern: "from src\\.config import get_max_season"
    - from: "scripts/bronze_ingestion_simple.py"
      to: "src/nfl_data_adapter.py"
      via: "import format_validation_output"
      pattern: "from src\\.nfl_data_adapter import.*format_validation_output"
---

<objective>
Close four tech debt items from the v1.0 milestone audit: fix hardcoded season bound in validate_data(), replace inline validation formatting with existing helper, and verify two already-resolved items (SUMMARY frontmatter and pyarrow dependency).

Purpose: Clean up stale code patterns that could cause bugs in future seasons (hardcoded 2025) and eliminate code duplication (inline validation formatting).
Output: Two production code fixes plus verification that SUMMARY frontmatter and pyarrow are already correct.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md

<interfaces>
<!-- Existing functions the executor needs -->

From src/config.py:
```python
def get_max_season() -> int:
    """Returns current year + 1 as the maximum valid season."""
```

From src/nfl_data_adapter.py:
```python
def format_validation_output(result: Dict[str, any]) -> Optional[str]:
    """Format a validation result dict into human-readable output lines.
    Returns formatted string or None if result is falsy."""
```

From scripts/bronze_ingestion_simple.py (current import, line 20):
```python
from src.nfl_data_adapter import NFLDataAdapter
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded season bound with get_max_season()</name>
  <files>src/nfl_data_integration.py</files>
  <action>
1. Add import at top of file (after existing imports around line 11):
   `from src.config import get_max_season`

2. Replace line 378:
   FROM: `invalid_seasons = [s for s in seasons if s < 1999 or s > 2025]`
   TO:   `invalid_seasons = [s for s in seasons if s < 1999 or s > get_max_season()]`

3. Verify no circular import by running:
   `python -c "from src.nfl_data_integration import NFLDataFetcher; print('OK')"`

Note: nfl_data_integration.py currently does NOT import from src.config. The file imports nfl_data_py, pandas, typing, datetime, and logging. Adding src.config is safe — no circular dependency risk since config.py has no imports from this module.
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "from src.nfl_data_integration import NFLDataFetcher; print('import OK')" && python -m pytest tests/ -x -q -k "max_season or validate" 2>&1 | tail -5</automated>
  </verify>
  <done>validate_data() uses get_max_season() instead of hardcoded 2025. Import succeeds without circular dependency. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline validation formatting with format_validation_output()</name>
  <files>scripts/bronze_ingestion_simple.py</files>
  <action>
1. Update the existing import on line 20 to also import format_validation_output:
   FROM: `from src.nfl_data_adapter import NFLDataAdapter`
   TO:   `from src.nfl_data_adapter import NFLDataAdapter, format_validation_output`

2. Replace lines 342-353 (the inline validation block) with:
   ```python
   # --- Validate schema ---
   try:
       val_result = adapter.validate_data(df, args.data_type)
       output = format_validation_output(val_result)
       if output:
           print(output)
   except Exception as e:
       print(f"  Warning Validation error: {e}")
   ```

   This replaces 10 lines of inline formatting with 6 lines that delegate to the existing tested helper. The try/except wrapper is preserved to ensure validation never blocks save (per Phase 6 decision).

3. Verify the two already-resolved audit items (no code changes needed):
   - Confirm 02-01-SUMMARY.md line 43 has `requirements-completed: [PBP-01, PBP-02, PBP-03, PBP-04]`
   - Confirm `python -c "import pyarrow"` succeeds and `pytest --collect-only tests/test_generate_inventory.py` collects 8 tests
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "from scripts.bronze_ingestion_simple import *; print('import OK')" 2>/dev/null; python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>bronze_ingestion_simple.py uses format_validation_output() instead of inline formatting. Full test suite passes. SUMMARY frontmatter and pyarrow confirmed present (no changes needed).</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` — full suite green (71+ tests)
2. `grep "get_max_season" src/nfl_data_integration.py` — confirms dynamic season
3. `grep "format_validation_output" scripts/bronze_ingestion_simple.py` — confirms helper usage
4. `grep "requirements-completed" .planning/phases/02-core-pbp-ingestion/02-01-SUMMARY.md` — confirms PBP-01-04
5. `python -c "import pyarrow; print(pyarrow.__version__)"` — confirms pyarrow available
</verification>

<success_criteria>
- validate_data() season check uses get_max_season() — no hardcoded year
- bronze_ingestion_simple.py delegates to format_validation_output() — no inline duplication
- Full test suite passes with no regressions
- All four v1.0 audit tech debt items resolved or verified
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
