---
phase: 14-bronze-cosmetic-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/bronze_cosmetic_cleanup.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/research/SUMMARY.md
  - .planning/research/PITFALLS.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "player_weekly 2016-2019 files stored at season level, not under week=0/"
    - "draft_picks has exactly 1 file per season (no duplicate append artifacts)"
    - "GITHUB_TOKEN documentation accurately reflects that nfl-data-py does NOT use it"
  artifacts:
    - path: "scripts/bronze_cosmetic_cleanup.py"
      provides: "One-time cleanup script for filesystem normalization"
      contains: "def normalize_player_weekly"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected SETUP-02 wording"
      contains: "StatsPlayerAdapter"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 8 success criteria wording"
  key_links:
    - from: "scripts/bronze_cosmetic_cleanup.py"
      to: "data/bronze/players/weekly/season=201*/week=0/"
      via: "shutil.move + rmdir"
      pattern: "shutil\\.move"
    - from: "scripts/bronze_cosmetic_cleanup.py"
      to: "data/bronze/draft_picks/season=*/"
      via: "pathlib unlink"
      pattern: "\\.unlink\\(\\)"
---

<objective>
Clean up 3 cosmetic inconsistencies from backfill phases: move player_weekly 2016-2019 files out of week=0/ subdirectories to season level, remove duplicate draft_picks files (keeping newest per season), and correct GITHUB_TOKEN documentation across planning files.

Purpose: Eliminate confusion when browsing data directories and reading docs. No runtime impact -- download_latest_parquet() and Silver readers already handle these correctly.
Output: Cleanup script committed, filesystem normalized, documentation corrected.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bronze-cosmetic-cleanup/14-CONTEXT.md
@.planning/phases/14-bronze-cosmetic-cleanup/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run filesystem cleanup script</name>
  <files>scripts/bronze_cosmetic_cleanup.py</files>
  <action>
Create `scripts/bronze_cosmetic_cleanup.py` using Python stdlib only (pathlib, shutil, argparse). The script handles two operations:

1. **player_weekly normalization (seasons 2016-2019):**
   - For each season in [2016, 2017, 2018, 2019], check if `data/bronze/players/weekly/season={year}/week=0/` exists
   - Move all `.parquet` files from `week=0/` up one level to `data/bronze/players/weekly/season={year}/`
   - If destination file already exists, skip with a warning (do not overwrite)
   - Remove empty `week=0/` directory after move
   - Print each operation: "MOVED: {src} -> {dest}", "REMOVED empty dir: {path}", or "SKIP (already exists): {dest}"

2. **draft_picks deduplication (seasons 2000-2025):**
   - For each `data/bronze/draft_picks/season=*/` directory, glob `draft_picks_*.parquet` files
   - Sort lexicographically (YYYYMMDD_HHMMSS format ensures chronological order)
   - If more than 1 file, keep the LAST (newest), delete all others
   - Print each operation: "DELETED: {path}", "KEPT: {path}"

Script behavior:
- Default mode: dry-run (prints what WOULD happen, no filesystem changes)
- `--execute` flag: actually performs moves and deletes
- Print summary at end: "Summary: {N} files moved, {M} files deleted, {K} dirs removed"
- Exit code 0 on success

After creating the script, run it with `--execute` to perform the actual cleanup.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && python scripts/bronze_cosmetic_cleanup.py --execute && test ! -d data/bronze/players/weekly/season=2016/week=0 && test ! -d data/bronze/players/weekly/season=2017/week=0 && test ! -d data/bronze/players/weekly/season=2018/week=0 && test ! -d data/bronze/players/weekly/season=2019/week=0 && ls data/bronze/players/weekly/season=2016/*.parquet && for d in data/bronze/draft_picks/season=*/; do count=$(ls "$d"/*.parquet 2>/dev/null | wc -l); if [ "$count" -ne 1 ]; then echo "FAIL: $d has $count files" && exit 1; fi; done && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
  - No `week=0/` directories exist under player_weekly for seasons 2016-2019
  - Each season 2016-2019 has its parquet file directly at season level
  - Each draft_picks season directory contains exactly 1 parquet file (the newest)
  - Cleanup script is committed for reproducibility reference
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct GITHUB_TOKEN documentation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/research/SUMMARY.md, .planning/research/PITFALLS.md</files>
  <action>
Update 4 planning documents to correct inaccurate claims about GITHUB_TOKEN usage. The key fact: nfl-data-py v0.3.3 does NOT use GITHUB_TOKEN for its downloads. The token IS used by (1) StatsPlayerAdapter in src/nfl_data_adapter.py for direct GitHub API calls, (2) GitHub Actions workflows, and (3) gh CLI.

Do NOT modify historical phase files (Phase 8 research/verification docs already accurately note the limitation).

Specific edits:

1. `.planning/REQUIREMENTS.md` line 13:
   - FROM: `GITHUB_TOKEN configured for nfl-data-py downloads to avoid rate limiting`
   - TO: `GITHUB_TOKEN configured for StatsPlayerAdapter, GitHub Actions, and gh CLI (note: nfl-data-py v0.3.3 does NOT use it for downloads)`

2. `.planning/ROADMAP.md` line 45 (Phase 8 success criteria #2):
   - FROM: `GITHUB_TOKEN is set and nfl-data-py downloads use authenticated requests (5000/hr rate limit)`
   - TO: `GITHUB_TOKEN is set for StatsPlayerAdapter and GitHub tooling (note: nfl-data-py itself does not use it)`

3. `.planning/research/SUMMARY.md` line 14:
   - The sentence mentioning "mitigated by setting GITHUB_TOKEN" -- add clarifying parenthetical: "(for StatsPlayerAdapter and gh CLI; nfl-data-py itself does not use it)"

4. `.planning/research/PITFALLS.md`:
   - Line 114: After "Set GITHUB_TOKEN environment variable", add note: "(used by StatsPlayerAdapter and gh CLI; nfl-data-py v0.3.3 does not read this token)"
   - Line 341 table entry: Update to clarify the token applies to direct GitHub API calls, not nfl-data-py
   - Do NOT modify every reference -- only the most prominent claims that could mislead a new reader

After edits, run existing test suite to ensure no regressions.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && grep "StatsPlayerAdapter" .planning/REQUIREMENTS.md && grep -c "nfl-data-py.*does NOT use" .planning/REQUIREMENTS.md && grep "StatsPlayerAdapter" .planning/ROADMAP.md && python -m pytest tests/ -v -x --timeout=60</automated>
  </verify>
  <done>
  - REQUIREMENTS.md SETUP-02 accurately describes GITHUB_TOKEN scope
  - ROADMAP.md Phase 8 success criteria #2 no longer claims nfl-data-py uses the token
  - research/SUMMARY.md and PITFALLS.md have clarifying notes on prominent GITHUB_TOKEN references
  - All 71 existing tests still pass
  </done>
</task>

</tasks>

<verification>
1. Filesystem: `find data/bronze/players/weekly -name "week=0" -type d` returns empty
2. Filesystem: `for d in data/bronze/draft_picks/season=*/; do ls "$d"/*.parquet | wc -l; done` shows all 1s
3. Documentation: `grep "does NOT use" .planning/REQUIREMENTS.md` returns match
4. Regression: `python -m pytest tests/ -v` -- all 71 tests pass
</verification>

<success_criteria>
- player_weekly 2016-2019 files at season level (no week=0/ subdirs)
- draft_picks has 1 file per season across all 26 seasons
- GITHUB_TOKEN documentation corrected in 4 planning files
- Cleanup script committed for reference
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-bronze-cosmetic-cleanup/14-01-SUMMARY.md`
</output>
