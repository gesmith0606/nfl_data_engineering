---
phase: 37-holdout-reset-and-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - models/ensemble/
  - .planning/phases/37-holdout-reset-and-baseline/BASELINE.md
autonomous: true
requirements:
  - HOLD-03

must_haves:
  truths:
    - "Ensemble is retrained on 2016-2024 data (2025 excluded)"
    - "Holdout evaluation against 2025 produces ATS accuracy, profit, and CLV metrics"
    - "Baseline metrics are documented for Phase 38 comparison"
  artifacts:
    - path: "models/ensemble/"
      provides: "Retrained ensemble model files (XGB, LGB, CB, Ridge, meta)"
    - path: ".planning/phases/37-holdout-reset-and-baseline/BASELINE.md"
      provides: "Documented 2025 holdout baseline metrics"
      contains: "ATS"
  key_links:
    - from: "scripts/train_ensemble.py"
      to: "models/ensemble/"
      via: "model save after training"
      pattern: "models/ensemble"
    - from: "scripts/backtest_predictions.py"
      to: ".planning/phases/37-holdout-reset-and-baseline/BASELINE.md"
      via: "metrics documented after --holdout run"
      pattern: "ATS.*accuracy"
---

<objective>
Retrain the ensemble on 2016-2024 (with 2025 as sealed holdout), evaluate against 2025, and document the baseline metrics that Phase 38's ablation will compare against.

Purpose: Establish the sealed 2025 baseline so Phase 38 can render a structurally valid ship-or-skip verdict on market features.
Output: Retrained model files in models/ensemble/, BASELINE.md with exact metrics.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-holdout-reset-and-baseline/37-CONTEXT.md
@.planning/phases/37-holdout-reset-and-baseline/37-01-SUMMARY.md

<interfaces>
<!-- Config after 37-01 -->
From src/config.py (after plan 37-01):
```python
HOLDOUT_SEASON = 2025
PREDICTION_SEASONS = list(range(2016, HOLDOUT_SEASON + 1))
TRAINING_SEASONS = list(range(2016, HOLDOUT_SEASON))
VALIDATION_SEASONS = [s for s in range(2019, HOLDOUT_SEASON)]
```

<!-- Training CLI -->
From scripts/train_ensemble.py:
```python
# Usage: python scripts/train_ensemble.py
# Trains XGB+LGB+CB+Ridge ensemble using TRAINING_SEASONS from config
# Saves to models/ensemble/
# --tune flag enables Optuna (NOT used per D-07)
```

<!-- Backtest CLI -->
From scripts/backtest_predictions.py:
```python
# Usage: python scripts/backtest_predictions.py --holdout
# Evaluates model against HOLDOUT_SEASON
# Outputs: ATS accuracy, profit, ROI, CLV metrics
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document 2024 baseline, retrain ensemble, and evaluate 2025 holdout</name>
  <files>models/ensemble/, .planning/phases/37-holdout-reset-and-baseline/BASELINE.md</files>
  <read_first>
    - src/config.py (confirm HOLDOUT_SEASON is 2025 after 37-01)
    - scripts/train_ensemble.py (understand CLI flags)
    - scripts/backtest_predictions.py (understand --holdout and --ensemble flags)
    - src/ensemble_training.py (understand holdout guard, model save paths)
    - src/prediction_backtester.py (understand evaluate_holdout output format)
  </read_first>
  <action>
Per D-03, D-04, D-05, D-07, D-08, D-09, D-10, D-11:

**Step 1 — Document the old 2024 baseline (per D-03):**
Record the known 2024 metrics before any changes:
- 53.0% ATS accuracy, +$3.09 profit, +1.2% ROI (from CLAUDE.md and CONTEXT.md)
These are the v2.0 production model results on the 2024 holdout.

**Step 2 — Retrain ensemble (per D-05, D-07, D-08):**
```bash
source venv/bin/activate
python scripts/train_ensemble.py
```
- This trains on TRAINING_SEASONS (2016-2024) since config now has HOLDOUT_SEASON=2025
- Do NOT use `--tune` flag (per D-07 — no hyperparameter re-tuning)
- Model saves to `models/ensemble/` (overwrites v2.1 model per D-08)
- Verify training completes without errors
- Verify the holdout guard rejects any 2025 data in training folds

**Step 3 — Evaluate against 2025 holdout (per D-09):**
```bash
python scripts/backtest_predictions.py --holdout --ensemble
```
- Captures ATS accuracy, profit, ROI, and CLV metrics for 2025

**Step 4 — Investigate if needed (per D-11):**
If 2025 holdout ATS < 50%, investigate data issues before proceeding. Check:
- Feature vector completeness for 2025 games
- NaN rates in key features
- 2025 game count (should be ~272 REG games)

**Step 5 — Write BASELINE.md (per D-10):**
Create `.planning/phases/37-holdout-reset-and-baseline/BASELINE.md` with:

```markdown
# Phase 37 Baseline — Sealed 2025 Holdout

## Prior Baseline (2024 Holdout — v2.0)
- ATS Accuracy: 53.0%
- Profit: +$3.09
- ROI: +1.2%

## New Baseline (2025 Holdout — v2.2)
- Training: 2016-2024 ({N} seasons)
- Holdout: 2025 (sealed)
- Model: P30 Ensemble (XGB+LGB+CB+Ridge)
- Hyperparameters: Unchanged from v2.0 (no --tune)

### Results
- ATS Accuracy: {X}%
- Total Profit: ${X}
- ROI: {X}%
- CLV: {metrics from backtest output}

### Notes
- 2025 injuries data absent (Phase 35 finding) — affects injury-related features
- 2024 now included in training (was previously sealed) — 1 extra training season
- Market features: NaN for 2022-2025 games (FinnedAI covers 2016-2021 only)

## Reference
This baseline is the comparison target for Phase 38 market feature ablation.
The ship-or-skip gate requires strict > on holdout ATS accuracy.
```

Fill in actual metrics from the backtest output.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -c "
import os, json
# Check model files exist
model_dir = 'models/ensemble'
assert os.path.isdir(model_dir), f'{model_dir} not found'
files = os.listdir(model_dir)
assert len(files) >= 1, f'No model files in {model_dir}'
print(f'Model files: {files}')

# Check BASELINE.md exists and has key content
baseline = '.planning/phases/37-holdout-reset-and-baseline/BASELINE.md'
assert os.path.isfile(baseline), f'{baseline} not found'
content = open(baseline).read()
assert 'ATS Accuracy' in content, 'Missing ATS Accuracy in BASELINE.md'
assert '2025' in content, 'Missing 2025 reference in BASELINE.md'
assert '53.0%' in content, 'Missing prior 2024 baseline (53.0%) in BASELINE.md'
print('BASELINE.md OK')
print('All checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `ls models/ensemble/` shows model files (at least one .pkl or .joblib)
    - `grep "ATS Accuracy" .planning/phases/37-holdout-reset-and-baseline/BASELINE.md` returns matches for both 2024 and 2025 baselines
    - `grep "53.0%" .planning/phases/37-holdout-reset-and-baseline/BASELINE.md` confirms prior baseline documented
    - `grep "2025" .planning/phases/37-holdout-reset-and-baseline/BASELINE.md` confirms new holdout documented
    - BASELINE.md contains actual numeric metrics (not placeholders) for the 2025 holdout evaluation
    - Training log shows 2025 excluded from training folds (holdout guard)
  </acceptance_criteria>
  <done>Ensemble retrained on 2016-2024, 2025 holdout evaluated, BASELINE.md documents exact ATS accuracy, profit, ROI, and CLV metrics for both old (2024) and new (2025) holdouts</done>
</task>

</tasks>

<verification>
1. `ls models/ensemble/` — model files present and recently modified
2. `cat .planning/phases/37-holdout-reset-and-baseline/BASELINE.md` — contains actual metrics (not placeholders)
3. `grep "ATS Accuracy" BASELINE.md` — both 2024 and 2025 baselines documented
4. Training output confirms 2025 season was excluded from all training folds
</verification>

<success_criteria>
- Ensemble model retrained on 2016-2024 and saved to models/ensemble/
- 2025 holdout evaluation produces ATS accuracy, profit, ROI, and CLV metrics
- BASELINE.md created with exact metrics for both 2024 (prior) and 2025 (new) holdouts
- Phase 38 can reference BASELINE.md for ship-or-skip comparison
</success_criteria>

<output>
After completion, create `.planning/phases/37-holdout-reset-and-baseline/37-02-SUMMARY.md`
</output>
