---
phase: 01-infrastructure-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - src/nfl_data_adapter.py
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-05

must_haves:
  truths:
    - "Season validation accepts 2026 (and future years) without code changes"
    - "All nfl-data-py import_* calls are isolated in a single adapter module"
    - "Per-data-type season ranges are defined in config (NGS: 2016+, PFR: 2018+, etc.)"
  artifacts:
    - path: "src/nfl_data_adapter.py"
      provides: "Adapter isolating all nfl-data-py import_* calls"
      exports: ["NFLDataAdapter"]
    - path: "src/config.py"
      provides: "Dynamic season validation and per-type season ranges"
      contains: "DATA_TYPE_SEASON_RANGES"
  key_links:
    - from: "src/nfl_data_adapter.py"
      to: "nfl_data_py"
      via: "import nfl_data_py"
      pattern: "import nfl_data_py"
    - from: "src/config.py"
      to: "datetime"
      via: "dynamic max season"
      pattern: "datetime.date.today"
---

<objective>
Create the foundational config and adapter layer that all subsequent data ingestion depends on.

Purpose: INFRA-02 (dynamic season validation), INFRA-03 (adapter layer for nfl-data-py isolation), and INFRA-05 (per-type season ranges) are prerequisites for the registry and local-first work in Plan 02.
Output: `src/nfl_data_adapter.py` with NFLDataAdapter class, updated `src/config.py` with dynamic season cap and DATA_TYPE_SEASON_RANGES.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/config.py
@src/nfl_data_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dynamic season validation and per-type season ranges to config</name>
  <files>src/config.py</files>
  <action>
1. Replace any hardcoded max season with a dynamic function:
   ```python
   import datetime
   def get_max_season() -> int:
       return datetime.date.today().year + 1
   ```
2. Add DATA_TYPE_SEASON_RANGES dict mapping each data type to its (min_season, max_season_fn) tuple. Use get_max_season for the upper bound. Include ALL existing types plus new ones:
   - schedules: 1999+
   - pbp: 1999+
   - player_weekly: 2002+
   - player_seasonal: 2002+
   - snap_counts: 2012+
   - injuries: 2009+
   - rosters: 2002+
   - teams: 1999+
   - ngs: 2016+
   - pfr_weekly: 2018+
   - pfr_seasonal: 2018+
   - qbr: 2006+
   - depth_charts: 2001+
   - draft_picks: 2000+
   - combine: 2000+
3. Add a helper function `validate_season_for_type(data_type: str, season: int) -> bool` that checks if a season is within the valid range for a data type.
4. Keep all existing config unchanged (SCORING_CONFIGS, S3 paths, etc.).
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -c "from src.config import get_max_season, DATA_TYPE_SEASON_RANGES, validate_season_for_type; assert get_max_season() >= 2026; assert validate_season_for_type('ngs', 2020); assert not validate_season_for_type('ngs', 2010); assert validate_season_for_type('schedules', 2026); print('config OK')"</automated>
  </verify>
  <done>get_max_season() returns current_year+1 dynamically; DATA_TYPE_SEASON_RANGES has entries for all 15 data types; validate_season_for_type works correctly for boundary cases.</done>
</task>

<task type="auto">
  <name>Task 2: Create nfl_data_adapter.py isolating all nfl-data-py calls</name>
  <files>src/nfl_data_adapter.py</files>
  <action>
Create `src/nfl_data_adapter.py` with class NFLDataAdapter that wraps every nfl-data-py import_* function used in the project. This is the ONLY module that should `import nfl_data_py`.

The adapter class should have methods matching the existing NFLDataFetcher usage plus new types:
- fetch_schedules(seasons) -> wraps nfl.import_schedules(seasons)
- fetch_weekly_data(seasons, columns=None) -> wraps nfl.import_weekly_data(seasons, columns)
- fetch_seasonal_data(seasons) -> wraps nfl.import_seasonal_data(seasons)
- fetch_snap_counts(season, week) -> wraps nfl.import_snap_counts(season, week)  (note: singular params)
- fetch_injuries(seasons, weeks=None) -> wraps nfl.import_injuries(seasons)
- fetch_rosters(seasons) -> wraps nfl.import_seasonal_rosters(seasons) (NOT import_rosters -- see MEMORY.md)
- fetch_team_descriptions() -> wraps nfl.import_team_desc()
- fetch_pbp(seasons, columns=None, downcast=True) -> wraps nfl.import_pbp_data(seasons, columns=columns, downcast=downcast)
- fetch_ngs(seasons, stat_type) -> wraps nfl.import_ngs_data(stat_type=stat_type, years=seasons)
- fetch_pfr_weekly(seasons, s_type) -> wraps nfl.import_weekly_pfr(s_type=s_type, years=seasons)
- fetch_pfr_seasonal(seasons, s_type) -> wraps nfl.import_seasonal_pfr(s_type=s_type, years=seasons)
- fetch_qbr(seasons, frequency='weekly') -> wraps nfl.import_qbr(years=seasons, frequency=frequency)
- fetch_depth_charts(seasons) -> wraps nfl.import_depth_charts(seasons)
- fetch_draft_picks(seasons) -> wraps nfl.import_draft_picks(seasons)
- fetch_combine(seasons) -> wraps nfl.import_combine_data(seasons)

Each method should:
1. Import config's validate_season_for_type and validate seasons before calling nfl-data-py
2. Wrap the call in try/except for ImportError (library missing) and general exceptions (API timeout)
3. Log using standard logging module
4. Return pd.DataFrame

Add type hints and Google-style docstrings.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -c "from src.nfl_data_adapter import NFLDataAdapter; a = NFLDataAdapter(); methods = [m for m in dir(a) if m.startswith('fetch_')]; assert len(methods) >= 15, f'Only {len(methods)} fetch methods'; print(f'{len(methods)} fetch methods found - OK')"</automated>
  </verify>
  <done>NFLDataAdapter class exists with 15+ fetch methods wrapping all nfl-data-py calls; no other module in the project needs to import nfl_data_py directly; season validation integrated.</done>
</task>

</tasks>

<verification>
- `python -c "from src.config import get_max_season; print(get_max_season())"` prints >= 2027
- `python -c "from src.nfl_data_adapter import NFLDataAdapter"` imports without error
- No other src/ file besides nfl_data_adapter.py imports nfl_data_py (existing NFLDataFetcher will be updated in Phase 2+)
</verification>

<success_criteria>
1. Dynamic season cap works without any hardcoded year
2. 15 data types have season ranges in config
3. NFLDataAdapter has a fetch method for every data type
4. Adapter is the single point of contact with nfl-data-py
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-prerequisites/01-01-SUMMARY.md`
</output>
