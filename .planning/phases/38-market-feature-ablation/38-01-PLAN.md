---
phase: 38-market-feature-ablation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-market-feature-ablation/VERDICT.md
autonomous: true
requirements:
  - HOLD-04

must_haves:
  truths:
    - "Ablation script completes without error, training both P30 baseline and market-augmented ensemble on 2016-2024"
    - "SHAP importance report identifies relative contribution of opening_spread and opening_total"
    - "Ship-or-skip verdict is rendered with exact ATS accuracy, profit, ROI, and CLV metrics for both models"
    - "VERDICT.md documents the definitive conclusion for v2.2"
  artifacts:
    - path: "models/ensemble_ablation/metadata.json"
      provides: "Ablation ensemble metadata with selected features and CV scores"
    - path: ".planning/phases/38-market-feature-ablation/VERDICT.md"
      provides: "Definitive market feature verdict with metrics"
      contains: "VERDICT"
  key_links:
    - from: "scripts/ablation_market_features.py"
      to: "models/ensemble_ablation/"
      via: "train_ensemble with ensemble_dir=ABLATION_DIR"
      pattern: "ABLATION_DIR"
    - from: "scripts/ablation_market_features.py"
      to: "src/config.py"
      via: "HOLDOUT_SEASON import"
      pattern: "HOLDOUT_SEASON"
---

<objective>
Run the definitive market feature ablation on the 2025 sealed holdout and deliver a ship-or-skip verdict.

Purpose: Determine whether market features (opening_spread, opening_total) improve game prediction accuracy with 6 seasons of market training data and a fresh holdout -- the structurally valid test that v2.1 could not provide.
Output: VERDICT.md with exact metrics, SHAP report, and definitive conclusion.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-market-feature-ablation/38-CONTEXT.md
@.planning/phases/37-holdout-reset-and-baseline/BASELINE.md

<interfaces>
<!-- The ablation script is fully self-contained. Key imports and contracts: -->

From scripts/ablation_market_features.py:
```python
def run_ablation(
    dry_run: bool = False,
    candidate_counts: Optional[List[int]] = None,
    correlation_threshold: float = 0.90,
) -> Dict[str, Any]:
    """Returns dict with baseline_ats, ablation_ats, baseline_profit,
    ablation_profit, verdict, shap_top_20, feature_count_baseline,
    feature_count_ablation."""

def compute_ship_or_skip(baseline_ats: float, ablation_ats: float) -> str:
    """Returns 'SHIP' if ablation_ats > baseline_ats, else 'SKIP'."""
```

From src/config.py:
```python
HOLDOUT_SEASON = 2025
TRAINING_SEASONS = list(range(2016, HOLDOUT_SEASON))  # 2016-2024
```

Baseline to beat (from BASELINE.md):
- ATS Accuracy: 51.7%
- Profit: -$3.73
- ROI: -1.38%
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run market feature ablation</name>
  <files>models/ensemble_ablation/metadata.json</files>
  <read_first>
    - scripts/ablation_market_features.py
    - src/config.py (verify HOLDOUT_SEASON=2025, TRAINING_SEASONS)
    - .planning/phases/37-holdout-reset-and-baseline/BASELINE.md
  </read_first>
  <action>
Run the existing ablation script per D-01 through D-06:

```bash
source venv/bin/activate
python scripts/ablation_market_features.py
```

This will (per the existing script):
1. Assemble multi-year features (2016-2025)
2. Evaluate the production P30 ensemble on 2025 holdout (baseline)
3. Run SHAP-based feature selection WITH market features as candidates (per D-04, using existing P30 hyperparameters -- no tuning)
4. Retrain ablation ensemble to `models/ensemble_ablation/` (per D-03)
5. Evaluate ablation ensemble on 2025 holdout
6. Compute ship-or-skip verdict (strict > on ATS accuracy, per D-02)
7. Print comparison report with SHAP importance (per D-05, D-06)
8. Apply decision (copy to production only if SHIP)

Capture the full console output -- it contains the ablation report, SHAP table, and verdict needed for Task 2.

Before running, verify config is correct:
- `HOLDOUT_SEASON` equals 2025 in config.py
- `models/ensemble/` contains the Phase 37 production model (metadata.json exists)

After running, verify:
- `models/ensemble_ablation/metadata.json` exists
- The script printed a verdict line containing either "SHIP" or "SKIP"
- The SHAP report lists features with importance scores
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "import json; m=json.load(open('models/ensemble_ablation/metadata.json')); print(f'Features: {len(m.get(\"selected_features\",[]))}, Holdout: {m.get(\"holdout_season\",\"?\")}')"</automated>
  </verify>
  <acceptance_criteria>
    - Ablation ensemble saved to models/ensemble_ablation/ with metadata.json
    - Console output shows baseline ATS, ablation ATS, delta, profit comparison
    - SHAP importance table printed with opening_spread and/or opening_total ranked
    - Verdict line printed: either "SHIP" or "SKIP"
  </acceptance_criteria>
  <done>Ablation script completed with both models evaluated on 2025 holdout and verdict rendered</done>
</task>

<task type="auto">
  <name>Task 2: Write VERDICT.md and conditional doc updates</name>
  <files>.planning/phases/38-market-feature-ablation/VERDICT.md</files>
  <read_first>
    - .planning/phases/37-holdout-reset-and-baseline/BASELINE.md
    - .planning/phases/38-market-feature-ablation/38-CONTEXT.md (decisions D-07 through D-10)
  </read_first>
  <action>
Create `.planning/phases/38-market-feature-ablation/VERDICT.md` per D-07 with the following structure:

```markdown
# Phase 38 Verdict -- Market Feature Ablation

## Setup
- Training: 2016-2024 (9 seasons, 6 with FinnedAI market data)
- Holdout: 2025 (sealed)
- Comparison: P30 baseline (no market features) vs market-augmented ensemble
- Gate: strict > on holdout ATS accuracy

## Results

### P30 Baseline (No Market Features)
- ATS Accuracy: {from ablation output}
- Record: {W-L-P}
- Profit: {units}
- ROI: {pct}

### Market-Augmented Ensemble
- ATS Accuracy: {from ablation output}
- Record: {W-L-P}
- Profit: {units}
- ROI: {pct}
- Features selected: {count}

### Delta
- ATS: {delta}
- Profit: {delta}

## SHAP Analysis
{Paste the SHAP importance table from Task 1 output}
{Note whether opening_spread/opening_total were selected and their relative importance per D-05/D-06}

## Verdict: {SHIP or SKIP}
{Explanation per D-08 if SHIP or D-09 if SKIP}

## Structural Validity (per D-10)
This ablation is structurally valid: 6 of 9 training seasons have FinnedAI market data, all 9 have opening lines via nflverse bridge, and the 2025 holdout has full market coverage. The conclusion is definitive for available free data.
```

Use exact metrics from the Task 1 ablation output -- do not approximate.

**Conditional doc updates (per D-08/D-09):**
- If SHIP: Note in VERDICT.md that market features are now part of the production model (the ablation script already copies models). Do NOT update CLAUDE.md or PROJECT.md in this task -- that is out of plan scope per the single-requirement boundary.
- If SKIP: Note in VERDICT.md that closing-line proxies do not carry enough signal, and pursuing paid opening-line data (The Odds API) would be the next step to revisit this question.
  </action>
  <verify>
    <automated>test -f .planning/phases/38-market-feature-ablation/VERDICT.md && grep -c "VERDICT\|ATS Accuracy\|SHAP\|Structural Validity" .planning/phases/38-market-feature-ablation/VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - VERDICT.md exists with exact metrics for both models
    - Contains ATS accuracy, profit, ROI for both baseline and ablation
    - Contains SHAP analysis section with market feature contribution
    - Contains ship-or-skip verdict with explanation
    - Contains structural validity statement per D-10
    - All numeric values match Task 1 console output exactly
  </acceptance_criteria>
  <done>VERDICT.md written with definitive market feature conclusion backed by exact metrics</done>
</task>

</tasks>

<verification>
1. `models/ensemble_ablation/metadata.json` exists with holdout_season=2025
2. VERDICT.md contains both model results, SHAP analysis, and ship-or-skip verdict
3. If verdict is SHIP: `models/ensemble/metadata.json` was updated by the ablation script
4. If verdict is SKIP: `models/ensemble/metadata.json` remains unchanged from Phase 37
</verification>

<success_criteria>
- HOLD-04 complete: market feature ablation run on 2025 holdout with 6 seasons of training market data
- Ship-or-skip verdict documented with exact metrics
- SHAP importance quantifies market feature contribution
- VERDICT.md is the definitive conclusion for v2.2 milestone
</success_criteria>

<output>
After completion, create `.planning/phases/38-market-feature-ablation/38-01-SUMMARY.md`
</output>
