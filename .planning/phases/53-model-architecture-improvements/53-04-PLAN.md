# Phase 53-04: Production Residual Experiment - Plan

**Created:** 2026-04-02
**Status:** Complete

## Tasks

### T-01: Build Production Heuristic Baseline
- Replicate exact production heuristic: `_weighted_baseline * usage_mult * matchup_factor`
- Apply `calculate_fantasy_points_df` and `PROJECTION_CEILING_SHRINKAGE`
- Build opponent rankings from Bronze weekly + schedule data

### T-02: Train Production Residual Models
- RidgeCV on 466 features -> (actual - production_heuristic)
- Walk-forward CV (train on seasons < val, predict val)
- Evaluate per position against production heuristic and standalone ML

### T-03: Per-Fold Stability Analysis
- Verify consistency across all 3 validation folds (2022, 2023, 2024)
- Determine which positions SHIP via production residual

## Success Criteria
1. Production residual evaluated against exact production heuristic
2. Per-fold stability confirmed for SHIP positions
3. Optimal position router determined (ML vs Residual vs Heuristic per position)
