# Phase 53-05: Wire Hybrid Residual into Production Pipeline - Plan

**Created:** 2026-04-02
**Status:** Complete

## Tasks

### T-01: Train and Save Persistent Residual Models
- Create `scripts/train_residual_models.py` CLI
- Train WR and TE residual Ridge models on full training data
- Save models to `models/residual/` with joblib + JSON metadata sidecar

### T-02: Update ML Projection Router
- Add HYBRID verdict routing for WR/TE in `src/ml_projection_router.py`
- Add RB SHIP promotion (falls back to heuristic when features unavailable)
- Add `HYBRID_POSITIONS` constant

### T-03: Update CLI and Backtest Scripts
- Update `--ml` help text in generate_projections.py and backtest_projections.py
- Graceful fallback: if residual model files missing, return heuristic unchanged

### T-04: Validate via Full Backtest
- Run backtest with hybrid router (2022-2024, W3-18, Half-PPR)
- Verify WR and TE improvements over baseline
- Confirm no regressions for QB and RB

### T-05: Tests
- Tests for save/load/apply residual models
- Tests for router integration with HYBRID_POSITIONS

## Success Criteria
1. WR and TE residual models saved and loadable
2. Backtest MAE improves for WR and TE
3. No regressions for QB and RB
4. All tests passing
