# Phase 53-03: Hybrid Residual Projection Experiment - Plan

**Created:** 2026-04-02
**Status:** Complete

## Tasks

### T-01: Implement Blend Approach
- `blended = alpha * heuristic + (1 - alpha) * ML`
- Grid-search alpha in [0.1, 0.2, ..., 0.9]
- Evaluate per position on walk-forward CV

### T-02: Implement Residual Model
- Train RidgeCV on `target = actual - heuristic` using ML features
- Walk-forward CV evaluation (train on seasons < val, predict val)
- `final = heuristic + ridge.predict(features)`

### T-03: Build Experiment Infrastructure
- Create `src/hybrid_projection.py` for blend + residual logic
- Create `scripts/run_hybrid_experiment.py` for experiment runner
- Tests for blend, residual, fantasy point conversion

## Success Criteria
1. Both blend and residual approaches evaluated per position
2. Determine which positions benefit from hybrid approach
3. Compare to standalone ML and standalone heuristic
