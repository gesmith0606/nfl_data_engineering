---
phase: 09-new-data-type-ingestion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_pbp_ingestion.py
autonomous: true
requirements:
  - INGEST-09

must_haves:
  truths:
    - "PBP ingestion for any season 2016-2025 produces a Parquet file with 103 curated columns"
    - "PBP single-season batch loop prevents memory exhaustion"
    - "validate_data() passes on PBP files"
  artifacts:
    - path: "data/bronze/pbp/"
      provides: "PBP Parquet files for seasons 2016-2025"
    - path: "tests/test_pbp_ingestion.py"
      provides: "Tests confirming PBP column curation and season range coverage"
  key_links:
    - from: "scripts/bronze_ingestion_simple.py"
      to: "src/nfl_data_adapter.py:fetch_pbp"
      via: "registry dispatch with PBP_COLUMNS, downcast=True, include_participation=False"
      pattern: "fetch_pbp.*columns.*PBP_COLUMNS"
---

<objective>
Ingest PBP (play-by-play) data for seasons 2016-2025, verifying the existing single-season batch loop handles the full range without memory issues.

Purpose: PBP is the largest dataset (~50K rows/season). The ingestion path already works from v1.0 but needs to be verified across the full 2016-2025 range. This plan runs in parallel with Plan 01 since PBP uses no sub-types and needs no CLI changes.

Output: PBP Parquet files for 2016-2025 with 103 curated columns each, plus a range coverage test.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-new-data-type-ingestion/09-CONTEXT.md
@.planning/phases/09-new-data-type-ingestion/09-RESEARCH.md

@scripts/bronze_ingestion_simple.py
@src/config.py
@tests/test_pbp_ingestion.py

<interfaces>
<!-- PBP ingestion path is established from v1.0 -->

From src/config.py:
```python
PBP_COLUMNS = [...]  # 103 curated columns
DATA_TYPE_SEASON_RANGES = {"pbp": (1999, get_max_season)}
```

From _build_method_kwargs:
```python
if method_name == "fetch_pbp":
    from src.config import PBP_COLUMNS
    kwargs["columns"] = PBP_COLUMNS
    kwargs["downcast"] = True
    kwargs["include_participation"] = False
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run PBP ingestion for 2016-2025</name>
  <files>scripts/bronze_ingestion_simple.py</files>
  <action>
    Run PBP ingestion across the full 2016-2025 range. The single-season batch loop already exists in main() and prevents memory exhaustion by processing one season at a time.

    ```bash
    python scripts/bronze_ingestion_simple.py --data-type pbp --seasons 2016-2025
    ```

    This will loop through each season 2016-2025, fetching PBP data one season at a time with 103 curated columns via PBP_COLUMNS, downcast=True, and include_participation=False.

    Monitor the output for:
    - Each season should produce a Parquet file in data/bronze/pbp/season=YYYY/
    - Row counts should be ~40K-60K per season
    - Column count should be exactly 103 (from PBP_COLUMNS)
    - validate_data() should pass for each season
    - Schema diff logging (from Plan 01 if running after, or not present if running in parallel before Plan 01 -- either is fine, PBP columns are curated so no diffs expected)

    If any season fails:
    - Check if it's a memory issue (unlikely with single-season loop)
    - Check if nfl-data-py returns empty for that season (warn and skip per standard behavior)
    - Do NOT modify the PBP_COLUMNS list or adapter -- these are stable from v1.0

    Note: This task involves actual data downloads from nflverse. Each season takes ~30-60 seconds. Total: ~5-10 minutes for 10 seasons.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -c "
import os, glob
pbp_path = 'data/bronze/pbp'
files = glob.glob(f'{pbp_path}/**/*.parquet', recursive=True)
print(f'PBP: {len(files)} season files')
assert len(files) >= 8, f'Expected >= 8 PBP files (2016-2025 minus possible empties), got {len(files)}'

# Verify column count on first file
import pandas as pd
sample = pd.read_parquet(files[0])
print(f'Sample file columns: {len(sample.columns)}')
assert len(sample.columns) == 103, f'Expected 103 columns, got {len(sample.columns)}'
print(f'Sample file rows: {len(sample)}')
print('PBP ingestion verified')
"</automated>
  </verify>
  <done>
    - PBP Parquet files exist for seasons 2016-2025 in data/bronze/pbp/season=YYYY/
    - Each file has exactly 103 curated columns
    - Each file has ~40K-60K rows
    - validate_data() passes on all PBP files
    - No memory issues during ingestion
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PBP season range coverage test</name>
  <files>tests/test_pbp_ingestion.py</files>
  <behavior>
    - Test: PBP ingestion for a single season produces exactly 103 columns matching PBP_COLUMNS
    - Test: PBP season range 2016-2025 is within DATA_TYPE_SEASON_RANGES for pbp
    - Test: PBP kwargs include columns=PBP_COLUMNS, downcast=True, include_participation=False
  </behavior>
  <action>
    Add tests to tests/test_pbp_ingestion.py that verify PBP configuration is correct for the full backfill range. These complement existing PBP tests (which test column curation and CLI kwargs) with range coverage verification.

    1. Test that seasons 2016-2025 are all valid for pbp type using `validate_season_for_type("pbp", season)` for each year.
    2. Test that `_build_method_kwargs` for PBP includes the correct kwargs (columns=PBP_COLUMNS, downcast=True, include_participation=False) -- this may already be tested, check first and skip if duplicate.
    3. Test that PBP_COLUMNS has exactly 103 entries (regression guard).

    Follow existing test patterns in test_pbp_ingestion.py. Use mocks where needed.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_pbp_ingestion.py -v -x</automated>
  </verify>
  <done>
    - PBP season range coverage test passes for 2016-2025
    - PBP_COLUMNS count test guards against regression
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
- PBP Parquet files exist for 2016-2025: `ls data/bronze/pbp/season=*/`
- Column count is 103 on all files
- All tests pass: `python -m pytest tests/ -v`
</verification>

<success_criteria>
1. PBP data ingested for seasons 2016-2025 with 103 curated columns each
2. No memory issues during ingestion (single-season batch loop works)
3. validate_data() passes on all PBP files
4. PBP range coverage test added and passing
</success_criteria>

<output>
After completion, create `.planning/phases/09-new-data-type-ingestion/09-03-SUMMARY.md`
</output>
