---
phase: 09-new-data-type-ingestion
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - tests/test_advanced_ingestion.py
autonomous: true
requirements:
  - INGEST-05
  - INGEST-06
  - INGEST-07
  - INGEST-08

must_haves:
  truths:
    - "Running --data-type qbr --seasons 2006-2025 produces both weekly and seasonal Parquet files per season"
    - "Running --data-type ngs --seasons 2016-2025 produces passing, rushing, and receiving Parquet files per season"
    - "Running --data-type pfr_weekly --seasons 2018-2025 produces pass, rush, rec, def Parquet files per season"
    - "Running --data-type pfr_seasonal --seasons 2018-2025 produces pass, rush, rec, def Parquet files per season"
    - "validate_data() passes on all ingested sub-type files"
  artifacts:
    - path: "tests/test_advanced_ingestion.py"
      provides: "Integration-style tests confirming all-variants-by-default CLI behavior"
    - path: "data/bronze/qbr/"
      provides: "QBR Parquet files with frequency prefix naming"
    - path: "data/bronze/ngs/"
      provides: "NGS Parquet files partitioned by sub_type and season"
    - path: "data/bronze/pfr/"
      provides: "PFR weekly and seasonal Parquet files partitioned by sub_type and season"
  key_links:
    - from: "scripts/bronze_ingestion_simple.py"
      to: "src/nfl_data_adapter.py"
      via: "variant loop dispatches to fetch_qbr, fetch_ngs, fetch_pfr_weekly, fetch_pfr_seasonal"
      pattern: "getattr.*adapter.*adapter_method"
---

<objective>
Ingest all 4 sub-type/multi-frequency data types (QBR, NGS, PFR weekly, PFR seasonal) across their full valid season ranges, leveraging the variant looping CLI from Plan 01.

Purpose: These are the most complex Bronze types due to sub-type variants. Plan 01's CLI enhancements make this straightforward -- just run the commands and verify output.

Output: Full Bronze coverage for QBR (2006-2025), NGS (2016-2025), PFR weekly (2018-2025), PFR seasonal (2018-2025).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-new-data-type-ingestion/09-CONTEXT.md
@.planning/phases/09-new-data-type-ingestion/09-RESEARCH.md
@.planning/phases/09-new-data-type-ingestion/09-01-SUMMARY.md

@scripts/bronze_ingestion_simple.py
@src/config.py
@src/nfl_data_adapter.py
@tests/test_advanced_ingestion.py

<interfaces>
<!-- From Plan 01: CLI now supports variant looping -->
<!-- --sub-type defaults to None (all variants) for ngs, pfr_weekly, pfr_seasonal -->
<!-- --frequency defaults to None (both weekly + season) for qbr -->
<!-- Schema diff logging and ingestion summary are active -->

From DATA_TYPE_REGISTRY:
```python
"ngs": {"sub_types": ["passing", "rushing", "receiving"], ...}
"pfr_weekly": {"sub_types": ["pass", "rush", "rec", "def"], ...}
"pfr_seasonal": {"sub_types": ["pass", "rush", "rec", "def"], ...}
"qbr": {"adapter_method": "fetch_qbr", ...}  # uses --frequency
```

From _build_method_kwargs:
```python
# Sub-type methods use different param names
if "sub_types" in entry:
    key = "stat_type" if method_name == "fetch_ngs" else "s_type"
    kwargs[key] = args.sub_type
# QBR frequency
if method_name == "fetch_qbr":
    kwargs["frequency"] = args.frequency
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run sub-type data ingestion — QBR, NGS, PFR weekly, PFR seasonal</name>
  <files>scripts/bronze_ingestion_simple.py</files>
  <action>
    Run the actual ingestion for each sub-type data type. Use each type's full valid range from DATA_TYPE_SEASON_RANGES. The CLI variant looping from Plan 01 means a single command per type ingests all variants.

    1. **QBR** (range 2006-2025, both weekly + seasonal frequencies):
       ```bash
       python scripts/bronze_ingestion_simple.py --data-type qbr --seasons 2006-2025
       ```
       Expected: `qbr_weekly_*.parquet` and `qbr_season_*.parquet` files per season.
       Note: Use "season" (not "seasonal") in filenames to match nfl-data-py API param (per research open question resolution).

    2. **NGS** (range 2016-2025, all 3 sub-types):
       ```bash
       python scripts/bronze_ingestion_simple.py --data-type ngs --seasons 2016-2025
       ```
       Expected: Files in data/bronze/ngs/passing/, ngs/rushing/, ngs/receiving/ per season.

    3. **PFR weekly** (range 2018-2025, all 4 sub-types):
       ```bash
       python scripts/bronze_ingestion_simple.py --data-type pfr_weekly --seasons 2018-2025
       ```
       Expected: Files in data/bronze/pfr/weekly/pass/, pfr/weekly/rush/, pfr/weekly/rec/, pfr/weekly/def/ per season.

    4. **PFR seasonal** (range 2018-2025, all 4 sub-types):
       ```bash
       python scripts/bronze_ingestion_simple.py --data-type pfr_seasonal --seasons 2018-2025
       ```
       Expected: Files in data/bronze/pfr/seasonal/pass/, pfr/seasonal/rush/, pfr/seasonal/rec/, pfr/seasonal/def/ per season.

    For each run:
    - Verify schema diff logging captures cross-year column changes
    - Verify ingestion summary shows correct ingested/skipped counts per variant
    - Note any seasons/variants that return empty data (expected: some early years, QBR 2024 may be empty per STATE.md blocker)
    - If any type fails, diagnose and fix the CLI issue. Do NOT modify NFLDataAdapter.

    If the variant loop reveals any edge cases in how `_build_method_kwargs` constructs kwargs for the current variant's sub_type/frequency, fix in bronze_ingestion_simple.py.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -c "
import os, glob
checks = {
    'qbr': ('data/bronze/qbr', 2),
    'ngs_passing': ('data/bronze/ngs/passing', 1),
    'ngs_rushing': ('data/bronze/ngs/rushing', 1),
    'ngs_receiving': ('data/bronze/ngs/receiving', 1),
    'pfr_weekly_pass': ('data/bronze/pfr/weekly/pass', 1),
    'pfr_weekly_rush': ('data/bronze/pfr/weekly/rush', 1),
    'pfr_seasonal_pass': ('data/bronze/pfr/seasonal/pass', 1),
    'pfr_seasonal_def': ('data/bronze/pfr/seasonal/def', 1),
}
for label, (path, min_files) in checks.items():
    files = glob.glob(f'{path}/**/*.parquet', recursive=True)
    print(f'{label}: {len(files)} files')
    assert len(files) >= min_files, f'Expected >= {min_files} files for {label}, got {len(files)}'
print('All sub-type data types have Parquet files')
"</automated>
  </verify>
  <done>
    - QBR: weekly and seasonal Parquet files for seasons 2006-2025 (empty seasons skipped with warning)
    - NGS: passing, rushing, receiving Parquet files for seasons 2016-2025
    - PFR weekly: pass, rush, rec, def Parquet files for seasons 2018-2025
    - PFR seasonal: pass, rush, rec, def Parquet files for seasons 2018-2025
    - validate_data() passes on all ingested files
    - Schema diffs logged for any cross-year column changes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for sub-type variant ingestion</name>
  <files>tests/test_advanced_ingestion.py</files>
  <behavior>
    - Test: QBR ingestion with frequency=None produces files for both weekly and season variants
    - Test: NGS ingestion with sub_type=None produces files for passing, rushing, receiving
    - Test: PFR weekly ingestion with sub_type=None produces files for pass, rush, rec, def
    - Test: PFR seasonal ingestion with sub_type="pass" produces only pass variant (filter works)
    - Test: QBR filenames use frequency prefix (qbr_weekly_*, qbr_season_*)
  </behavior>
  <action>
    Add tests to tests/test_advanced_ingestion.py that verify the all-variants-by-default CLI behavior end-to-end. Use the existing mock pattern (patch NFLDataAdapter methods) to test that main() dispatches correctly for each variant.

    Key test scenarios:
    1. Mock fetch_qbr to return a DataFrame. Run main() with --data-type qbr (no --frequency). Assert fetch_qbr is called twice: once with frequency="weekly", once with frequency="season".
    2. Mock fetch_ngs to return a DataFrame. Run main() with --data-type ngs (no --sub-type). Assert fetch_ngs is called 3 times with stat_type="passing", "rushing", "receiving".
    3. Mock fetch_pfr_weekly. Run main() with --data-type pfr_weekly --sub-type pass. Assert fetch_pfr_weekly is called once with s_type="pass" (filter works).
    4. Verify QBR filename prefix: when frequency="weekly", file is named qbr_weekly_*.parquet; when frequency="season", file is named qbr_season_*.parquet.

    Use `unittest.mock.patch` on NFLDataAdapter methods and `argparse` namespace injection or `sys.argv` patching to drive main().
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_advanced_ingestion.py -v -x -k "variant or qbr_frequency or sub_type_default"</automated>
  </verify>
  <done>
    - Tests confirm all-variants-by-default behavior for QBR, NGS, PFR weekly, PFR seasonal
    - Tests confirm --sub-type filter works to select single variant
    - Tests confirm QBR filename frequency prefix
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
- All existing tests pass: `python -m pytest tests/ -v`
- Sub-type Parquet files exist in correct directory structure
- QBR files have frequency prefix in filenames
- No empty Parquet files saved
</verification>

<success_criteria>
1. QBR, NGS, PFR weekly, and PFR seasonal have Parquet files across their full valid season ranges
2. All variants are ingested by default (no --sub-type or --frequency required)
3. Integration tests verify variant looping behavior
4. Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-new-data-type-ingestion/09-02-SUMMARY.md`
</output>
