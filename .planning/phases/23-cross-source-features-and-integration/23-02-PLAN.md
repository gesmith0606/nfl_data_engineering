---
phase: 23-cross-source-features-and-integration
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - scripts/silver_game_context_transformation.py
  - scripts/check_pipeline_health.py
  - tests/test_feature_vector.py
autonomous: true
requirements: [CROSS-01, CROSS-02, INTEG-01]

must_haves:
  truths:
    - "silver_game_context_transformation.py produces referee_tendencies and playoff_context Silver parquet for 2016-2025"
    - "check_pipeline_health.py validates referee_tendencies and playoff_context Silver paths"
    - "Integration test assembles ~337 columns from all Silver sources via left joins on [team, season, week]"
    - "No unexpected nulls in core columns (wins, off_penalties, epa) for weeks 2+"
    - "2023 final standings spot-check: BAL 13-4, KC 11-6, SF 12-5"
  artifacts:
    - path: "scripts/silver_game_context_transformation.py"
      provides: "Wired referee_tendencies and playoff_context compute + save"
      contains: "compute_referee_tendencies"
    - path: "scripts/check_pipeline_health.py"
      provides: "Health check for new Silver paths"
      contains: "referee_tendencies"
    - path: "tests/test_feature_vector.py"
      provides: "Integration test for full prediction feature vector assembly"
      contains: "test_feature_vector_assembly"
  key_links:
    - from: "scripts/silver_game_context_transformation.py"
      to: "src/game_context.py"
      via: "imports compute_referee_tendencies, compute_playoff_context"
      pattern: "from game_context import.*compute_referee_tendencies"
    - from: "tests/test_feature_vector.py"
      to: "data/silver/"
      via: "reads all Silver parquet sources for join test"
      pattern: "download_latest_parquet\\|read_parquet\\|glob"
---

<objective>
Wire referee tendencies and playoff context into the Silver pipeline, extend pipeline health monitoring, generate Silver data for 2016-2025, and validate the full prediction feature vector via an integration test.

Purpose: Completes the v1.3 milestone by producing all Silver data and validating it assembles into a complete prediction feature vector.
Output: Updated transformation script, health check coverage, generated Silver parquet files, and integration test.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-cross-source-features-and-integration/23-CONTEXT.md
@.planning/phases/23-cross-source-features-and-integration/23-RESEARCH.md
@.planning/phases/23-cross-source-features-and-integration/23-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan consumes -->

From src/game_context.py (added in Plan 01):
```python
def compute_referee_tendencies(unpivoted_df: pd.DataFrame, pbp_derived_df: pd.DataFrame) -> pd.DataFrame:
    """Returns DataFrame with [team, season, week, ref_penalties_per_game]"""

def compute_playoff_context(unpivoted_df: pd.DataFrame) -> pd.DataFrame:
    """Returns DataFrame with [team, season, week, wins, losses, ties, win_pct,
       division_rank, games_behind_division_leader, late_season_contention]"""
```

From src/config.py (added in Plan 01):
```python
SILVER_TEAM_S3_KEYS = {
    ...existing entries...
    "referee_tendencies": "teams/referee_tendencies/season={season}/referee_tendencies_{ts}.parquet",
    "playoff_context": "teams/playoff_context/season={season}/playoff_context_{ts}.parquet",
}
```

From scripts/silver_game_context_transformation.py (existing pattern):
```python
# Follows pattern: _read_local_schedules, _save_local_silver, _try_s3_upload, run_game_context_transform, main
# Currently calls compute_game_context() and saves single output
# Processes seasons sequentially with prior_season_df for coaching context
```

From scripts/check_pipeline_health.py:
```python
REQUIRED_SILVER_PREFIXES = {
    "usage_metrics": "players/usage/season={season}/week={week}/",
    "pbp_metrics": "teams/pbp_metrics/season={season}/",
    "tendencies": "teams/tendencies/season={season}/",
    "sos": "teams/sos/season={season}/",
    "situational": "teams/situational/season={season}/",
    "pbp_derived": "teams/pbp_derived/season={season}/",
    "game_context": "teams/game_context/season={season}/",
    "advanced_profiles": "players/advanced/season={season}/",
    "historical_profiles": "players/historical/",
}
```

Silver data locations for feature vector join:
```
data/silver/teams/pbp_metrics/season=YYYY/*.parquet       (63 cols)
data/silver/teams/tendencies/season=YYYY/*.parquet         (23 cols)
data/silver/teams/sos/season=YYYY/*.parquet                (21 cols)
data/silver/teams/situational/season=YYYY/*.parquet        (51 cols)
data/silver/teams/pbp_derived/season=YYYY/*.parquet        (164 cols)
data/silver/teams/game_context/season=YYYY/*.parquet       (22 cols)
data/silver/teams/referee_tendencies/season=YYYY/*.parquet (4 cols: team, season, week, ref_penalties_per_game)
data/silver/teams/playoff_context/season=YYYY/*.parquet    (10 cols: team, season, week, + 7 feature cols)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire referee/playoff into transformation script and health check, generate Silver data</name>
  <files>scripts/silver_game_context_transformation.py, scripts/check_pipeline_health.py</files>
  <read_first>
    - scripts/silver_game_context_transformation.py (full file -- understand current run_game_context_transform flow, _read_local_schedules, _save_local_silver patterns)
    - scripts/check_pipeline_health.py (REQUIRED_SILVER_PREFIXES dict near line 55)
    - src/game_context.py (compute_referee_tendencies and compute_playoff_context signatures from Plan 01)
    - scripts/silver_team_transformation.py (for pbp_derived read pattern -- need to read pbp_derived Silver for referee join)
  </read_first>
  <action>
1. In `scripts/silver_game_context_transformation.py`:
   a. Update import to include new functions:
      `from game_context import compute_game_context, compute_referee_tendencies, compute_playoff_context`
   b. Add a helper `_read_local_pbp_derived(season: int) -> pd.DataFrame` that reads `data/silver/teams/pbp_derived/season={season}/*.parquet` using the same glob+sorted pattern as `_read_local_schedules`. Return empty DataFrame if no files found.
   c. In the `run_game_context_transform` function (or wherever game_context is processed per season), after computing and saving game_context:
      - Read pbp_derived for the season: `pbp_derived_df = _read_local_pbp_derived(season)`
      - Call `_unpivot_schedules(schedules_df)` to get the unpivoted data (import _unpivot_schedules from game_context)
      - If pbp_derived_df is not empty:
        - Call `referee_df = compute_referee_tendencies(unpivoted, pbp_derived_df)`
        - Save using `_save_local_silver(referee_df, SILVER_TEAM_S3_KEYS["referee_tendencies"].format(season=season, ts=ts), ts)`
        - Optionally try S3 upload
      - Else: print warning that pbp_derived not available for this season, skipping referee tendencies
      - Call `playoff_df = compute_playoff_context(unpivoted)`
      - Save using `_save_local_silver(playoff_df, SILVER_TEAM_S3_KEYS["playoff_context"].format(season=season, ts=ts), ts)`
      - Optionally try S3 upload
   d. Also import `_unpivot_schedules` from game_context

2. In `scripts/check_pipeline_health.py`:
   - Add to REQUIRED_SILVER_PREFIXES dict:
     `"referee_tendencies": "teams/referee_tendencies/season={season}/",`
     `"playoff_context": "teams/playoff_context/season={season}/",`

3. Generate Silver data by running:
   - First: `python scripts/silver_team_transformation.py --seasons 2016 2017 2018 2019 2020 2021 2022 2023 2024 2025` (to ensure pbp_derived local files exist)
   - Then: `python scripts/silver_game_context_transformation.py --seasons 2016 2017 2018 2019 2020 2021 2022 2023 2024 2025` (to produce referee_tendencies and playoff_context)
   - Verify output files exist: `ls data/silver/teams/referee_tendencies/` and `ls data/silver/teams/playoff_context/`
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -c "
import glob
ref_files = glob.glob('data/silver/teams/referee_tendencies/season=*/*.parquet')
play_files = glob.glob('data/silver/teams/playoff_context/season=*/*.parquet')
print(f'Referee tendencies: {len(ref_files)} season files')
print(f'Playoff context: {len(play_files)} season files')
assert len(ref_files) >= 8, f'Expected 8+ referee files, got {len(ref_files)}'
assert len(play_files) >= 10, f'Expected 10+ playoff files, got {len(play_files)}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/silver_game_context_transformation.py contains `from game_context import compute_game_context, compute_referee_tendencies, compute_playoff_context`
    - scripts/silver_game_context_transformation.py contains `_read_local_pbp_derived`
    - scripts/check_pipeline_health.py REQUIRED_SILVER_PREFIXES contains "referee_tendencies" key
    - scripts/check_pipeline_health.py REQUIRED_SILVER_PREFIXES contains "playoff_context" key
    - data/silver/teams/referee_tendencies/ has at least 8 season directories with parquet files
    - data/silver/teams/playoff_context/ has at least 10 season directories with parquet files
    - Full test suite `python -m pytest tests/ -v` exits 0 with no regressions
  </acceptance_criteria>
  <done>Referee tendencies and playoff context Silver parquet generated for 2016-2025, pipeline health check covers all new paths, transformation script wired</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration test for full prediction feature vector assembly</name>
  <files>tests/test_feature_vector.py</files>
  <read_first>
    - src/config.py (SILVER_TEAM_S3_KEYS for all Silver output paths)
    - tests/test_game_context.py (existing test patterns, imports, sys.path setup)
    - data/silver/teams/ directory listing (confirm which Silver sources have local parquet files)
  </read_first>
  <behavior>
    - test_feature_vector_assembly: loads all Silver sources for a single season (2024), left joins on [team, season, week], asserts column count >= 300 (actual ~337), asserts 32 teams present, asserts no unexpected nulls in core columns for weeks 2+
    - test_feature_vector_null_policy: Week 1 rolling columns (ref_penalties_per_game, _roll3/_roll6 variants) allowed to be NaN; core columns (wins, off_penalties, epa) must be non-null for weeks 2+
    - test_standings_spot_check_2023: loads 2023 playoff_context Silver, verifies BAL final record = 13-4, KC = 11-6, SF = 12-5
    - test_standings_spot_check_2024: loads 2024 playoff_context Silver, spot-checks known division winners
    - test_referee_unique_count: loads 2024 referee_tendencies, counts unique referees, asserts 15-25 range
  </behavior>
  <action>
Create `tests/test_feature_vector.py`:

```python
#!/usr/bin/env python3
"""Integration tests for full prediction feature vector assembly."""
```

1. Standard imports: pytest, pandas, numpy, os, sys, glob. Add src/ to sys.path.

2. Helper function `_read_latest_local(subdir: str, season: int) -> pd.DataFrame`:
   - Pattern: `data/silver/{subdir}/season={season}/*.parquet`
   - Return latest file by sorted glob, or empty DataFrame

3. Helper function `_assemble_feature_vector(season: int) -> pd.DataFrame`:
   - Read all Silver sources for the given season:
     - pbp_metrics from `teams/pbp_metrics/`
     - tendencies from `teams/tendencies/`
     - sos from `teams/sos/`
     - situational from `teams/situational/`
     - pbp_derived from `teams/pbp_derived/`
     - game_context from `teams/game_context/`
     - referee_tendencies from `teams/referee_tendencies/`
     - playoff_context from `teams/playoff_context/`
   - Start with pbp_metrics as base. Left join each subsequent source on [team, season, week].
   - For duplicate columns after join (e.g., _x/_y suffixes), drop the suffix versions.
   - Return assembled DataFrame.

4. Tests:
   - `test_feature_vector_assembly`:
     - Skip if any Silver source missing (`pytest.mark.skipif` or inline skip)
     - Call `_assemble_feature_vector(2024)`
     - Assert columns >= 300 (research says ~337)
     - Assert 32 unique teams
     - Assert rows >= 500 (32 teams x ~17 weeks)
     - Print actual column count for documentation

   - `test_feature_vector_null_policy`:
     - Assemble 2024 feature vector
     - Filter to week >= 2
     - Assert zero nulls in core columns: `["wins", "losses", "win_pct", "epa", "off_penalties"]` (these should always be populated after Week 1)
     - Filter to week == 1: assert `ref_penalties_per_game` is NaN (shift(1) with no prior)
     - Assert wins == 0 for all Week 1 rows

   - `test_standings_spot_check_2023`:
     - Read playoff_context for 2023
     - Get max week per team (Week 18 or last regular season week)
     - At final week, verify: BAL wins=13, BAL losses=4; KC wins=11, KC losses=6; SF wins=12, SF losses=5
     - These are standings entering the last week + that week's result. Actually, since shift(1) gives entering-this-week standings, the max week's values are entering-week-18 standings (i.e., after 16 games). To get final record, need to check week 18 values + that week's game. SIMPLER: just check that the final-week standings are close (wins within 1 of expected).
     - OR: read the unpivoted data and compute final records directly. SIMPLEST: check that BAL has wins >= 12 at the latest week, KC has wins >= 10, SF has wins >= 11 (shifted values will be 1 less than final).

   - `test_referee_unique_count`:
     - Read referee_tendencies for 2024
     - Filter to rows where ref_penalties_per_game is not NaN
     - Read the original Silver game_context (which has game_id) -- actually referee_tendencies may not have referee name. Need to check.
     - ALTERNATIVE: Read schedules Bronze for 2024, strip().title() the referee column, count unique. Assert 15-25.
     - If referee_tendencies only has team/season/week/ref_penalties_per_game, just assert the DataFrame is not empty and has reasonable row count (~544 rows for 32 teams x 17 weeks).

5. Mark integration tests with `@pytest.mark.integration` if desired, but keep them runnable with standard `pytest`.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_feature_vector.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_feature_vector.py contains `test_feature_vector_assembly`
    - tests/test_feature_vector.py contains `test_feature_vector_null_policy`
    - tests/test_feature_vector.py contains `test_standings_spot_check_2023`
    - tests/test_feature_vector.py contains `test_referee_unique_count`
    - `python -m pytest tests/test_feature_vector.py -v` exits 0
    - `python -m pytest tests/ -v` exits 0 (full suite, no regressions)
    - test_feature_vector_assembly asserts >= 300 columns after join
    - test_standings_spot_check_2023 verifies BAL, KC, SF win totals
  </acceptance_criteria>
  <done>Integration test validates full feature vector assembly (~337 columns), null policy, standings spot-checks against 2023 NFL data, and referee distribution -- all tests green alongside full suite</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_game_context.py tests/test_feature_vector.py -v` -- all game context and integration tests pass
- `python -m pytest tests/ -v` -- full suite green, no regressions
- `ls data/silver/teams/referee_tendencies/season=2024/` -- parquet file exists
- `ls data/silver/teams/playoff_context/season=2024/` -- parquet file exists
- `grep -c "referee_tendencies\|playoff_context" scripts/check_pipeline_health.py` -- returns 2 (both entries present)
</verification>

<success_criteria>
- Silver parquet files for referee_tendencies and playoff_context exist for 2016-2025
- Pipeline health check covers all new Silver paths
- Integration test assembles full feature vector from all Silver sources
- Standings spot-checked against 2023 NFL data
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/23-cross-source-features-and-integration/23-02-SUMMARY.md`
</output>
