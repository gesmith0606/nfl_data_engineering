---
phase: 04-documentation-update
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - docs/NFL_GAME_PREDICTION_DATA_MODEL.md
  - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md
autonomous: true
requirements:
  - DOC-02
  - DOC-04

must_haves:
  truths:
    - "Prediction data model doc has inline status badges on every table/section showing implemented vs planned"
    - "Prediction data model cross-references NFL_DATA_DICTIONARY.md for column specs instead of duplicating them"
    - "Implementation guide reflects actual GSD Phases 1-4 instead of obsolete 8-week Delta Lake roadmap"
    - "Implementation guide references pandas/pyarrow/Parquet/DuckDB instead of Delta Lake/PySpark/Spark"
    - "Implementation guide includes v2 requirements (SLV-01 to SLV-03, ML-01 to ML-03) as upcoming phases"
  artifacts:
    - path: "docs/NFL_GAME_PREDICTION_DATA_MODEL.md"
      provides: "Game prediction data model with implementation status badges"
      min_lines: 400
      contains: "Implemented"
    - path: "docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md"
      provides: "Updated implementation roadmap aligned with actual project state"
      min_lines: 400
      contains: "Phase 1"
  key_links:
    - from: "docs/NFL_GAME_PREDICTION_DATA_MODEL.md"
      to: "docs/NFL_DATA_DICTIONARY.md"
      via: "cross-reference links for column specs"
      pattern: "NFL_DATA_DICTIONARY|data dictionary"
    - from: "docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "v2 requirement IDs referenced"
      pattern: "SLV-0[123]|ML-0[123]"
---

<objective>
Add implementation status badges to the prediction data model doc and rewrite the implementation guide to reflect actual project state.

Purpose: The prediction data model has no status indicators (readers cannot tell what is built vs aspirational). The implementation guide references an obsolete 8-week Delta Lake/PySpark roadmap that does not match the actual work done in GSD Phases 1-4.

Output: Updated `NFL_GAME_PREDICTION_DATA_MODEL.md` with status badges + rewritten `NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md` with actual phase history and v2 roadmap.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-documentation-update/04-CONTEXT.md
@.planning/phases/04-documentation-update/04-RESEARCH.md

# Need Plan 02 summary for data dictionary cross-references
@.planning/phases/04-documentation-update/04-02-SUMMARY.md

@docs/NFL_GAME_PREDICTION_DATA_MODEL.md
@docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add status badges to NFL_GAME_PREDICTION_DATA_MODEL.md</name>
  <files>docs/NFL_GAME_PREDICTION_DATA_MODEL.md</files>
  <action>
    Read the existing 1210-line prediction data model doc and add inline status badges to every table/section:

    **Badge convention (per user decision):**
    - Implemented: section header includes "Implemented" marker
    - In Progress: section header includes "In Progress" marker
    - Planned: section header includes "Planned" marker

    **Status mapping based on actual project state:**

    Bronze Layer tables -- mark as Implemented:
    - Games/Schedules, Play-by-Play, Player Weekly Stats, Player Seasonal Stats
    - Snap Counts, Injuries, Rosters, Teams
    - NGS (Passing/Rushing/Receiving), PFR (Weekly/Seasonal), QBR
    - Depth Charts, Draft Picks, Combine

    Silver Layer -- mark based on actual state:
    - Usage metrics, rolling averages, opp rankings: Implemented (existing silver pipeline)
    - Team EPA aggregates, matchup features: Planned (v2 SLV-01 to SLV-03)

    Gold Layer -- mark based on actual state:
    - Fantasy projections: Implemented
    - Game outcome predictions: Planned

    Platinum/ML -- mark as Planned:
    - Feature engineering, ML models, Neo4j graph: Planned

    **Cross-reference rule (per user decision):** Where the doc currently lists column specs for Bronze tables, replace with a link to `NFL_DATA_DICTIONARY.md` (e.g., "See [NFL Data Dictionary](NFL_DATA_DICTIONARY.md#schedules-games) for full column specs"). Do NOT duplicate column schemas between the two docs.

    **Keep aspirational content** (Platinum Layer, ML pipeline, etc.) but badge clearly as Planned.

    Preserve the overall document structure and useful content. This is surgical badge insertion + cross-reference replacement, not a full rewrite.
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "
content = open('docs/NFL_GAME_PREDICTION_DATA_MODEL.md').read()
assert 'Implemented' in content, 'Missing Implemented badges'
assert 'Planned' in content, 'Missing Planned badges'
assert 'NFL_DATA_DICTIONARY' in content or 'data dictionary' in content.lower(), 'Missing data dictionary cross-reference'
# Verify no duplicate column specs (should link to dictionary instead)
lines = content.split('\n')
# Count section headers with badges
badge_count = sum(1 for l in lines if 'Implemented' in l or 'Planned' in l or 'In Progress' in l)
assert badge_count >= 8, f'Expected 8+ badged sections, got {badge_count}'
print(f'Prediction model verified: {badge_count} badged sections')
"</automated>
  </verify>
  <done>Every table/section in the prediction data model has an inline status badge. Bronze tables marked as Implemented. Future layers marked as Planned. Column specs replaced with cross-references to NFL_DATA_DICTIONARY.md.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md</name>
  <files>docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md</files>
  <action>
    Substantially rewrite the 2166-line implementation guide. The current guide has an obsolete 8-week Delta Lake/PySpark/Spark roadmap that does not reflect reality.

    **New document structure (per user decisions):**

    1. **Title + Overview**: NFL Data Platform Implementation Guide. Brief description of the medallion architecture (Bronze/Silver/Gold) with actual tech stack.

    2. **Tech Stack** (replace all Delta Lake/PySpark/Spark references):
       - Data processing: pandas, pyarrow
       - Storage: Parquet files (local-first, S3 optional)
       - Query: DuckDB for ad-hoc analysis
       - ML (planned): XGBoost, LightGBM
       - Infrastructure: AWS S3 (us-east-2), GitHub Actions
       - Python libraries: nfl-data-py (via NFLDataAdapter), boto3

    3. **Completed Phases** (actual GSD work):
       - Phase 1: Infrastructure Prerequisites (local-first, dynamic seasons, adapter, registry)
       - Phase 2: Core PBP Ingestion (103 curated columns, memory-safe batch processing)
       - Phase 3: Advanced Stats & Context Data (NGS, PFR, QBR, depth charts, draft picks, combine)
       - Phase 4: Documentation Update (this phase -- inventory script, data dictionary, status badges)
       - Reference requirement IDs (INFRA-01 to INFRA-05, PBP-01 to PBP-04, ADV-01 to ADV-05, CTX-01 to CTX-02, VAL-01 to VAL-03, DOC-01 to DOC-04)

    4. **Existing Capabilities** (pre-Bronze-expansion work):
       - Silver layer: usage metrics, rolling averages, opp rankings
       - Gold layer: weekly + preseason projections with injury adjustments
       - Draft tool: snake, auction, mock simulation, waiver wire
       - Backtesting: MAE 4.91, correlation 0.52 across 3 seasons

    5. **Upcoming Phases (v2)** from REQUIREMENTS.md:
       - Silver Prediction Layer: SLV-01 (team EPA aggregates), SLV-02 (exponential rolling metrics), SLV-03 (matchup features)
       - ML Pipeline: ML-01 (200+ features), ML-02 (RF/XGBoost), ML-03 (65%+ accuracy target)
       - Migration: MIG-01 (nfl-data-py to nflreadpy)

    6. **Architecture Reference**: Brief overview of key files (link to CLAUDE.md for full file map). S3 key pattern. Data flow diagram (text-based).

    7. **Development Setup**: How to set up the project (venv, dependencies, local data). Link to CLAUDE.md for full command reference.

    **Preserve useful content** from the original doc where applicable (code examples, patterns that are still valid with the actual tech stack). Delete content that references Delta Lake, PySpark, Spark, or the obsolete 8-week timeline.

    **Per user decision:** This is a living roadmap, not a historical document. Keep it forward-looking while grounding in actual accomplishments.
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "
content = open('docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md').read()
content_lower = content.lower()
# Must NOT have obsolete references
assert 'delta lake' not in content_lower, 'Still references Delta Lake'
assert 'pyspark' not in content_lower, 'Still references PySpark'
# Must have actual tech stack
assert 'pandas' in content_lower, 'Missing pandas reference'
assert 'pyarrow' in content_lower, 'Missing pyarrow reference'
assert 'parquet' in content_lower, 'Missing Parquet reference'
# Must have actual phases
assert 'Phase 1' in content or 'phase 1' in content_lower, 'Missing Phase 1'
assert 'Phase 2' in content or 'phase 2' in content_lower, 'Missing Phase 2'
assert 'Phase 3' in content or 'phase 3' in content_lower, 'Missing Phase 3'
# Must have v2 requirements
assert 'SLV-01' in content or 'slv-01' in content_lower, 'Missing v2 Silver requirements'
assert 'ML-01' in content or 'ml-01' in content_lower, 'Missing v2 ML requirements'
# Must reference DuckDB and XGBoost as current/planned stack
assert 'duckdb' in content_lower, 'Missing DuckDB reference'
print('Implementation guide verified: correct tech stack, actual phases, v2 roadmap')
"</automated>
  </verify>
  <done>Implementation guide reflects actual GSD Phases 1-4. All Delta Lake/PySpark/Spark references replaced with pandas/pyarrow/Parquet/DuckDB. v2 requirements (SLV, ML, MIG) included as upcoming work. Document serves as a living roadmap grounded in actual accomplishments.</done>
</task>

</tasks>

<verification>
- Prediction data model has status badges on every section
- Prediction data model cross-references data dictionary (no duplicate column specs)
- Implementation guide has zero Delta Lake/PySpark/Spark references
- Implementation guide covers actual Phases 1-4 + v2 roadmap
- `python -m pytest tests/ -x -q` full suite still passes
</verification>

<success_criteria>
- Every section in the prediction data model is badged as Implemented, In Progress, or Planned
- Column specs in prediction model point to NFL_DATA_DICTIONARY.md
- Implementation guide tech stack matches reality (pandas, pyarrow, Parquet, DuckDB)
- Implementation guide phase history matches actual GSD work (not obsolete 8-week plan)
- v2 requirements from REQUIREMENTS.md are included as upcoming phases
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-update/04-03-SUMMARY.md`
</output>
