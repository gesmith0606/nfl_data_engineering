# Phase 51-01: Ship/Skip Gate — Graph-Enhanced Models - Plan

**Created:** 2026-04-02
**Status:** Complete

## Tasks

### T-01: Retrain Per-Position Models with Graph Features
- Add all 22 graph features to the player feature vector
- Retrain XGBoost per-position models (QB, RB, WR, TE) with walk-forward CV
- Run SHAP feature selection to identify which graph features survive

### T-02: Stage 1 — XGB-Only Ship Gate
- Compare per-position OOF MAE vs heuristic baseline
- Apply dual agreement gate: both OOF and holdout must show 4%+ improvement
- Record ship/skip verdict per position

### T-03: Stage 2 — Ensemble for SKIP Positions
- For positions that SKIP in Stage 1, train XGB+LGB+Ridge ensemble
- Compare ensemble OOF MAE vs heuristic baseline
- Record final ship/skip verdict

### T-04: Update ML Projection Router
- Update routing for any position that passes ship gate
- Verify all tests pass after routing changes
- Run fantasy backtest with final v3.1 model configuration

## Dependencies
- Phase 50 (graph features must be populated in Silver)
- Existing player model training infrastructure

## Success Criteria
1. Walk-forward CV retraining completed for all 4 positions
2. SHAP feature selection identifies which graph features survive
3. Per-position holdout MAE compared to baseline
4. ML projection router updated for any position that ships
5. At least one position shows improvement (target: WR)
6. All tests passing after routing changes
