# Phase 52-01: Kicker Backtesting + Final Validation - Plan

**Created:** 2026-04-02
**Status:** Complete

## Tasks

### T-01: Build Kicker Backtest Framework
- Create `scripts/backtest_kicker_projections.py` standalone CLI
- For each week in 2022-2024, W3-18:
  - Load PBP data available before target week (no data leakage)
  - Compute kicker features: rolling stats, team RZ stall rate, opponent features
  - Generate kicker projections via `kicker_projection.generate_kicker_projections()`
  - Compute actual kicker fantasy points from PBP via `kicker_analytics.compute_kicker_stats()`
  - Merge projected vs actual on kicker_player_id (with name fallback)

### T-02: Report Metrics
- MAE, RMSE, correlation, bias
- Error distribution (within 1/3/5/7 pts)
- Per-season splits
- Best/worst predicted kickers
- Compare against flat baseline (8.0 pts)

### T-03: Final System Validation
- Run full-system backtest with final v3.1 model configuration
- Confirm overall fantasy MAE
- Verify all tests passing

## Dependencies
- Phase 48 (kicker projection system)
- Phase 51 (final model state must be known)
- PBP Bronze data with field_goal columns

## Success Criteria
1. Kicker projections backtested against 2022-2024 actuals
2. Kicker MAE reported (baseline established)
3. Full-system backtest run with final v3.1 configuration
4. All tests passing, docs updated
