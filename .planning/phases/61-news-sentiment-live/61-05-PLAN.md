---
phase: 61-news-sentiment-live
plan: 05
type: execute
wave: 3
depends_on:
  - "61-01"
  - "61-02"
  - "61-04"
files_modified:
  - web/api/services/news_service.py
  - web/api/routers/news.py
  - web/api/models/schemas.py
  - web/frontend/src/app/dashboard/news/page.tsx
  - web/frontend/src/app/dashboard/players/[id]/page.tsx
  - web/frontend/src/features/nfl/components/TeamEventDensityGrid.tsx
  - web/frontend/src/features/nfl/components/EventBadges.tsx
  - tests/web/test_news_router_live.py
autonomous: false
requirements:
  - NEWS-02
  - NEWS-03
  - NEWS-04
must_haves:
  truths:
    - "The news page renders real articles from the expanded source set with source attribution, publication date, and player tags"
    - "Each news item shows event badges (questionable, traded, usage_boost, weather_risk, etc.) derived from rule-extracted flags"
    - "The team sentiment dashboard renders all 32 teams in a color-coded grid using event density (count of events per team), NOT a continuous sentiment score"
    - "Player detail pages show bullish/bearish badges derived from event flags (not a numerical score)"
    - "All UI endpoints degrade gracefully when Gold sentiment data is empty (return [] or a zero-state component)"
  artifacts:
    - path: "web/api/services/news_service.py"
      provides: "Service functions for news feed, team event density, player badges"
      contains: "get_team_event_density|get_player_event_badges"
    - path: "web/api/routers/news.py"
      provides: "Router endpoints for /news/feed (already exists — enhanced with event filter), /news/team-events, /news/player-badges/{player_id}"
      contains: "team-events|player-badges"
    - path: "web/frontend/src/features/nfl/components/TeamEventDensityGrid.tsx"
      provides: "32-team grid component for NEWS-03"
      min_lines: 120
    - path: "web/frontend/src/features/nfl/components/EventBadges.tsx"
      provides: "Reusable badge component for NEWS-04"
      min_lines: 80
  key_links:
    - from: "web/frontend/src/app/dashboard/news/page.tsx"
      to: "/news/feed API"
      via: "fetch call on page load"
      pattern: "/news/feed|/api/news"
    - from: "web/frontend/src/features/nfl/components/TeamEventDensityGrid.tsx"
      to: "/news/team-events API"
      via: "fetch in useEffect or server component load"
      pattern: "team-events"
    - from: "web/frontend/src/app/dashboard/players/\\[id\\]/page.tsx"
      to: "EventBadges + /news/player-badges/{id}"
      via: "component rendered in player detail card"
      pattern: "EventBadges|player-badges"
---

<objective>
Render the rule-extracted event data on the website per NEWS-02, NEWS-03, NEWS-04. Wire `web/api/routers/news.py` + `news_service.py` to surface the new event flags, build the 32-team event density grid, render event badges on player pages, and update the news page to show real articles with tags.

Purpose: This is the user-visible payoff of Phases 61-01 through 61-04. Sources are expanded, rules are expanded, the cron runs — now users can see it.

Output: New backend endpoints, two new React components, updated news and player pages, live end-to-end against repo-committed Gold data.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-news-sentiment-live/61-CONTEXT.md
@.planning/phases/61-news-sentiment-live/61-01-SUMMARY.md
@.planning/phases/61-news-sentiment-live/61-02-SUMMARY.md

@web/api/routers/news.py
@web/api/services/news_service.py
@web/api/models/schemas.py
@web/frontend/src/app/dashboard/news/page.tsx
@web/frontend/src/app/dashboard/players/[id]/page.tsx

<interfaces>
Existing NewsItem Pydantic model (web/api/models/schemas.py) likely has fields like:
```python
class NewsItem(BaseModel):
    external_id: str
    title: str
    url: str
    source: str
    published_at: str
    player_names: List[str]
    player_ids: List[str]
    team_hint: Optional[str]
```
This plan adds an `event_flags: List[str]` field (list of human-readable tags like "questionable", "traded") plus optional `summary: Optional[str]` (populated only by Plan 06 when LLM enrichment is enabled; null otherwise).

New TeamEvents Pydantic model for NEWS-03:
```python
class TeamEvents(BaseModel):
    team: str                   # 3-letter abbr
    negative_event_count: int   # ruled_out + inactive + usage_drop + weather_risk + suspended
    positive_event_count: int   # returning + activated + usage_boost + signed
    neutral_event_count: int    # traded + questionable
    total_articles: int
    sentiment_label: str        # "bullish" | "bearish" | "neutral"
    top_events: List[str]       # human-readable summaries of the 3 loudest events
```

New PlayerEventBadges Pydantic model for NEWS-04:
```python
class PlayerEventBadges(BaseModel):
    player_id: str
    badges: List[str]           # e.g. ["Questionable", "Returning", "Usage Boost"]
    overall_label: str          # "bullish" | "bearish" | "neutral"
    article_count: int
    most_recent_article: Optional[NewsItem]
```

Event-flag → human label mapping (establish ONCE in news_service.py and reuse):
```python
EVENT_LABELS = {
    "is_ruled_out": "Ruled Out",
    "is_inactive": "Inactive",
    "is_questionable": "Questionable",
    "is_suspended": "Suspended",
    "is_returning": "Returning",
    "is_traded": "Traded",
    "is_released": "Released",
    "is_signed": "Signed",
    "is_activated": "Activated",
    "is_usage_boost": "Usage Boost",
    "is_usage_drop": "Usage Drop",
    "is_weather_risk": "Weather Risk",
}
```

Sentiment bucket assignment (per D-03 — discrete, not continuous):
```python
NEGATIVE = {"is_ruled_out", "is_inactive", "is_suspended", "is_usage_drop", "is_weather_risk", "is_released"}
POSITIVE = {"is_returning", "is_activated", "is_usage_boost", "is_signed"}
NEUTRAL  = {"is_traded", "is_questionable"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend news_service + router with event-based endpoints</name>
  <files>web/api/services/news_service.py, web/api/routers/news.py, web/api/models/schemas.py, tests/web/test_news_router_live.py</files>
  <behavior>
    - Test 1: get_news_feed returns items with event_flags list populated from Silver signal records
    - Test 2: get_team_event_density(season, week) returns exactly 32 rows (one per team) even when only a subset have events (zero-fill the rest)
    - Test 3: A team with 3 is_ruled_out events has sentiment_label == "bearish"
    - Test 4: A team with 5 is_usage_boost events has sentiment_label == "bullish"
    - Test 5: A team with 0 events has sentiment_label == "neutral" and all counts == 0
    - Test 6: get_player_event_badges(player_id) returns unique badge labels (no duplicates) sorted by frequency desc
    - Test 7: All endpoints return empty shapes ([] or zero-filled rows) when Gold data does not exist for the requested week
  </behavior>
  <action>
    1. **Add Pydantic models** in `web/api/models/schemas.py`:
       - Extend `NewsItem` with `event_flags: List[str] = []` and `summary: Optional[str] = None`.
       - Add `TeamEvents` and `PlayerEventBadges` models as shown in `<interfaces>`.

    2. **Add EVENT_LABELS, NEGATIVE, POSITIVE, NEUTRAL constants** at the top of `web/api/services/news_service.py`.

    3. **Extend get_news_feed / get_player_news** in `news_service.py` to populate `event_flags` from the Silver signal records. Each signal record already has an `events` dict (written by Plan 02's `_build_silver_record` update). For each True flag, append the mapped human label from `EVENT_LABELS`.

    4. **Add new service function** `get_team_event_density(season: int, week: int) -> List[dict]`:
       - Read Gold sentiment Parquet for (season, week). If empty, read raw Silver signals and aggregate to team level.
       - Group event flags by team (use `team_hint` from Bronze items or map via `player_id` → team via rosters).
       - Produce one row per team; any of the 32 NFL teams missing from the data gets a zero-filled row.
       - Compute sentiment_label from counts: bearish if negative_event_count > positive_event_count * 1.5, bullish if positive > negative * 1.5, else neutral.
       - Populate top_events with up to 3 human-readable entries like "3x Questionable, 1x Traded".

    5. **Add new service function** `get_player_event_badges(player_id: str, season: int, week: int) -> dict`:
       - Read the player's Silver signals for the week.
       - Aggregate all True event flags into a deduplicated list, sorted by occurrence count desc.
       - Return the `PlayerEventBadges` shape.

    6. **Add router endpoints** in `web/api/routers/news.py`:
       - `GET /news/team-events?season=&week=` → `List[TeamEvents]` (exactly 32 entries)
       - `GET /news/player-badges/{player_id}?season=&week=` → `PlayerEventBadges`
       - Both endpoints return 200 with zero-filled data on empty state, not 404. Log-and-return-empty on exceptions, matching the existing router pattern.

    7. **Add integration tests** in `tests/web/test_news_router_live.py`:
       - Use FastAPI's `TestClient` against `web/api/main:app`.
       - Use tmpdir fixtures that write sample Silver signal JSON files to simulate populated Gold data.
       - Cover the 7 behaviors listed above.

    8. Type hints, Google-style docstrings, `logger` (no `print`). Tests pass on local machine with `python -m pytest tests/web/test_news_router_live.py -v`.

    This delivers the backend for NEWS-02 (feed with tags), NEWS-03 (team grid), NEWS-04 (player badges).
  </action>
  <verify>
    <automated>source venv/bin/activate && python -m pytest tests/web/test_news_router_live.py -v</automated>
  </verify>
  <done>All 7 tests pass. `curl http://localhost:8000/news/team-events?season=2026&week=1 | jq 'length'` returns 32 (when API is running). New endpoints visible in OpenAPI schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build EventBadges + TeamEventDensityGrid React components</name>
  <files>web/frontend/src/features/nfl/components/EventBadges.tsx, web/frontend/src/features/nfl/components/TeamEventDensityGrid.tsx, web/frontend/src/app/dashboard/players/[id]/page.tsx, web/frontend/src/app/dashboard/news/page.tsx</files>
  <behavior>
    - Test 1 (visual via story or unit test): EventBadges with `["Questionable", "Returning"]` renders 2 pill badges with distinct color classes (red for bearish, green for bullish, yellow for neutral)
    - Test 2: EventBadges renders null/nothing for empty array (no empty wrapper)
    - Test 3: TeamEventDensityGrid renders exactly 32 team tiles in a grid layout
    - Test 4: Teams with sentiment_label "bearish" get red background; "bullish" get green; "neutral" get gray
    - Test 5: Clicking a team tile navigates to a filtered news feed for that team
  </behavior>
  <action>
    1. **Create `EventBadges.tsx`** as a presentational React component:
       - Props: `{ badges: string[]; overallLabel?: "bullish" | "bearish" | "neutral" }`.
       - Maps badge names to color classes using a lookup: bearish for {Ruled Out, Inactive, Suspended, Usage Drop, Weather Risk, Released}; bullish for {Returning, Activated, Usage Boost, Signed}; neutral for {Traded, Questionable}.
       - Uses Tailwind (inspect existing components like `ProjectionsTable` for the exact class conventions and component library idiom). Pill shape, small font, tight spacing, consistent with existing badges in the app.
       - Returns null when `badges.length === 0` (so zero-state is clean).
       - TypeScript strict, no `any`.

    2. **Create `TeamEventDensityGrid.tsx`**:
       - Props: `{ season: number; week: number }`.
       - Fetches `/news/team-events?season=&week=` on mount (or is rendered as a server component per the project's Next.js conventions — inspect `news/page.tsx` to match).
       - Renders a 4×8 or 8×4 grid of 32 team tiles (use CSS grid via Tailwind).
       - Each tile: team logo (reuse existing team-logo helper if present — grep for `teamLogo` in frontend), team abbr, negative/positive/neutral counts, top_events as a small text list.
       - Background color class keyed off `sentiment_label` ("bg-red-100 dark:bg-red-900" for bearish, "bg-green-100 dark:bg-green-900" for bullish, "bg-neutral-100 dark:bg-neutral-800" for neutral). Match existing brand colors — check tailwind.config.ts or CSS variables.
       - Each tile is a link that navigates to `/dashboard/news?team={abbr}` (existing feed supports team query param).
       - Accessibility: each tile is a keyboard-focusable link with aria-label.

    3. **Update `web/frontend/src/app/dashboard/news/page.tsx`**:
       - Render real articles from `/news/feed` (this endpoint already exists — verify it now returns `event_flags` per Task 1).
       - For each article, render: source badge, publication date, title as link to URL, truncated body preview, player tag pills, and `<EventBadges badges={item.event_flags} />`.
       - Add a filter control (source, team, date range) if not already present — inspect the existing page; reuse existing filter components when possible.

    4. **Update `web/frontend/src/app/dashboard/players/[id]/page.tsx`**:
       - Call `/news/player-badges/{id}` on load (server component preferred per Next.js 14+ convention).
       - Render an `<EventBadges badges={data.badges} overallLabel={data.overall_label} />` block in the player header card.
       - Below it, render the top 5 most recent news items for the player using the same article component from the news page.

    5. **Add storybook or unit tests** where the project already has testing patterns. Look for existing `*.test.tsx` or `*.spec.tsx` files — if none exist, create `EventBadges.test.tsx` using the existing test runner (bun test if package.json says so, else jest/vitest). Cover the 5 behaviors listed above. If the project has NO frontend test infrastructure, write a minimal smoke test and document this limitation in the plan summary.

    6. TypeScript: strict mode, Google-style JSDoc on exported components, no `any`.

    This delivers the UI for NEWS-02, NEWS-03, NEWS-04.
  </action>
  <verify>
    <automated>cd web/frontend && (bun test --filter EventBadges 2>/dev/null || npm test -- --filter EventBadges 2>/dev/null || npx tsc --noEmit)</automated>
  </verify>
  <done>Components exist, pass type checks, and tests pass if a runner is present. Manually verified by loading the news page, the team grid, and a player detail page in Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually verify end-to-end UI against live data</name>
  <what-built>Backend endpoints for team events + player badges; frontend components rendering article feeds, team grid, and player badges from real rule-extracted event data.</what-built>
  <how-to-verify>
    1. Start the backend locally: `./web/run_dev.sh` (or `uvicorn web.api.main:app --reload --port 8000`).
    2. Start the frontend: `cd web/frontend && bun dev` (or `npm run dev`). Open http://localhost:3000/dashboard/news.
    3. Verify the news page (NEWS-02):
       - Articles show source (e.g., "Reddit r/fantasyfootball", "RotoWire", "PFT", "ESPN RSS")
       - Each article has a publication date, a title link, and player tag pills
       - At least one article has ≥1 event badge (e.g., "Questionable", "Returning", "Usage Boost")
    4. Navigate to http://localhost:3000/dashboard/news (scroll to the team grid, OR visit /dashboard/predictions if the grid lives there per existing layout). Verify NEWS-03:
       - All 32 teams are visible
       - Colors are applied (at least one red team, at least one green team — if data allows)
       - Clicking a team filters the news feed to that team
    5. Navigate to a player detail page, e.g., http://localhost:3000/dashboard/players/00-0033873 (Patrick Mahomes). Verify NEWS-04:
       - Player header shows event badges (if any exist in current data)
       - Below the header, recent news items are listed
    6. If Gold data is sparse and some flows show empty state, that is acceptable — confirm the empty state renders without crashes (zero-state copy + no red errors in console).

    Expected outcome: All four ROADMAP success criteria visible in the browser. No console errors. No 500 responses from the backend.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues encountered</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → browser | API responses carry untrusted title/body strings from public RSS/Reddit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Tampering | Article title/body rendering | mitigate | React auto-escapes string children; do NOT use `dangerouslySetInnerHTML` anywhere in EventBadges, TeamEventDensityGrid, or news page components |
| T-61-05-02 | Tampering | Article URL as link href | mitigate | Sanitize `item.url` — reject non-http(s) schemes; add `rel="noopener noreferrer"` on all external links |
| T-61-05-03 | Information Disclosure | Player badge API | accept | No PII exposure; all data is public NFL news |
| T-61-05-04 | Denial of Service | Team grid unbounded query | mitigate | Team-events endpoint fixed at 32 rows; player-badges endpoint caps recent items at 10 |
</threat_model>

<verification>
Backend: `python -m pytest tests/web/test_news_router_live.py -v`
Frontend types: `cd web/frontend && npx tsc --noEmit`
Live smoke: Human checkpoint Task 3 loads the three pages.
</verification>

<success_criteria>
- NEWS-02: news page lists real articles with source + date + player tags + event badges
- NEWS-03: team sentiment dashboard renders all 32 teams with color coding tied to event density
- NEWS-04: player detail pages show bullish/bearish badges derived from event flags
- All three API endpoints return valid shapes with zero-state fallback
- Zero console errors on live smoke
</success_criteria>

<output>
After completion, create `.planning/phases/61-news-sentiment-live/61-05-SUMMARY.md` with:
- Endpoint signatures (request/response)
- Screenshots (or descriptions) of the three verified pages
- Any empty-state UI decisions documented
</output>
