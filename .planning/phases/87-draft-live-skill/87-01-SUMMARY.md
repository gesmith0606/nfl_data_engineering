# 87-01 Summary

Shipped (commit 9680da0): `scripts/draft_live.py` (resolve by username/draft-id,
snapshot + `--watch` + `--json`, `--manual`/`--add-pick` D-09 fallback) and
`.claude/skills/draft-live/SKILL.md` (`/draft-live` + D-05 proactivity policy). Docs in
CLAUDE.md + cross-link from `/draft-prep`. 3 tests.
Requirements: SKILL-01..04. ✓
