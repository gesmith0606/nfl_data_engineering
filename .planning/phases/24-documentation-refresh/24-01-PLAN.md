---
phase: 24-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/NFL_DATA_DICTIONARY.md
  - docs/NFL_GAME_PREDICTION_DATA_MODEL.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "Data dictionary contains schema definitions for all 12 Silver output paths with column names, types, and descriptions"
    - "Data dictionary contains the existing Gold fantasy projection schema (25 columns)"
    - "Data dictionary contains the planned Gold game prediction schema marked as Planned"
    - "Aspirational Silver sections (Games Silver, Teams Silver) are replaced with real schemas from parquet files"
  artifacts:
    - path: "docs/NFL_DATA_DICTIONARY.md"
      provides: "Complete Silver and Gold layer schema documentation"
      contains: "## Silver Layer Tables"
  key_links:
    - from: "docs/NFL_DATA_DICTIONARY.md"
      to: "data/silver/**/*.parquet"
      via: "Schema extracted using pyarrow.parquet.read_schema()"
      pattern: "Silver Layer Tables"
    - from: "docs/NFL_DATA_DICTIONARY.md"
      to: "data/gold/**/*.parquet"
      via: "Schema extracted for existing fantasy projections"
      pattern: "Gold Layer Tables"
---

<objective>
Update the Silver and Gold sections of NFL_DATA_DICTIONARY.md with accurate schemas extracted from actual parquet files. Replace the 2 aspirational Silver tables with real schemas from all 12 Silver output paths, document the existing Gold fantasy projection schema (25 columns), and add the planned Gold game prediction schema.

Purpose: Downstream planning agents and Phase 25 feature assembly need accurate schema documentation to build game-level differential features from Silver data.
Output: Updated docs/NFL_DATA_DICTIONARY.md with complete Silver (12 tables) and Gold (2 tables) schemas.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-documentation-refresh/24-CONTEXT.md
@.planning/phases/24-documentation-refresh/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Silver layer schemas and replace aspirational content in data dictionary</name>
  <files>docs/NFL_DATA_DICTIONARY.md</files>
  <read_first>
    - docs/NFL_DATA_DICTIONARY.md (current state — Silver section starts at line 930, Gold at line 1137)
    - .planning/phases/24-documentation-refresh/24-RESEARCH.md (Silver path inventory with column counts at lines 79-97)
  </read_first>
  <action>
Use pyarrow to extract schemas from all 12 Silver output paths, then rewrite the Silver Layer Tables section (lines 930 onward to line ~1136) of docs/NFL_DATA_DICTIONARY.md.

**Step 1: Extract schemas.** For each of the 12 Silver paths below, find the latest parquet file and run `pyarrow.parquet.read_schema()` to get column names and types:

1. `data/silver/defense/positional/` — 6 columns, from game_context.py
2. `data/silver/players/advanced/` — 119 columns, from player_advanced_analytics.py
3. `data/silver/players/historical/` — 63 columns, from historical_profiles.py
4. `data/silver/players/usage/` — 173 columns, from player_analytics.py
5. `data/silver/teams/game_context/` — 22 columns, from game_context.py
6. `data/silver/teams/pbp_derived/` — 164 columns, from team_analytics.py
7. `data/silver/teams/pbp_metrics/` — 63 columns, from team_analytics.py
8. `data/silver/teams/playoff_context/` — 10 columns, from game_context.py
9. `data/silver/teams/referee_tendencies/` — 4 columns, from game_context.py
10. `data/silver/teams/situational/` — 51 columns, from team_analytics.py
11. `data/silver/teams/sos/` — 21 columns, from team_analytics.py
12. `data/silver/teams/tendencies/` — 23 columns, from team_analytics.py

**Step 2: Replace the Silver section.** Delete everything from `## Silver Layer Tables` (line 930) through the line before `## Gold Layer Tables` (line 1137). Replace with 12 subsections, one per Silver path, using this format per table:

```markdown
### N. [Table Name]

**Source Module:** `src/[module].py`
**Local Path:** `data/silver/[path]/`
**S3 Path:** `s3://nfl-refined/[path]/season=YYYY/`
**Columns:** [count]

| Column Name | Data Type | Nullable | Description | Example |
|-------------|-----------|----------|-------------|---------|
| col_name | type | Yes/No | Brief description | example_value |
```

For Nullable: mark partition columns (season, week) and primary identifiers as NO, everything else YES.
For Description: write a brief 5-15 word description based on column name semantics (e.g., `pass_rate_over_expected` -> "Team pass rate compared to league expected rate").
For Example: use realistic NFL values (team abbrs like "KC", seasons like 2024, stats like 0.45).

For tables with >50 columns (players/advanced at 119, players/usage at 173, teams/pbp_derived at 164, teams/pbp_metrics at 63, teams/situational at 51): document ALL columns but group them under sub-headers by category (e.g., "Identifiers", "Passing Metrics", "Rushing Metrics", "Receiving Metrics", etc.) to keep the table readable.

**Step 3: Verify** the two old aspirational tables ("Games (Silver)" and "Teams (Silver)") are fully removed — they should not appear in the final output.

NOTE: The roadmap says "11 Silver output paths" but research confirmed 12 exist on disk. Document all 12.
  </action>
  <verify>
    <automated>grep -c "### [0-9]" docs/NFL_DATA_DICTIONARY.md | grep -q "1[2-9]\|[2-9][0-9]" && echo "At least 12 subsections found" && ! grep -q "Games (Silver)" docs/NFL_DATA_DICTIONARY.md && echo "Aspirational content removed" && grep -q "defense/positional" docs/NFL_DATA_DICTIONARY.md && echo "All 12 paths present"</automated>
  </verify>
  <acceptance_criteria>
    - docs/NFL_DATA_DICTIONARY.md contains `## Silver Layer Tables` section header
    - docs/NFL_DATA_DICTIONARY.md contains exactly 12 numbered subsection headers matching `### N.` pattern (N from 1-12)
    - docs/NFL_DATA_DICTIONARY.md contains strings: `defense/positional`, `players/advanced`, `players/historical`, `players/usage`, `teams/game_context`, `teams/pbp_derived`, `teams/pbp_metrics`, `teams/playoff_context`, `teams/referee_tendencies`, `teams/situational`, `teams/sos`, `teams/tendencies`
    - docs/NFL_DATA_DICTIONARY.md does NOT contain `Games (Silver)` or `games_silver` (aspirational content removed)
    - Each Silver subsection contains a markdown table with `| Column Name | Data Type |` header row
    - docs/NFL_DATA_DICTIONARY.md `## Gold Layer Tables` section still exists (not accidentally deleted)
  </acceptance_criteria>
  <done>All 12 Silver output paths have schema tables with column names, types, and descriptions extracted from actual parquet files. Old aspirational Silver content is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Document Gold layer schemas — existing fantasy projections and planned game predictions</name>
  <files>docs/NFL_DATA_DICTIONARY.md</files>
  <read_first>
    - docs/NFL_DATA_DICTIONARY.md (Gold section starts at line ~1137 after Task 1 rewrites — re-read to find new position)
    - data/gold/ (list directory to find latest fantasy projection parquet for schema extraction)
    - .planning/REQUIREMENTS.md (PRED-01, PRED-02, PRED-03 define planned prediction columns)
    - .planning/phases/24-documentation-refresh/24-RESEARCH.md (Gold schemas at lines 102-112)
  </read_first>
  <action>
Rewrite the `## Gold Layer Tables` section of docs/NFL_DATA_DICTIONARY.md with two subsections:

**Subsection 1: Fantasy Projections (Existing)**

Extract schema from the latest Gold fantasy projection parquet file in `data/gold/` using `pyarrow.parquet.read_schema()`. Expected 25 columns:
`player_id, player_name, position, recent_team, proj_season, proj_week, proj_passing_yards, proj_passing_tds, proj_interceptions, proj_rushing_yards, proj_rushing_tds, proj_carries, proj_receptions, proj_receiving_yards, proj_receiving_tds, proj_targets, projected_points, is_bye_week, is_rookie_projection, vegas_multiplier, position_rank, injury_status, injury_multiplier, projected_floor, projected_ceiling`

Format:
```markdown
### 1. Fantasy Projections

**Status:** Active
**Source Module:** `src/projection_engine.py`
**Local Path:** `data/gold/projections/`
**S3 Path:** `s3://nfl-trusted/projections/season=YYYY/week=WW/`
**Columns:** 25

| Column Name | Data Type | Nullable | Description | Example |
|-------------|-----------|----------|-------------|---------|
```

Write descriptions for all 25 columns. Examples:
- `player_id` -> "Unique player identifier from nfl-data-py"
- `projected_points` -> "Total projected fantasy points for the scoring format"
- `vegas_multiplier` -> "Adjustment factor from implied team total (total/23.0)"
- `projected_floor` -> "Low-end projection based on position-specific variance"

**Subsection 2: Game Predictions (Planned)**

Add a section for the planned game prediction output. Mark it clearly as planned with a badge:

```markdown
### 2. Game Predictions

**Status:** Planned (v1.4)
**Source Module:** `scripts/generate_predictions.py` (Phase 27)
**Local Path:** `data/gold/predictions/season=YYYY/week=WW/`
**S3 Path:** `s3://nfl-trusted/predictions/season=YYYY/week=WW/`
**Columns:** 13 (planned)

> This schema is planned for v1.4 Phase 27. Columns are derived from requirements PRED-01, PRED-02, PRED-03.

| Column Name | Data Type | Nullable | Description | Example |
|-------------|-----------|----------|-------------|---------|
| game_id | STRING | NO | Unique game identifier | "2025_10_KC_BUF" |
| season | INT | NO | NFL season year | 2025 |
| week | INT | NO | NFL week number | 10 |
| home_team | STRING | NO | Home team abbreviation | "KC" |
| away_team | STRING | NO | Away team abbreviation | "BUF" |
| model_spread | FLOAT | NO | Model-predicted point spread (negative = home favored) | -3.5 |
| model_total | FLOAT | NO | Model-predicted over/under total points | 48.2 |
| vegas_spread | FLOAT | YES | Vegas closing line spread for comparison | -4.0 |
| vegas_total | FLOAT | YES | Vegas closing line over/under for comparison | 47.5 |
| spread_edge | FLOAT | YES | Model spread minus Vegas spread (positive = model sees more home advantage) | 0.5 |
| total_edge | FLOAT | YES | Model total minus Vegas total (positive = model sees higher scoring) | 0.7 |
| spread_confidence_tier | STRING | YES | Edge magnitude tier: high (>3pts), medium (1.5-3pts), low (<1.5pts) | "low" |
| total_confidence_tier | STRING | YES | Edge magnitude tier: high (>3pts), medium (1.5-3pts), low (<1.5pts) | "medium" |
| model_version | STRING | NO | Version identifier for the model that generated this prediction | "v1.4.0" |
| prediction_timestamp | TIMESTAMP | NO | UTC datetime when prediction was generated | "2025-11-05T14:30:00Z" |
```

Also review docs/NFL_GAME_PREDICTION_DATA_MODEL.md for staleness. If it still mentions LightGBM as a model option, update references to say "XGBoost only (LightGBM dropped per v1.4 decision)". If it says "v1.2 in progress" for Silver, update to "v1.2 shipped 2026-03-15". Keep changes minimal and targeted.
  </action>
  <verify>
    <automated>grep -q "Fantasy Projections" docs/NFL_DATA_DICTIONARY.md && grep -q "Game Predictions" docs/NFL_DATA_DICTIONARY.md && grep -q "Planned (v1.4)" docs/NFL_DATA_DICTIONARY.md && grep -q "spread_edge" docs/NFL_DATA_DICTIONARY.md && grep -q "projected_points" docs/NFL_DATA_DICTIONARY.md && echo "Gold schemas complete"</automated>
  </verify>
  <acceptance_criteria>
    - docs/NFL_DATA_DICTIONARY.md contains `### 1. Fantasy Projections` with `**Status:** Active`
    - docs/NFL_DATA_DICTIONARY.md contains `### 2. Game Predictions` with `**Status:** Planned (v1.4)`
    - Fantasy Projections section contains all 25 column names including `projected_points`, `vegas_multiplier`, `projected_floor`, `projected_ceiling`
    - Game Predictions section contains all 15 column names including `model_spread`, `spread_edge`, `spread_confidence_tier`, `model_version`
    - docs/NFL_GAME_PREDICTION_DATA_MODEL.md does not contain "LightGBM" as an active model option (either removed or marked as dropped)
  </acceptance_criteria>
  <done>Gold section has both existing fantasy projection schema (25 columns from parquet) and planned game prediction schema (15 columns from requirements). Prediction data model doc updated if stale.</done>
</task>

</tasks>

<verification>
- `grep -c "###" docs/NFL_DATA_DICTIONARY.md` shows at least 14 subsection headers (12 Silver + 2 Gold)
- `grep "Games (Silver)" docs/NFL_DATA_DICTIONARY.md` returns no matches (aspirational content removed)
- `grep "defense/positional" docs/NFL_DATA_DICTIONARY.md` returns a match (all 12 Silver paths documented)
- `grep "Planned (v1.4)" docs/NFL_DATA_DICTIONARY.md` returns a match (Gold prediction schema marked as planned)
- `grep "spread_edge" docs/NFL_DATA_DICTIONARY.md` returns a match (prediction columns documented)
</verification>

<success_criteria>
1. Data dictionary Silver section contains schema definitions for all 12 Silver output paths (not 11 -- research confirmed 12)
2. Each Silver schema table has column names, data types, nullable flags, descriptions, and examples extracted from actual parquet files
3. Gold section has the existing fantasy projection schema (25 columns from real parquet) and planned game prediction schema (15 columns from requirements)
4. Aspirational Silver content (Games Silver, Teams Silver) is fully removed
5. Planned prediction schema is clearly marked with "Planned (v1.4)" badge
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation-refresh/24-01-SUMMARY.md`
</output>
