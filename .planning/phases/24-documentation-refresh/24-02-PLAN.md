---
phase: 24-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md
  - docs/BRONZE_LAYER_DATA_INVENTORY.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "CLAUDE.md reflects current architecture with 15+ Bronze types, 12 Silver paths, 360 tests, and v1.3 completion status"
    - "Implementation guide shows phases 18-23 as completed and phases 24-27 as planned with status badges"
    - "Bronze inventory shows PBP at 140 columns and includes officials data type"
    - "CLAUDE.md key files table includes nfl_data_adapter.py, team_analytics.py, game_context.py, historical_profiles.py, player_advanced_analytics.py"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate project reference for all Claude Code sessions"
      contains: "360"
    - path: "docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md"
      provides: "Implementation history through v1.3 with v1.4 planned phases"
      contains: "Phase 23"
    - path: "docs/BRONZE_LAYER_DATA_INVENTORY.md"
      provides: "Current Bronze layer file inventory"
      contains: "officials"
  key_links:
    - from: "CLAUDE.md"
      to: "src/*.py"
      via: "Key Files table references all current modules"
      pattern: "team_analytics.py"
---

<objective>
Refresh CLAUDE.md with current architecture and status, update the implementation guide with phases 18-23 completed and v1.4 planned, and regenerate the Bronze inventory to show PBP at 140 columns and officials data type.

Purpose: CLAUDE.md is loaded into every Claude Code session and must be accurate. The implementation guide and Bronze inventory are key references for downstream planning.
Output: Updated CLAUDE.md, docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md, docs/BRONZE_LAYER_DATA_INVENTORY.md
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-documentation-refresh/24-CONTEXT.md
@.planning/phases/24-documentation-refresh/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Bronze inventory and update implementation guide</name>
  <files>docs/BRONZE_LAYER_DATA_INVENTORY.md, docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md</files>
  <read_first>
    - docs/BRONZE_LAYER_DATA_INVENTORY.md (current state — 33 lines, last generated 2026-03-11)
    - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md (current state — 627 lines, last documented phase is 17, says v1.2 in progress)
    - scripts/generate_inventory.py (understand how it works, what arguments it accepts)
    - .planning/phases/24-documentation-refresh/24-RESEARCH.md (implementation guide gaps at lines 142-151, Bronze inventory details)
    - .planning/ROADMAP.md (phase completion dates for phases 18-23, v1.4 phases 24-27)
  </read_first>
  <action>
**Part A: Bronze Inventory (DOCS-05)**

Run the existing inventory generation script to regenerate the Bronze inventory:
```bash
source venv/bin/activate
python scripts/generate_inventory.py > docs/BRONZE_LAYER_DATA_INVENTORY.md
```

After generation, verify the output contains:
- A row for `pbp` showing 140 in the columns field
- A row for `officials` as a data type
- Total file count and size summary at the top

If the script does not output to the correct format, capture its stdout and write to docs/BRONZE_LAYER_DATA_INVENTORY.md.

**Part B: Implementation Guide (DOCS-04)**

Update docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md with the following specific changes:

1. **Add phases 18-23 as completed.** After the existing Phase 17 section, add sections for:
   - Phase 18: Historical Context — Completed 2026-03-15 (v1.2). Player career trajectories, historical performance profiles, rookie/veteran classification.
   - Phase 19: v1.2 Tech Debt Cleanup — Completed 2026-03-15 (v1.2). Code cleanup, test additions, documentation.
   - Phase 20: Infrastructure and Data Expansion — Completed 2026-03-16 (v1.3). PBP expanded to 140 columns, officials data ingested, nfl_data_adapter.py created.
   - Phase 21: PBP-Derived Team Metrics — Completed 2026-03-16 (v1.3). Team PBP analytics with 164 derived columns, scoring context, game situation analysis.
   - Phase 22: Schedule-Derived Context — Completed 2026-03-17 (v1.3). Game context features, referee tendencies, playoff context, defensive positional stats.
   - Phase 23: Cross-Source Features and Integration — Completed 2026-03-19 (v1.3). 337-column prediction feature vector from 8 Silver sources, cross-source validation.

   Use the same format as existing phase sections (header, bullet points of what was done, completion date badge).

2. **Add v1.4 phases 24-27 as planned.** After Phase 23:
   - Phase 24: Documentation Refresh — In Progress. Update all project docs for v1.3 completion.
   - Phase 25: Feature Assembly and Model Training — Planned. XGBoost spread/total models with walk-forward CV.
   - Phase 26: Backtesting and Validation — Planned. ATS accuracy, profit analysis, holdout validation.
   - Phase 27: Prediction Pipeline — Planned. Weekly predictions with edge detection vs Vegas.

   Mark each with a status badge: `In Progress` or `Planned`.

3. **Update milestone summary table.** Find the milestone/status table and update:
   - v1.2 Silver Expansion: change from "In Progress" to "Shipped 2026-03-15"
   - Add v1.3 Prediction Data Foundation: "Shipped 2026-03-19"
   - Add v1.4 ML Game Prediction: "In Progress"

4. **Update test count.** Find all occurrences of "274" (old test count) and replace with "360".

5. **Update Silver data paths list.** Find the section listing Silver output paths and add the missing ones:
   - `defense/positional` (defensive positional stats)
   - `teams/game_context` (game-level context features)
   - `teams/pbp_derived` (PBP-derived team metrics — 164 columns)
   - `teams/playoff_context` (playoff seeding and context)
   - `teams/referee_tendencies` (referee penalty and call patterns)
   - `players/historical` (career trajectory profiles)

6. **Update ML section.** If the guide mentions "XGBoost, LightGBM" as planned models, change to "XGBoost only (LightGBM dropped — insufficient benefit at ~1,900 game scale)".
  </action>
  <verify>
    <automated>source venv/bin/activate && python scripts/generate_inventory.py 2>&1 | grep -q "pbp" && python scripts/generate_inventory.py 2>&1 | grep -q "officials" && echo "Inventory script outputs pbp and officials" && grep -q "Phase 23" docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md && grep -q "Phase 27" docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md && grep -q "360" docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md && echo "Implementation guide updated"</automated>
  </verify>
  <acceptance_criteria>
    - docs/BRONZE_LAYER_DATA_INVENTORY.md contains a row with `pbp` and `140` on the same line
    - docs/BRONZE_LAYER_DATA_INVENTORY.md contains a row with `officials`
    - docs/BRONZE_LAYER_DATA_INVENTORY.md has a generated timestamp from today (2026-03-20)
    - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md contains `Phase 18`, `Phase 19`, `Phase 20`, `Phase 21`, `Phase 22`, `Phase 23` sections
    - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md contains `Phase 24`, `Phase 25`, `Phase 26`, `Phase 27` sections with status badges
    - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md contains `360` (test count) and does NOT contain `274` as a test count
    - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md contains `v1.3` and `Shipped` or `Complete` on the same line in the milestone table
    - docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md does NOT contain "LightGBM" as an active planned model
  </acceptance_criteria>
  <done>Bronze inventory regenerated from script showing PBP at 140 columns and officials data type. Implementation guide has phases 18-23 completed and 24-27 planned with correct test counts and milestone statuses.</done>
</task>

<task type="auto">
  <name>Task 2: Full refresh of CLAUDE.md with current architecture, key files, and status</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current state — 136 lines, stale on test count, data types, Silver description, status, key files)
    - .planning/phases/24-documentation-refresh/24-RESEARCH.md (CLAUDE.md gaps table at lines 129-141)
    - src/config.py (S3 paths, scoring configs — lines 1-50 for current config values)
    - .planning/ROADMAP.md (v1.4 phase details for status section)
  </read_first>
  <action>
Perform a full refresh of CLAUDE.md. The file is 136 lines and loaded into every Claude Code session, so keep it concise but accurate. Make these specific updates:

**1. Architecture diagram** (the ASCII art section). Update the Bronze description and add Silver detail:

Current (stale):
```
Bronze (s3://nfl-raw/)     -- raw game, player, snap, injury, roster data
        |
Silver (s3://nfl-refined/) -- usage metrics, rolling averages, opp rankings (1-32)
```

Replace with:
```
Bronze (s3://nfl-raw/)     -- 15+ data types: PBP (140 cols), player stats, schedules,
                              snap counts, injuries, rosters, NGS, PFR, QBR, depth charts,
                              draft picks, combine, officials, teams
        |
Silver (s3://nfl-refined/) -- 12 paths: player usage/advanced/historical, team PBP metrics/
                              tendencies/SOS/situational, game context, referee, defense,
                              playoff context, PBP-derived (164 cols)
        |
Gold   (s3://nfl-trusted/) -- fantasy projections (PPR/Half-PPR/Standard) + game predictions (v1.4)
```

Keep the Draft Tool and Neo4j deferred lines as-is.

**2. Development Commands — data-types comment.** Update the comment listing data types:

Current: `# data-types: schedules, pbp, teams, player_weekly, snap_counts, injuries, rosters, player_seasonal`

Replace with: `# data-types: schedules, pbp, teams, player_weekly, snap_counts, injuries, rosters, player_seasonal, ngs, pfr_stats, qbr, depth_charts, draft_picks, combine, officials`

**3. Key Files table.** Add these missing modules after the existing entries:

| `src/nfl_data_adapter.py` | NFLDataAdapter — unified data fetching with local-first reads |
| `src/team_analytics.py` | Team PBP metrics, tendencies, SOS, situational splits |
| `src/player_advanced_analytics.py` | Advanced player profiles, target shares, efficiency metrics |
| `src/game_context.py` | Game context features, referee tendencies, playoff context, defense positional |
| `src/historical_profiles.py` | Career trajectories, historical performance, rookie/veteran classification |
| `scripts/silver_team_transformation.py` | Silver team metrics CLI — PBP metrics, tendencies, SOS, situational |
| `scripts/silver_game_context.py` | Silver game context CLI — game context, referee, playoff, defense |
| `scripts/silver_advanced_profiles.py` | Silver advanced profiles CLI — player advanced and historical |

Also update the existing `src/nfl_data_integration.py` description from "NFLDataFetcher — all 8 fetch methods + validate_data()" to "NFLDataFetcher — legacy fetcher (see nfl_data_adapter.py) + validate_data()".

Verify each script/module exists on disk before adding (some may have slightly different names). Use `ls src/*.py scripts/silver*.py` to confirm actual filenames.

**4. Status section.** Replace the entire Status section:

Current:
```
**Done**: Bronze (8 types) -> Silver (usage/rolling/rankings) -> Gold (projections w/ injury adjustments, regression shrinkage, floor/ceiling) -> Draft tool (snake/auction/mock/waiver) -> Pipeline monitoring (GHA + health check) -> S3 deduplication fix -> Projection engine (bye weeks, rookies, Vegas lines) -> Backtesting (MAE 4.91, r=0.52 across 3 seasons) -> Sleeper ADP refresh -> 71 unit tests passing

**In progress**: Weekly pipeline cron tuning | Local-first data reads (S3 as fallback)

**Planned**: Neo4j Phase 5 | ML upgrade (RF/XGBoost) | Live Sleeper league integration
```

Replace with:
```
**Done**: v1.0 Bronze Expansion (15+ data types, PBP 140 cols) -> v1.1 Bronze Backfill (2020-2025 historical) -> v1.2 Silver Expansion (12 Silver paths, team/player/game analytics) -> v1.3 Prediction Data Foundation (337-col feature vector, cross-source validation) -> Fantasy: projections (PPR/Half/Standard, bye/rookie/Vegas/injury), draft tool (snake/auction/mock/waiver), backtesting (MAE 4.91) -> 360 tests passing

**In progress**: v1.4 ML Game Prediction (XGBoost spread/total models, walk-forward CV, edge detection)

**Planned**: Neo4j Phase 5 (WR-CB matchups, target networks) | Live Sleeper league integration
```

**5. Test count.** Search for any remaining occurrences of "71 unit tests" or "71 tests" and replace with "360 tests".

**6. Silver description in Architecture section.** Already handled in step 1 above.

**7. NFL Business Rules.** No changes needed — still accurate.

**8. Development Patterns.** No changes needed — still accurate.

Do NOT add a prediction feature vector section or make CLAUDE.md longer than ~150 lines. Conciseness is critical since it is loaded into every session.
  </action>
  <verify>
    <automated>grep -q "360" CLAUDE.md && grep -q "team_analytics.py" CLAUDE.md && grep -q "game_context.py" CLAUDE.md && grep -q "nfl_data_adapter.py" CLAUDE.md && grep -q "v1.4" CLAUDE.md && ! grep -q "71 unit tests" CLAUDE.md && echo "CLAUDE.md refresh complete"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `360` (current test count)
    - CLAUDE.md does NOT contain `71 unit tests` or `71 tests passing`
    - CLAUDE.md contains `nfl_data_adapter.py` in the key files table
    - CLAUDE.md contains `team_analytics.py` in the key files table
    - CLAUDE.md contains `game_context.py` in the key files table
    - CLAUDE.md contains `historical_profiles.py` in the key files table
    - CLAUDE.md contains `player_advanced_analytics.py` in the key files table
    - CLAUDE.md architecture diagram contains `15+` (Bronze data types count)
    - CLAUDE.md architecture diagram contains `12 paths` (Silver paths count)
    - CLAUDE.md Status section contains `v1.4 ML Game Prediction`
    - CLAUDE.md Status section contains `v1.3 Prediction Data Foundation` as done
    - CLAUDE.md data-types comment contains `officials`
    - CLAUDE.md is under 170 lines total (conciseness maintained)
    - CLAUDE.md contains `nfl_data_integration.py` with "legacy" in its description
  </acceptance_criteria>
  <done>CLAUDE.md accurately reflects current platform: 15+ Bronze types, 12 Silver paths, 360 tests, v1.3 done, v1.4 in progress. Key files table includes all v1.2/v1.3 modules. File stays under 170 lines.</done>
</task>

</tasks>

<verification>
- `python scripts/generate_inventory.py 2>&1 | grep pbp` shows 140 columns
- `grep "360" CLAUDE.md` returns a match
- `grep "Phase 23" docs/NFL_DATA_MODEL_IMPLEMENTATION_GUIDE.md` returns a match
- `grep "officials" docs/BRONZE_LAYER_DATA_INVENTORY.md` returns a match
- `grep "v1.4" CLAUDE.md` returns a match
- `wc -l CLAUDE.md` shows under 170 lines
</verification>

<success_criteria>
1. CLAUDE.md reflects current architecture (15+ Bronze, 12 Silver paths, 360 tests, v1.3 done, v1.4 in progress) and stays concise (<170 lines)
2. Key files table in CLAUDE.md includes all v1.2/v1.3 src modules (nfl_data_adapter, team_analytics, game_context, historical_profiles, player_advanced_analytics)
3. Implementation guide has phases 18-23 as completed with dates and phases 24-27 as planned with badges
4. Implementation guide milestone table shows v1.2 shipped, v1.3 shipped, v1.4 in progress
5. Bronze inventory regenerated showing PBP at 140 columns and officials data type
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation-refresh/24-02-SUMMARY.md`
</output>
