---
phase: 11-orchestration-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - docs/BRONZE_LAYER_DATA_INVENTORY.md
autonomous: true
requirements:
  - VALID-02

must_haves:
  truths:
    - "BRONZE_LAYER_DATA_INVENTORY.md reflects all 15+ data type groupings from data/bronze/"
    - "Inventory shows 10-year coverage (2016-2025) for applicable types"
  artifacts:
    - path: "docs/BRONZE_LAYER_DATA_INVENTORY.md"
      provides: "Complete Bronze inventory markdown"
      contains: "Bronze Layer Data Inventory"
  key_links:
    - from: "scripts/generate_inventory.py"
      to: "data/bronze/"
      via: "scan_local() walks directory tree"
      pattern: "scan_local"
    - from: "scripts/generate_inventory.py"
      to: "docs/BRONZE_LAYER_DATA_INVENTORY.md"
      via: "--output flag writes markdown"
      pattern: "--output"
---

<objective>
Regenerate the Bronze inventory document to reflect the complete 15-type, 10-year dataset produced by Phases 9-10.

Purpose: Produces the definitive inventory proving v1.1 backfill completeness (VALID-02).
Output: Updated `docs/BRONZE_LAYER_DATA_INVENTORY.md`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/generate_inventory.py
@tests/test_generate_inventory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Bronze inventory</name>
  <files>docs/BRONZE_LAYER_DATA_INVENTORY.md</files>
  <action>
    Run the existing inventory generator to produce an updated inventory document:

    ```bash
    cd /Users/georgesmith/repos/nfl_data_engineering
    source venv/bin/activate
    python scripts/generate_inventory.py --output docs/BRONZE_LAYER_DATA_INVENTORY.md
    ```

    After generation, verify the output:
    1. Confirm the markdown file lists 15+ data type rows (directory-based groupings may differ slightly from registry keys due to path structure -- e.g., `players/weekly`, `players/seasonal`, `ngs/passing`, `ngs/rushing`, `ngs/receiving`, `pfr/weekly/pass`, etc.)
    2. Confirm season ranges show 2016-2025 coverage for applicable types
    3. Confirm file counts are reasonable (hundreds of files total)

    If the inventory shows fewer than 15 data type groupings, investigate which types are missing by checking `data/bronze/` directory structure.

    Run existing inventory tests to ensure no regressions:
    ```bash
    python -m pytest tests/test_generate_inventory.py -v
    ```
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_generate_inventory.py -v && test -f docs/BRONZE_LAYER_DATA_INVENTORY.md && grep -c "^|" docs/BRONZE_LAYER_DATA_INVENTORY.md</automated>
  </verify>
  <done>
    - docs/BRONZE_LAYER_DATA_INVENTORY.md exists with 15+ data type rows
    - Season ranges reflect 10-year coverage (2016-2025 for most types)
    - All existing generate_inventory tests pass
  </done>
</task>

</tasks>

<verification>
- `docs/BRONZE_LAYER_DATA_INVENTORY.md` has 15+ data type rows in the table
- Season ranges show full coverage (e.g., schedules 2016-2025, injuries 2016-2024)
- `python -m pytest tests/test_generate_inventory.py -v` passes
- `python -m pytest tests/ -v` -- full suite passes
</verification>

<success_criteria>
- BRONZE_LAYER_DATA_INVENTORY.md is regenerated with all 15+ data type groupings
- Inventory reflects the complete v1.1 backfill dataset
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/11-orchestration-and-validation/11-02-SUMMARY.md`
</output>
