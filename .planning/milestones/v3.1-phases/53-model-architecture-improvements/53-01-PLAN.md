# Phase 53-01: Ridge/ElasticNet as Primary Model for RB/WR/TE - Plan

**Created:** 2026-04-02
**Status:** Complete

## Tasks

### T-01: Add Interaction Features
- Create `compute_interaction_features()` in player_feature_engineering.py
- 7 key rolling stats x 3 context columns = up to 21 multiplicative interactions
- Wire into `assemble_player_features()` step 10

### T-02: Implement Ridge/ElasticNet Model Factories
- Add `create_ridge_pipeline()` and `create_elasticnet_pipeline()` to player_model_training.py
- Add `train_position_models_linear()` for full per-position training loop
- Add `predict_player_stats_linear()` for inference
- Skip SHAP feature selection (L2/L1 handles multicollinearity)

### T-03: CLI Integration
- Add `--model-type {xgb, ridge, elasticnet}` flag to train_player_models.py
- Linear models skip SHAP feature selection step

### T-04: Tests and Ship Gate
- Create tests for interaction features and Ridge/ElasticNet pipelines
- Run ship gate: compare Ridge OOF MAE vs heuristic for all positions

## Success Criteria
1. Ridge/ElasticNet pipelines train and predict successfully
2. Ship gate results compared to XGB and heuristic baselines
3. All tests passing
