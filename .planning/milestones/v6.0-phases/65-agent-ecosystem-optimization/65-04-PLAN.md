---
phase: 65-agent-ecosystem-optimization
plan: 04
type: execute
wave: 3
depends_on: ["65-01", "65-02", "65-03"]
files_modified:
  - .planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md
autonomous: false
requirements: [AGNT-04]

must_haves:
  truths:
    - "Every skill and every project-owned agent has been scored on all 8 skill-optimizer dimensions (Clarity, Completeness, Accuracy, Specificity, Anti-patterns, Testability, Freshness, Integration)"
    - "Fewer than 3 items score below 6/10 on any dimension (SHIP GATE — this is the phase SC)"
    - "If >=3 items score below 6/10, the audit does not pass and an additional remediation plan is required"
    - "The scorecard is saved as a durable artifact and referenced by future `/skill-audit` runs"
    - "Human confirms the scorecard pass before phase marked complete"
  artifacts:
    - path: ".planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md"
      provides: "Per-skill/agent scores across 8 dimensions with pass/fail verdict"
      contains: "below_6_count"
  key_links:
    - from: "SKILL-AUDIT-SCORECARD.md"
      to: ".claude/skills/ and .claude/agents/"
      via: "every skill/agent has a row with 8 numeric scores"
      pattern: "Clarity.*Completeness.*Accuracy"
---

<objective>
Run the `skill-optimizer` agent over all project-owned skills and agents, produce a SKILL-AUDIT-SCORECARD.md, and confirm fewer than 3 items score below 6/10. This is the SHIP GATE for phase 65 — if >=3 items fail, the audit does not pass and remediation work is required before phase completion.

Purpose: Satisfies AGNT-04 literally. The skill-optimizer agent already has its evaluation criteria defined in `.claude/agents/skill-optimizer.md`. This plan invokes it, captures the output, and enforces the <3 threshold.

Output: One scorecard document + human confirmation checkpoint. If the audit fails, the checkpoint resume-signal routes back to targeted remediation (not a rewrite).

DEPENDENCIES: Runs AFTER 65-01 (inventory), 65-02 (design consolidation), and 65-03 (NFL rules) so the audit sees the post-cleanup state of the ecosystem — not the pre-cleanup state.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/agents/skill-optimizer.md
@.planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md
@.planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md
@.planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md
@.claude/rules/nfl-data-conventions.md
@.claude/rules/nfl-scoring-formats.md
@.claude/rules/nfl-validation-patterns.md

<interfaces>
<!-- The skill-optimizer agent evaluates each item on 8 dimensions, 1-10 scale -->
<!-- Dimensions (from .claude/agents/skill-optimizer.md): Clarity, Completeness, Accuracy, Specificity, Anti-patterns, Testability, Freshness, Integration -->
<!-- Scope: .claude/skills/*/SKILL.md (25 skills) + project-owned .claude/agents/*.md (11 agents per CLAUDE.md active table) -->
<!-- Framework-owned gsd-* agents are EVALUATED but their scores do NOT count against the <3 threshold — they are upstream-owned -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke skill-optimizer and produce SKILL-AUDIT-SCORECARD.md</name>
  <files>.planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md</files>
  <action>
Spawn the `skill-optimizer` agent (default model: Opus 4.6) with this instruction:

> Audit every skill and project-owned agent in this repo using your 8-dimension evaluation criteria. Output a Markdown scorecard to `.planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md`.
>
> Scope:
> - `.claude/skills/*/SKILL.md` (25 skills — see SKILL-INVENTORY.md for triage)
> - `.claude/agents/{data-engineer,design-engineer,code-reviewer,security-reviewer,build-error-resolver,docs-specialist,devops-engineer,web-scraper,data-modeler,skill-optimizer,git-code-reviewer}.md` (11 project-owned agents)
>
> OUT of scope for failing-the-threshold: `gsd-*` agents (score them for transparency but flag them as FRAMEWORK-OWNED in the scorecard — their scores do not count toward the <3 threshold).
>
> Scorecard format:
>
> ```markdown
> # Skill & Agent Audit Scorecard — Phase 65
>
> **Audit date:** {date}
> **Evaluator:** skill-optimizer (Opus 4.6)
> **SHIP GATE:** <3 items scoring below 6 on ANY dimension
>
> ## Summary
> - Items audited: {N}
> - Items with >=1 dimension below 6: {M}
> - VERDICT: PASS ({M < 3}) / FAIL ({M >= 3})
>
> ## Scorecard
>
> | Item | Type | Owner | Clarity | Completeness | Accuracy | Specificity | Anti-patterns | Testability | Freshness | Integration | Min Score | Flag |
> |------|------|-------|---------|--------------|----------|-------------|---------------|-------------|-----------|-------------|-----------|------|
> | ingest | skill | nfl | 8 | 9 | 9 | 8 | 7 | 9 | 9 | 8 | 7 | - |
> | data-engineer | agent | nfl | 9 | 8 | 9 | 8 | 7 | 8 | 9 | 8 | 7 | - |
> | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |
>
> ## Failing Items (Min Score < 6)
>
> For each item with any dimension below 6:
> - **{item name}** — {which dimensions failed, specific issues, recommended fix}
>
> ## Recommendations
>
> Top 5 highest-impact improvements, prioritized by (usage × gap).
> ```
>
> Notes:
> - For "Freshness", verify all file paths/API signatures referenced in the skill still exist. Use Grep to check each reference.
> - For "Accuracy", spot-check one CLI command or code example per skill — does it still work?
> - For the 5 design-holistic skills, the newly-added "Invocation Routing (Phase 65 consolidation)" block should raise their Integration scores. Flag if it doesn't.
> - For project-owned agents, check that their "Owned Skills" lists match SKILL-INVENTORY.md.

After the skill-optimizer returns the scorecard, verify:

1. Scorecard file exists at the target path
2. Every item has 8 numeric scores (1-10)
3. "Min Score" column exists and equals the minimum of the 8 dimensions
4. Summary section correctly counts items with min_score < 6
5. VERDICT is either "PASS" (M < 3) or "FAIL" (M >= 3)
  </action>
  <verify>
    <automated>test -f .planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md && grep -qE "VERDICT.*(PASS|FAIL)" .planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md && grep -q "Clarity" .planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md && grep -q "Freshness" .planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md</automated>
  </verify>
  <done>Scorecard exists with all 8 dimensions per row, explicit VERDICT, and failing-items breakdown if any exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirmation of SHIP GATE</name>
  <what-built>
The skill-optimizer produced SKILL-AUDIT-SCORECARD.md scoring every project-owned skill and agent on 8 dimensions. The scorecard includes a VERDICT (PASS if fewer than 3 items score below 6 on any dimension; FAIL otherwise).
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/65-agent-ecosystem-optimization/SKILL-AUDIT-SCORECARD.md`
2. Read the Summary section — confirm the "Items with >=1 dimension below 6" count
3. Read the VERDICT line — confirm PASS or FAIL
4. Spot-check 3 rows you care about most (e.g., `ingest`, `data-engineer`, `impeccable`) — do the scores seem reasonable?
5. If FAIL (3+ items below 6): read the "Failing Items" section — are the issues legitimate or evaluator false-positives?

Expected outcome for this phase to complete:
- **PASS verdict** — phase 65 SC #4 (AGNT-04) is satisfied, phase is complete
- **FAIL verdict** — need a remediation sub-plan (65-05) to fix the failing items; phase 65 is NOT complete

What to look for in "reasonable" scores:
- Skills with recent ECC updates may legitimately score 9-10 (they're externally maintained)
- Project-owned skills should score 7+ because they were written specifically for this project
- Low Freshness scores are usually fixable (stale file references)
- Low Specificity scores indicate missing examples
  </how-to-verify>
  <resume-signal>
Type one of:
- "pass" — scorecard verdict is PASS, phase complete
- "fail, remediate" — scorecard verdict is FAIL, spawn gsd-planner to create plan 65-05 closing the gap
- "evaluator wrong on [item]" — re-run skill-optimizer with a correction prompt for the named item
  </resume-signal>
</task>

</tasks>

<verification>
- SKILL-AUDIT-SCORECARD.md exists and contains all 8 dimensions per row
- Summary section states the count of items with min_score < 6
- VERDICT is explicitly PASS or FAIL
- Human confirmation received at Task 2 checkpoint
- If PASS: phase SC #4 (AGNT-04) is marked complete
- If FAIL: a remediation plan is queued (65-05) BEFORE phase completion
</verification>

<success_criteria>
AGNT-04 satisfied: the skill optimizer audit completes with fewer than 3 items scoring below 6/10, OR a remediation plan is explicitly queued to close the gap before phase 65 is marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/65-agent-ecosystem-optimization/65-04-SUMMARY.md` with:
- Final verdict (PASS / FAIL)
- Count of items audited
- Count of items with any dimension below 6
- If FAIL: names of failing items and whether 65-05 remediation plan was spawned
- If PASS: note that phase 65 is complete
</output>
