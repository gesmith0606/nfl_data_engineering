---
phase: 65-agent-ecosystem-optimization
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - .claude/skills/impeccable/SKILL.md
  - .claude/skills/taste-skill/SKILL.md
  - .claude/skills/redesign-skill/SKILL.md
  - .claude/skills/soft-skill/SKILL.md
  - .claude/skills/emil-design-eng/SKILL.md
  - .claude/agents/design-engineer.md
  - .planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md
autonomous: false
requirements: [AGNT-01, AGNT-02]

must_haves:
  truths:
    - "The 5 overlapping design skills have a single clear invocation hierarchy — one primary, four specialized/aliased"
    - "The design-engineer agent's owned-skills list reflects the consolidated invocation pattern (no ambiguity about which of the 5 to call when)"
    - "A design-engineer invocation for a new page resolves to exactly ONE skill — no redundant triggering"
    - "Framework-owned SKILL.md files are minimally edited — a single 'Invocation Routing' block added at the top, not a rewrite"
    - "A human checkpoint confirms the consolidation hierarchy before any SKILL.md is edited"
  artifacts:
    - path: ".planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md"
      provides: "The consolidation decision — which skill is primary, how the other 4 defer/specialize, when each fires"
      contains: "primary_skill"
    - path: ".claude/skills/impeccable/SKILL.md"
      provides: "Primary design skill with routing block pointing at specialized siblings"
      contains: "Invocation Routing"
    - path: ".claude/agents/design-engineer.md"
      provides: "Updated agent with consolidated invocation guidance"
      contains: "Invocation Routing"
  key_links:
    - from: "DESIGN-CONSOLIDATION.md"
      to: ".claude/skills/{5-skills}/SKILL.md"
      via: "Invocation Routing block is identical across all 5 files"
      pattern: "Invocation Routing"
    - from: ".claude/agents/design-engineer.md"
      to: "DESIGN-CONSOLIDATION.md"
      via: "agent references the consolidation document as source of truth"
      pattern: "DESIGN-CONSOLIDATION"
---

<objective>
Consolidate the 5 overlapping DESIGN-HOLISTIC skills (identified in 65-01) into a single, non-redundant invocation hierarchy. One primary skill owns the "generate a premium interface" use case; the other 4 either specialize (e.g., redesigning existing code vs greenfield) or are aliased into the primary.

Purpose: Satisfies AGNT-01 directly. Today, a design task can plausibly trigger 2-3 of these 5 skills simultaneously, producing contradictory output (e.g., impeccable wants Geist, soft-skill bans it, taste-skill demands tabular-nums everywhere). A single routing block resolves the ambiguity.

Output: One DESIGN-CONSOLIDATION.md decision doc, an identical "Invocation Routing" block prepended to each of the 5 SKILL.md files, and an updated design-engineer agent definition that references the hierarchy.

CONSTRAINT: Framework-owned files are edited minimally. The Invocation Routing block is a single ~15-line section added at the top of each SKILL.md, not a rewrite. This survives future `git pull` updates from ~/repos/everything-claude-code/ because it is an additive block, not a modification to existing content.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md
@.planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md
@.claude/README.md
@.claude/agents/design-engineer.md

<interfaces>
<!-- The 5 design-holistic skills (confirmed by 65-01 Task 2). Each has a frontmatter block we must preserve. -->

.claude/skills/impeccable/SKILL.md
  - frontmatter: name=impeccable, version=2.1.1, user-invocable=true, argument-hint=[craft|teach|extract]
  - role: primary "create distinctive production-grade frontend" skill

.claude/skills/taste-skill/SKILL.md
  - frontmatter: name=design-taste-frontend
  - role: enforces metric-based rules (DESIGN_VARIANCE, MOTION_INTENSITY, VISUAL_DENSITY)

.claude/skills/redesign-skill/SKILL.md
  - frontmatter: name=redesign-existing-projects
  - role: upgrades existing websites (not greenfield) — clearest specialization

.claude/skills/soft-skill/SKILL.md
  - frontmatter: name=high-end-visual-design
  - role: Awwwards-tier archetype engine (vibe/texture/layout combos)

.claude/skills/emil-design-eng/SKILL.md
  - frontmatter: name=emil-design-eng
  - role: Emil Kowalski's design-engineering philosophy (craft sensibility, unseen details)
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm consolidation hierarchy (HUMAN DECISION)</name>
  <decision>Which of the 5 design-holistic skills is primary, and how do the other 4 relate to it?</decision>
  <context>
All 5 skills cover "generate a premium interface" with overlapping anti-pattern lists. They contradict each other (different banned fonts, different motion philosophies). We need one primary routing hierarchy before editing any file.

Based on 65-01 SKILL-INVENTORY.md analysis, the recommended hierarchy is:

**Primary:** `impeccable` — already the canonical entry (v2.1.1, has Context Gathering Protocol, is referenced by `polish` as mandatory prerequisite)

**Specializations:**
  - `redesign-skill` — fires when the user has existing code to upgrade (has the clearest use-case boundary: "existing project")
  - `emil-design-eng` — fires when the user explicitly asks about "craft", "taste", "Emil", or animation philosophy questions (advisory, not generative)

**Aliases (merged into impeccable invocation):**
  - `taste-skill` — its metric-based config (DESIGN_VARIANCE / MOTION_INTENSITY / VISUAL_DENSITY) becomes an optional config block inside impeccable
  - `soft-skill` — its Awwwards archetype selector becomes an optional "archetype" mode inside impeccable

Each skill file keeps its SKILL.md but gets an identical "Invocation Routing" block at the top that explicitly says "If [condition], use [other skill] instead."
  </context>
  <options>
    <option id="option-a">
      <name>Recommended hierarchy (impeccable primary, redesign-skill + emil specialized, taste + soft aliased)</name>
      <pros>Matches existing file structure (polish already depends on impeccable), clearest use-case boundaries, preserves all 5 files (no deletions, survives ECC upstream updates), only ~75 lines added total across 5 files</pros>
      <cons>Requires users to remember the routing table; aliased skills still fire standalone if user explicitly invokes them</cons>
    </option>
    <option id="option-b">
      <name>Archive 4, keep only impeccable</name>
      <pros>Zero ambiguity, smallest surface area</pros>
      <cons>Breaks git pull from ECC (deleted files come back); loses unique contributions (Emil's philosophy, Awwwards archetypes); aggressive</cons>
    </option>
    <option id="option-c">
      <name>Keep all 5 fully independent, add routing ONLY to design-engineer agent</name>
      <pros>Zero skill file edits</pros>
      <cons>Does not satisfy AGNT-01 — skills still fire redundantly when triggered by keywords; the overlap remains</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, or option-c. If option-a, also confirm the primary skill choice (default: impeccable).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write DESIGN-CONSOLIDATION.md and apply Invocation Routing to all 5 SKILL.md files</name>
  <files>.planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md, .claude/skills/impeccable/SKILL.md, .claude/skills/taste-skill/SKILL.md, .claude/skills/redesign-skill/SKILL.md, .claude/skills/soft-skill/SKILL.md, .claude/skills/emil-design-eng/SKILL.md</files>
  <action>
Based on the Task 1 decision (expected: option-a with impeccable primary):

1. Create `.planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md` containing:
   - The chosen hierarchy (primary / specializations / aliases)
   - Per-skill "when to fire" rule (one sentence each)
   - A routing table: trigger phrase → target skill
   - Example scenarios (e.g., "user says 'redesign the lineups page'" → redesign-skill; "user says 'build the draft tool page'" → impeccable)

2. Draft the Invocation Routing block (identical text for all 5 SKILL.md files). Template:

```markdown
## Invocation Routing (Phase 65 consolidation)

This skill is part of a 5-skill design cluster. Before acting, confirm this is the right entry point:

- **Greenfield UI (new page/component):** → use `/impeccable craft`
- **Upgrading existing code:** → use `/redesign-existing-projects`
- **Advisory question about taste/craft/animations:** → use `/emil-design-eng`
- **Inside impeccable, need metric-based config:** → this skill (taste-skill) supplies DESIGN_VARIANCE/MOTION_INTENSITY/VISUAL_DENSITY — do NOT invoke standalone
- **Inside impeccable, need archetype selection:** → this skill (soft-skill) supplies vibe/texture/layout archetypes — do NOT invoke standalone

Full decision table: `.planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md`
```

3. Prepend this block to each of the 5 SKILL.md files, placing it immediately after the YAML frontmatter closing `---` and before the next content. Use the Edit tool with `old_string` = the line immediately after `---` frontmatter close and `new_string` = routing block + that same line. Do NOT modify frontmatter. Do NOT alter any existing content below the routing block.

4. For `impeccable/SKILL.md`, note that it already has a `<post-update-cleanup>` section — place the Invocation Routing block AFTER that cleanup block (not before), since cleanup must run first.

5. Verify each file still parses as valid Markdown (frontmatter intact, no duplicate headings) by reading the first 40 lines of each.
  </action>
  <verify>
    <automated>for f in .claude/skills/impeccable/SKILL.md .claude/skills/taste-skill/SKILL.md .claude/skills/redesign-skill/SKILL.md .claude/skills/soft-skill/SKILL.md .claude/skills/emil-design-eng/SKILL.md; do grep -q "Invocation Routing (Phase 65 consolidation)" "$f" || { echo "MISSING: $f"; exit 1; }; done && test -f .planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md</automated>
  </verify>
  <done>All 5 SKILL.md files contain the "Invocation Routing (Phase 65 consolidation)" block; DESIGN-CONSOLIDATION.md exists with the full decision table; `git diff` on each SKILL.md shows ONLY the routing block added (no content below it altered).</done>
</task>

<task type="auto">
  <name>Task 3: Update design-engineer agent to reference the consolidation</name>
  <files>.claude/agents/design-engineer.md</files>
  <action>
Edit `.claude/agents/design-engineer.md` to add a short "Owned Skills — Invocation Routing" section that tells the agent which of the 5 skills to invoke based on user intent.

1. Read the current file to find where the "Owned Skills" or equivalent section lives.
2. After the owned-skills list, add this section:

```markdown
## Owned Skills — Invocation Routing

The 5 design-holistic skills (impeccable, taste-skill, redesign-skill, soft-skill, emil-design-eng) are consolidated under a single invocation hierarchy (Phase 65 cleanup). Before invoking, resolve to exactly ONE primary skill:

- **New page/component from scratch:** `impeccable` (primary)
- **Upgrading existing code:** `redesign-existing-projects`
- **Advisory taste/craft question:** `emil-design-eng`
- **taste-skill and soft-skill:** invoked only AS CONFIG INSIDE `impeccable` — never standalone

The 9 DESIGN-TARGETED skills (polish, animate, layout, colorize, typeset, bolder, audit, critique, minimalist-skill) fire standalone as before — they are not part of the consolidation cluster.

Decision source: `.planning/phases/65-agent-ecosystem-optimization/DESIGN-CONSOLIDATION.md`
```

3. Do NOT touch any other section of the agent definition.
  </action>
  <verify>
    <automated>grep -q "Owned Skills — Invocation Routing" .claude/agents/design-engineer.md && grep -q "DESIGN-CONSOLIDATION" .claude/agents/design-engineer.md</automated>
  </verify>
  <done>design-engineer.md contains the routing section referencing DESIGN-CONSOLIDATION.md; no other sections of the file were modified.</done>
</task>

</tasks>

<verification>
- Five SKILL.md files contain the Invocation Routing block (exact string match)
- design-engineer.md contains the routing section
- DESIGN-CONSOLIDATION.md exists and is referenced from all 6 edited files
- `git diff --stat` shows changes ONLY in: 5 SKILL.md files + 1 agent file + 1 new .planning/ doc
- No content deleted; all edits are additive (grep for phrase from pre-edit frontmatter still succeeds)
</verification>

<success_criteria>
After this plan ships: a user saying "redesign the lineups page" triggers redesign-skill alone (not redesign-skill + impeccable + taste-skill fighting each other). A user saying "build me a new component" triggers impeccable alone. The design-engineer agent has a clear decision tree and will not double-invoke.
</success_criteria>

<output>
After completion, create `.planning/phases/65-agent-ecosystem-optimization/65-02-SUMMARY.md` with:
- Which hierarchy option was selected at the checkpoint
- Files modified (exact list)
- Lines added per file (confirm minimal edits)
- Before/after: example invocation that previously triggered 3 skills, now triggers 1
</output>
