---
phase: 65-agent-ecosystem-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md
  - .planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md
autonomous: true
requirements: [AGNT-02]

must_haves:
  truths:
    - "Every agent in .claude/agents/ has a triage tag (ACTIVE / DORMANT / REDUNDANT / FRAMEWORK-OWNED)"
    - "Every dormant agent has an archive-or-activate recommendation with one-sentence reasoning"
    - "Every skill in .claude/skills/ has a triage tag (OWNED / FRAMEWORK-OWNED / OVERLAPPING)"
    - "The 5 most-overlapping design skills are explicitly identified for consolidation in plan 65-02"
    - "Inventory is a durable reference doc for downstream plans 65-02, 65-03, 65-04"
  artifacts:
    - path: ".planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md"
      provides: "Full agent triage table with active/dormant/redundant tags and recommendations"
      contains: "triage_status"
    - path: ".planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md"
      provides: "Full skill triage table; identifies the 5 overlapping design skills"
      contains: "overlap_group"
  key_links:
    - from: "AGENT-INVENTORY.md"
      to: "CLAUDE.md agent-skill table"
      via: "cross-reference of active agents"
      pattern: "ACTIVE.*CLAUDE.md"
    - from: "SKILL-INVENTORY.md"
      to: "65-02-PLAN.md"
      via: "5 overlapping design skills identified here feed consolidation plan"
      pattern: "overlap_group.*design-holistic"
---

<objective>
Produce two inventory documents — AGENT-INVENTORY.md and SKILL-INVENTORY.md — that triage every file in .claude/agents/ and .claude/skills/ into actionable categories.

Purpose: Downstream plans (65-02 consolidation, 65-03 rules, 65-04 audit) all require a ground-truth map of what exists, what is used, and what overlaps. Without this inventory, consolidation and audit decisions are guesses.

Output: Two Markdown tables saved in the phase directory. No agent or skill files are modified in this plan; it is read-only analysis.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.claude/README.md
@.claude/rules/agents.md

<!-- Source-of-truth agent-skill map: CLAUDE.md "Agent-Skill Architecture" table -->
<!-- Active agents per CLAUDE.md: data-engineer, design-engineer, code-reviewer, security-reviewer, build-error-resolver, docs-specialist, devops-engineer, web-scraper, data-modeler, skill-optimizer, git-code-reviewer (11 total) -->
<!-- All 30 gsd-* agents are framework-owned (updated via `npx get-shit-done-cc@latest`) per .claude/README.md — treat as FRAMEWORK-OWNED, not dormant -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AGENT-INVENTORY.md with triage tags</name>
  <files>.planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md</files>
  <action>
Enumerate every file under .claude/agents/ (41 files total: 11 project-owned + 30 gsd-*). For each agent:

1. Read the frontmatter (name, description, model) via Read tool — do NOT read the full body unless description is missing.
2. Assign exactly one triage tag:
   - `ACTIVE` — listed in CLAUDE.md "Agent-Skill Architecture" table (the 11 agents: data-engineer, design-engineer, code-reviewer, security-reviewer, build-error-resolver, docs-specialist, devops-engineer, web-scraper, data-modeler, skill-optimizer, git-code-reviewer)
   - `FRAMEWORK-OWNED` — filename starts with `gsd-` (managed by GSD framework updates per .claude/README.md; cannot be archived locally)
   - `DORMANT` — project-owned (not gsd-*) but NOT in CLAUDE.md active table
   - `REDUNDANT` — project-owned and functionally duplicates another agent (e.g., same owned skills, same description pattern)

3. For each DORMANT or REDUNDANT agent, write a one-sentence `recommendation` column: either "ACTIVATE: add to CLAUDE.md with owned skills [X, Y]" or "ARCHIVE: reason — [specific reason]".

4. For FRAMEWORK-OWNED agents, recommendation is literally "Framework-owned — no local action" (skill-optimizer audit in 65-04 still evaluates them for quality but does not archive).

Output format — save as a single Markdown file with this exact structure:

```markdown
# Agent Inventory — Phase 65

**Generated:** {date}
**Source of truth:** CLAUDE.md "Agent-Skill Architecture" table

## Summary
- ACTIVE: {N}
- FRAMEWORK-OWNED: {N}
- DORMANT: {N}
- REDUNDANT: {N}
- TOTAL: 41

## Triage Table

| Agent | Status | Default Model | Owned Skills | Recommendation |
|-------|--------|---------------|--------------|----------------|
| data-engineer | ACTIVE | Sonnet | ingest, validate-data, weekly-pipeline, backtest, model-training, prediction-pipeline | Keep as-is |
| ... | ... | ... | ... | ... |

## Dormant Agent Details
{For each DORMANT agent, a paragraph explaining why it appears unused and what evidence supports activate-vs-archive.}

## Redundant Agent Details
{For each REDUNDANT agent, which agent it duplicates and which should be kept.}
```

Do NOT modify any file in .claude/agents/. This plan is read-only analysis.
  </action>
  <verify>
    <automated>test -f .planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md && grep -c "^|" .planning/phases/65-agent-ecosystem-optimization/AGENT-INVENTORY.md | awk '$1 >= 42 {exit 0} {exit 1}'</automated>
  </verify>
  <done>AGENT-INVENTORY.md exists, contains a triage table with 41 rows + header + separator (>=42 pipe-prefixed lines), summary counts sum to 41, and every DORMANT/REDUNDANT row has a non-empty Recommendation column.</done>
</task>

<task type="auto">
  <name>Task 2: Build SKILL-INVENTORY.md and identify 5 overlapping design skills</name>
  <files>.planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md</files>
  <action>
Enumerate every directory under .claude/skills/ (25 skills total). For each skill:

1. Read the SKILL.md frontmatter (name, description) and the first ~30 lines to understand scope. Do NOT read the full body.
2. Assign exactly one category tag:
   - `DATA-OWNED` — project-owned data/test skills (ingest, validate-data, weekly-pipeline, backtest, model-training, prediction-pipeline, test, draft-prep)
   - `DESIGN-HOLISTIC` — design skills that generate full interfaces/pages with heavy anti-pattern rules (candidates for consolidation)
   - `DESIGN-TARGETED` — design skills with a single narrow verb (polish, animate, layout, colorize, typeset, bolder, audit, critique, minimalist-skill — these stay)
   - `DOC-SPECIALIST` — notebooklm, fireworks-tech-graph (docs-specialist agent skills)
   - `FRAMEWORK` — skill-creator (owned by skill-optimizer, kept as-is)

3. Within DESIGN-HOLISTIC, identify the 5 skills with the highest mutual overlap. Based on the frontmatter descriptions, the expected 5 are: `impeccable`, `taste-skill`, `redesign-skill`, `soft-skill`, `emil-design-eng`. Confirm by extracting each one's "core directive" sentence and comparing. If the actual overlap cluster differs from this hypothesis, record WHICH 5 were selected and WHY (at least one concrete overlap dimension per pair, e.g., "both generate full pages with anti-AI-slop rules", "both enforce banned-fonts lists").

4. Add an `overlap_group` column that is one of: `design-holistic-cluster` (for the 5), `standalone` (for the 9 targeted design skills + other categories), or `owned-by-nfl` (for DATA-OWNED).

Output format:

```markdown
# Skill Inventory — Phase 65

**Generated:** {date}

## Summary
- DATA-OWNED: 8
- DESIGN-HOLISTIC: 5 (consolidation target)
- DESIGN-TARGETED: 9
- DOC-SPECIALIST: 2
- FRAMEWORK: 1
- TOTAL: 25

## Triage Table

| Skill | Category | Overlap Group | Description (one line) |
|-------|----------|---------------|-------------------------|
| ingest | DATA-OWNED | owned-by-nfl | Bronze ingestion for NFL data types |
| impeccable | DESIGN-HOLISTIC | design-holistic-cluster | ... |
| ... | ... | ... | ... |

## Design Holistic Cluster — Overlap Analysis

For the 5 DESIGN-HOLISTIC skills, document:
- Shared concerns: {e.g., "All 5 produce full-page/component code", "All 5 enforce anti-generic-font lists", ...}
- Unique contributions each brings: {one line per skill}
- Consolidation recommendation preview: {e.g., "Merge into one `impeccable` umbrella with mode flags, OR keep `impeccable` as primary and alias the other 4 to invoke it"}
```

Do NOT modify any file in .claude/skills/. This plan is read-only analysis.
  </action>
  <verify>
    <automated>test -f .planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md && grep -c "design-holistic-cluster" .planning/phases/65-agent-ecosystem-optimization/SKILL-INVENTORY.md | awk '$1 >= 5 {exit 0} {exit 1}'</automated>
  </verify>
  <done>SKILL-INVENTORY.md exists, all 25 skills appear in the triage table, exactly 5 rows are tagged `design-holistic-cluster`, and the "Design Holistic Cluster — Overlap Analysis" section names those 5 skills with pairwise overlap evidence.</done>
</task>

</tasks>

<verification>
- Both inventory files exist in .planning/phases/65-agent-ecosystem-optimization/
- AGENT-INVENTORY.md covers all 41 agents; counts sum to 41
- SKILL-INVENTORY.md covers all 25 skills; exactly 5 are identified as the overlap cluster
- No files in .claude/agents/ or .claude/skills/ were modified (verify via `git status`)
- Every DORMANT/REDUNDANT agent has a non-empty recommendation
</verification>

<success_criteria>
Plan 65-02 (design consolidation) can open SKILL-INVENTORY.md and see exactly which 5 skills to consolidate without re-analyzing. Plan 65-04 (skill-optimizer audit) can open AGENT-INVENTORY.md to know which agents are in scope for scoring (exclude framework-owned from archive decisions, include them in quality scoring).
</success_criteria>

<output>
After completion, create `.planning/phases/65-agent-ecosystem-optimization/65-01-SUMMARY.md` with:
- Counts from both inventories
- The 5 design-holistic skills identified (feeds 65-02)
- List of DORMANT/REDUNDANT agents flagged for 65-02/65-04 consideration
- Any surprises (e.g., if the 5 overlapping skills are NOT the expected set)
</output>
