---
phase: 63-ai-advisor-hardening
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - web/api/routers/projections.py
  - web/api/routers/lineups.py
  - web/api/routers/news.py
  - web/api/routers/draft.py
  - web/api/services/projection_service.py
  - web/api/services/news_service.py
  - web/api/services/external_rankings_service.py
autonomous: true
requirements: [ADVR-01]
tags: [advisor, backend, fastapi, fixes]
must_haves:
  truths:
    - "Every advisor tool marked FAIL in TOOL-AUDIT.md returns a non-FAIL payload when re-probed locally against the fixed code (use uvicorn on port 8000)"
    - "Every tool marked WARN due to empty payload now returns a documented reason (preseason-empty is fine; silent-empty is fixed)"
    - "Fixes do NOT introduce new required environment variables on Railway without being recorded in user_setup"
    - "Schema contracts visible in web/frontend/src/app/api/chat/route.ts remain backward-compatible (same keys, same types) after fixes"
  artifacts:
    - path: "web/api/routers/projections.py"
      provides: "Stable projections endpoint with position/limit filters that never returns {} — returns {projections: []} with HTTP 200 for empty preseason slices"
    - path: "web/api/routers/news.py"
      provides: "news/feed, news/team-sentiment, news/summary endpoints that never 500 when Parquet fallback has no sentiment data"
    - path: "web/api/routers/lineups.py"
      provides: "lineups endpoint that returns {lineup: []} for unknown team rather than 500"
  key_links:
    - from: "web/api/routers/*.py"
      to: "web/api/services/*.py"
      via: "Service-layer calls with DuckDB / Parquet fallback"
      pattern: "def get_.*\\(.*\\)"
    - from: "fixed endpoints"
      to: "web/frontend/src/app/api/chat/route.ts tool schema"
      via: "JSON response keys (projections, lineup, predictions, board, players, ...)"
      pattern: "return.*\\{.*\\}"
---

<objective>
Fix every advisor tool marked FAIL in `TOOL-AUDIT.md` (produced by plan 63-01), along with any WARN cases where the root cause is a backend bug rather than legitimately absent data (e.g. preseason week 1 has no game predictions — that's fine; but HTTP 500 from `/api/news/team-sentiment` is not).

Purpose: The live-site audit in wave 3 (plan 63-06) is the SHIP gate for ADVR-01. Every FAIL surfaced in 63-01 has to either become PASS/WARN-preseason or be explicitly documented as out-of-scope-for-v6.0 in the re-audit.

Output: Backend-only fixes to FastAPI routers and services. No frontend changes here (frontend changes live in plan 63-04 for conversation persistence and plan 63-05 for widget reach).
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT.md
@.planning/phases/63-ai-advisor-hardening/63-01-SUMMARY.md

# The tool contracts (what frontend/AI expects back)
@web/frontend/src/app/api/chat/route.ts

# Backend components to fix
@web/api/main.py
@web/api/routers/projections.py
@web/api/routers/lineups.py
@web/api/routers/news.py
@web/api/routers/draft.py
@web/api/services/projection_service.py
@web/api/services/news_service.py

<interfaces>
<!-- Response shape contracts that the advisor frontend relies on.
     Any fix MUST preserve these keys and types exactly. -->

GET /api/projections → { "projections": [ { player_name, player_id, team, position,
  projected_points, projected_floor, projected_ceiling, injury_status } ] }

GET /api/predictions  → { "predictions": [ { game_id, home_team, away_team, predicted_spread,
  predicted_total, vegas_spread, vegas_total, edge_spread, edge_total, confidence_tier,
  ats_pick, ou_pick } ] }

GET /api/players/search → [ { player_id, player_name, team, position } ]

GET /api/lineups → { "lineup": [ { player_name, player_id, team, position, projected_points,
  projected_floor, projected_ceiling, injury_status, is_starter } ] }

GET /api/news/feed → [ { doc_id, title, source, url, published_at, sentiment, category,
  player_id, player_name, team, body_snippet, is_ruled_out, is_inactive, is_questionable,
  is_suspended, is_returning } ]

GET /api/news/team-sentiment → [ { team, sentiment_score, article_count,
  bullish_count, bearish_count } ]

GET /api/news/summary → { total_articles, sources, bullish_players, bearish_players,
  average_sentiment }

GET /api/draft/board → { "board": [ { player_name, player_id, team, position, adp,
  projected_points, vorp, value_tier, bye_week } ] }

GET /api/rankings/compare → { source, scoring, position_filter, players: [ { player_name,
  position, team, external_rank, our_rank, rank_diff, our_projected_points } ] }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden routers against FAIL modes from TOOL-AUDIT.md</name>
  <files>web/api/routers/projections.py, web/api/routers/lineups.py, web/api/routers/news.py, web/api/routers/draft.py, web/api/routers/predictions.py</files>
  <behavior>
    - Every router that TOOL-AUDIT.md flagged as FAIL now returns a valid envelope (HTTP 200 with empty list) instead of 404/500 when the underlying Parquet slice is missing.
    - `GET /api/projections` with a known season/week returns `{ "projections": [...] }` and NEVER `{}` or `null`.
    - `GET /api/lineups?team=XYZ` for an unknown team returns `{ "lineup": [] }` with 200 (not 500).
    - `GET /api/news/team-sentiment?season=2026&week=1` returns `[]` (not 500) when no sentiment is available in Parquet fallback mode.
    - `GET /api/news/summary` returns `{ total_articles: 0, sources: [], bullish_players: [], bearish_players: [], average_sentiment: 0 }` (not 500) when no articles exist.
    - `GET /api/draft/board` with default params returns `{ "board": [ … ] }` with len >= 50 in 2026 preseason data.
    - `GET /api/predictions?season=2026&week=1` returns `{ "predictions": [] }` when the week has no games yet (preseason/offseason).
    - Every endpoint listed in the interfaces block preserves its response-key contract exactly — tests fail if a renamed key slips in.
  </behavior>
  <action>
    1. Open `TOOL-AUDIT.md` and list every row with Status=FAIL along with its reason category. For each, locate the responsible router/service and apply the minimum fix.
    2. Apply the following patterns (only where a TOOL-AUDIT row flags that tool):
       - SCHEMA_MISMATCH / UNKNOWN: check the envelope keys in the router's return statement against the interfaces block above. Fix renamed or missing keys. Do NOT touch keys the frontend already consumes.
       - HTTP_ERROR 500 on empty data: wrap the service call in a try/except that logs the error and returns the empty envelope rather than raising. Use `logging.getLogger(__name__).warning(...)` (per .claude/rules/hooks.md — no print).
       - HTTP_ERROR 404: if the route path is wrong, correct it; otherwise ensure the route matches what the frontend sends.
       - EXTERNAL_SOURCE_DOWN (rankings/compare): in `external_rankings_service.py` ensure the cached JSON fallback in `data/external/*.json` is used when the live Sleeper/FantasyPros/ESPN fetch fails (already the stated strategy — verify and repair).
    3. Write unit tests under `tests/web/` (create directory if missing) using FastAPI's TestClient. Required cases (write BEFORE editing the routers — tests must fail first, then pass):
       - `test_projections_empty_week_returns_envelope`: GET /api/projections?season=2026&week=99 → 200, body has key `projections` which is a list (may be empty).
       - `test_lineups_unknown_team_returns_empty`: GET /api/lineups?team=ZZZ&season=2026&week=1 → 200, body == {"lineup": []}.
       - `test_team_sentiment_no_data_returns_empty`: GET /api/news/team-sentiment?season=2026&week=99 → 200, body is a list.
       - `test_news_summary_no_data_returns_zeros`: GET /api/news/summary?season=2026&week=99 → 200, body has keys total_articles (int), bullish_players (list), bearish_players (list).
       - `test_draft_board_has_at_least_50`: GET /api/draft/board?scoring=half_ppr → 200, len(body['board']) >= 50.
       - `test_predictions_preseason_returns_envelope`: GET /api/predictions?season=2026&week=1 → 200, body has key `predictions` (list, may be empty).
    4. For any FAIL in TOOL-AUDIT that is NOT covered by the six patterns above, add a line to the plan's SUMMARY noting the bug + its targeted fix location — then fix it following the same minimum-change principle.
    5. Do NOT introduce new response envelopes. Do NOT rename fields. Do NOT alter authentication. Do NOT touch the frontend.
    6. Follow `.claude/rules/coding-style.md` (black, type hints). Follow `.claude/rules/testing.md` (pytest).
  </action>
  <verify>
    <automated>python -m pytest tests/web/ -x -q --tb=short</automated>
  </verify>
  <done>
    All six required unit tests pass. Running `python scripts/audit_advisor_tools.py` against a local uvicorn (`uvicorn web.api.main:app --port 8000`) shows every tool that was FAIL in TOOL-AUDIT.md is now at worst WARN (preseason empty). No response-key contract from the interfaces block has been changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-audit locally + capture deltas</name>
  <files>.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-LOCAL.md</files>
  <action>
    1. Start uvicorn locally: `uvicorn web.api.main:app --host 0.0.0.0 --port 8000 &` (background, capture PID).
    2. Wait up to 10s for `GET /api/health` to respond 200. If it doesn't, kill and report.
    3. Run the audit script from plan 63-01 against the local server: `RAILWAY_API_URL=http://localhost:8000 python scripts/audit_advisor_tools.py`.
    4. Rename/copy the output to `.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-LOCAL.md` (do not overwrite the live-site baseline).
    5. Kill the uvicorn process.
    6. Append a `## Delta vs Baseline` section to TOOL-AUDIT-LOCAL.md that lists every tool whose status improved (e.g. FAIL → PASS) or regressed.
    7. If any tool is still FAIL and is NOT EXTERNAL_SOURCE_DOWN, stop and report — plan 63-02 is not complete. EXTERNAL_SOURCE_DOWN is handled by plan 63-03.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-LOCAL.md && grep -q "Delta vs Baseline" .planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-LOCAL.md && echo OK</automated>
  </verify>
  <done>
    TOOL-AUDIT-LOCAL.md exists, contains the 12-tool table, has a "## Delta vs Baseline" section, and every non-EXTERNAL_SOURCE_DOWN FAIL from the baseline is resolved locally. Any remaining FAIL is EXTERNAL_SOURCE_DOWN (deferred to plan 63-03) or documented as out-of-scope with rationale.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/web/ -x` passes
- Local uvicorn audit shows no non-EXTERNAL_SOURCE_DOWN FAILs
- Response-key contracts in the interfaces block are unchanged (diff the frontend tool definitions vs actual backend responses)
</verification>

<success_criteria>
Every tool that was FAIL in the baseline TOOL-AUDIT.md due to a backend bug now returns a valid envelope (possibly empty list) against a local uvicorn. Rankings-specific external-source failures are explicitly deferred to plan 63-03. The frontend contract is unchanged — no breaking schema changes.
</success_criteria>

<output>
After completion, create `.planning/phases/63-ai-advisor-hardening/63-02-SUMMARY.md` listing every router/service touched and every test added, plus the delta table from TOOL-AUDIT-LOCAL.md.
</output>
