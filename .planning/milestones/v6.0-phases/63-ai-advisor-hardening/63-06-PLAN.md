---
phase: 63-ai-advisor-hardening
plan: 06
type: execute
wave: 3
depends_on: [63-02, 63-03, 63-04, 63-05]
files_modified:
  - .planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md
  - .planning/phases/63-ai-advisor-hardening/ADVISOR-E2E.md
  - scripts/audit_advisor_tools.py
autonomous: false
requirements: [ADVR-01, ADVR-02, ADVR-03, ADVR-04]
tags: [advisor, verification, ship-gate, live-audit]
must_haves:
  truths:
    - "Running scripts/audit_advisor_tools.py against the LIVE Railway backend (post-deploy) shows every one of the 12 tools as PASS or WARN-preseason — zero FAIL"
    - "A deployed-on-Vercel smoke test confirms the floating widget renders on at least 7 distinct dashboard pages and conversation persists across navigation"
    - "An AI-in-the-loop transcript confirms that 'who are the top 10 RBs' returns 10 real RB names with projected points traceable to the Gold layer"
    - "External rankings comparison is either PASS (live source reachable) or WARN-stale (cache-backed) for sleeper, fantasypros, espn — never FAIL"
    - "The final audit, transcript, and SHIP verdict are archived under .planning/phases/63-ai-advisor-hardening/"
  artifacts:
    - path: ".planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md"
      provides: "Re-run of the wave-1 audit after all fixes, against the live Railway backend — 12/12 PASS or WARN-preseason"
    - path: ".planning/phases/63-ai-advisor-hardening/ADVISOR-E2E.md"
      provides: "End-to-end advisor transcript showing each SC (ADVR-01..04) verified on the live site"
  key_links:
    - from: "scripts/audit_advisor_tools.py"
      to: "TOOL-AUDIT-FINAL.md"
      via: "same probe code as wave 1"
      pattern: "write_audit_markdown"
    - from: "ADVISOR-E2E.md"
      to: "frontend-jet-seven-33.vercel.app/dashboard/*"
      via: "human-in-the-loop screenshot or copy-paste of transcripts"
      pattern: "https://frontend-jet-seven-33.vercel.app"
---

<objective>
Re-audit the advisor on the LIVE production stack (Vercel frontend + Railway backend) after all wave-2 fixes have shipped. This is the SHIP gate for phase 63 — all four ADVR-* requirements are verified against production, not localhost.

Purpose: TOOL-AUDIT.md was a baseline; TOOL-AUDIT-LOCAL.md was a local-dev check. Neither proves the fix works on the live site. Real users hit the Vercel→Railway path, so that's where we verify.

Output: A post-deploy audit report, an end-to-end advisor transcript, and a SHIP/NO-SHIP verdict for phase 63.

**UI hint: YES** — this plan requires a human to interact with the live frontend.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT.md
@.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-LOCAL.md

# Source of 12 tools (final check they're all still wired)
@web/frontend/src/app/api/chat/route.ts

<interfaces>
<!-- The SHIP gate for each SC -->

| SC     | Verification source                                                 | Pass criterion                                      |
|--------|---------------------------------------------------------------------|-----------------------------------------------------|
| ADVR-01 | TOOL-AUDIT-FINAL.md against live Railway                            | All 12 tools PASS or WARN-preseason                 |
| ADVR-02 | ADVISOR-E2E.md "top 10 RBs" transcript                              | 10 real RBs, projected_points populated, data_as_of cited |
| ADVR-03 | ADVISOR-E2E.md "compare my rankings to Sleeper"                     | non-empty compare result with rank_diff values       |
| ADVR-04 | ADVISOR-E2E.md widget-nav screenshot checklist                      | widget visible on ≥7 dashboard pages, state persists |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run audit against LIVE Railway backend</name>
  <files>.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md</files>
  <action>
    1. Confirm deployment status before auditing:
       - Check Railway: `curl -sS https://nfldataengineering-production.up.railway.app/api/health` → expect 200.
       - Check Vercel: `curl -sSI https://frontend-jet-seven-33.vercel.app/dashboard/advisor` → expect 200 or 307.
       - If either is not live, STOP and surface to the user — do not run the audit against a stale deploy.
    2. Run the probe script against live Railway:
         RAILWAY_API_URL=https://nfldataengineering-production.up.railway.app \
         RAILWAY_API_KEY=<from env / Railway env> \
         python scripts/audit_advisor_tools.py
    3. Copy the resulting TOOL-AUDIT.md output to `.planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md` (distinct file so we keep the baseline).
    4. Append a `## Verdict` section:
       - PASS if 0 FAIL and WARNs are documented (preseason-empty or cache-stale).
       - HOLD if any FAIL remains — list them and link to the owning plan (63-02/03/04).
    5. Append a `## Delta vs Baseline` section diffing against `TOOL-AUDIT.md`, one row per tool.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md && grep -q "^## Verdict" .planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md && grep -q "^## Delta vs Baseline" .planning/phases/63-ai-advisor-hardening/TOOL-AUDIT-FINAL.md && echo OK</automated>
  </verify>
  <done>
    TOOL-AUDIT-FINAL.md exists with a `## Verdict` section (PASS or HOLD) and a `## Delta vs Baseline` section. Zero FAILs means phase 63 can ship; otherwise the failing plan is re-opened.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human E2E verification of all four ADVR requirements on live site</name>
  <what-built>
    The advisor has been re-audited against live Railway (Task 1). Now verify the user-facing experience end-to-end on the live Vercel frontend, covering ADVR-01..04.
  </what-built>
  <how-to-verify>
    Open a fresh browser tab (clear localStorage first: Application → Local Storage → delete `advisor:conversation:v1`).

    **ADVR-04 — Widget reach + persistence:**
    1. Visit https://frontend-jet-seven-33.vercel.app/dashboard/projections. Confirm the floating robot button is visible bottom-right.
    2. Open the widget. Send "who are the top 10 RBs".
    3. After the response renders, navigate via the sidebar to each of:
       /dashboard/predictions, /dashboard/news, /dashboard/draft, /dashboard/players,
       /dashboard/lineups, /dashboard/matchups, /dashboard/rankings, /dashboard/accuracy, /dashboard/advisor
       Confirm at each route (a) widget is visible, (b) when re-opened it still shows the top-10-RB response.
    4. On /dashboard/advisor, confirm the same conversation is visible in the full-page view.
    5. Hard-refresh the tab (Cmd+Shift+R). Re-open widget — messages should be restored.
    6. Click the "Clear" button. Confirm messages empty and localStorage key is removed.

    **ADVR-02 — Top-10 query with real data:**
    7. Ask "who are the top 10 RBs this season". Verify:
       - 10 distinct player names, all RB.
       - `projected_points` values rendered in the ProjectionCard.
       - The advisor's text response cites `data_as_of` or the week the data is from.
       - Cross-check: first name should match the top RB shown on /dashboard/projections sorted by projected_points descending for RB.

    **ADVR-03 — External rankings comparison:**
    8. Ask "compare our RB rankings to Sleeper". Verify:
       - Response mentions Sleeper as the source.
       - Lists at least 5 players with our rank, Sleeper rank, and the diff.
       - If any source returned stale data, the advisor mentions that.
    9. Ask "compare our WR rankings to FantasyPros". Verify the same for FantasyPros (stale-cache response is acceptable and should be mentioned).

    **ADVR-01 — All 12 tools valid:**
    10. Reference TOOL-AUDIT-FINAL.md (from Task 1). Confirm PASS/WARN only, no FAIL.

    Capture all transcripts in `.planning/phases/63-ai-advisor-hardening/ADVISOR-E2E.md` with a section per ADVR. Screenshots optional but recommended.
  </how-to-verify>
  <resume-signal>Type "approved — phase 63 ships" if all four ADVR checks pass, or describe which one failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write ADVISOR-E2E.md verdict + close phase</name>
  <files>.planning/phases/63-ai-advisor-hardening/ADVISOR-E2E.md</files>
  <action>
    1. After Task 2 is approved, create `ADVISOR-E2E.md` with the following structure:
       - Run timestamp + frontend + backend URLs tested.
       - Section "ADVR-01 — All 12 tools valid" → link to TOOL-AUDIT-FINAL.md verdict.
       - Section "ADVR-02 — Top 10 RBs" → pasted advisor transcript.
       - Section "ADVR-03 — External rankings compare" → pasted advisor transcript (include both Sleeper and FantasyPros queries).
       - Section "ADVR-04 — Widget reach + persistence" → bulleted checklist of the 11 verification steps from Task 2 with PASS/FAIL each.
       - Section "Final verdict: SHIP / HOLD" — SHIP only if every check passed.
    2. Do NOT modify ROADMAP.md or REQUIREMENTS.md in this plan — the orchestrator updates traceability after phase close.
  </action>
  <verify>
    <automated>test -f .planning/phases/63-ai-advisor-hardening/ADVISOR-E2E.md && grep -q "Final verdict" .planning/phases/63-ai-advisor-hardening/ADVISOR-E2E.md && echo OK</automated>
  </verify>
  <done>
    ADVISOR-E2E.md exists with all four ADVR sections + a Final verdict. If verdict is SHIP, phase 63 is complete. If HOLD, the failing check is documented with a pointer to the owning plan (63-02..05) for gap-closure planning.
  </done>
</task>

</tasks>

<verification>
- TOOL-AUDIT-FINAL.md exists, has 0 FAILs against live Railway
- ADVISOR-E2E.md exists with all four ADVR sections + Final verdict
- Human checkpoint passed
</verification>

<success_criteria>
All four ADVR-* requirements are PASS on the live production stack (Vercel + Railway). Phase 63 can be marked complete in ROADMAP.md and REQUIREMENTS.md by the verify-work orchestrator.
</success_criteria>

<output>
After completion, create `.planning/phases/63-ai-advisor-hardening/63-06-SUMMARY.md` with the SHIP/HOLD verdict and links to TOOL-AUDIT-FINAL.md + ADVISOR-E2E.md.
</output>
