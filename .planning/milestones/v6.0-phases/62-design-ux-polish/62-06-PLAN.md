---
phase: 62-design-ux-polish
plan: 06
type: execute
wave: 4
depends_on:
  - 62-03
  - 62-04
  - 62-05
files_modified:
  - web/frontend/src/app/dashboard/page.tsx
  - web/frontend/src/app/dashboard/accuracy/page.tsx
  - web/frontend/src/app/dashboard/projections/page.tsx
  - web/frontend/src/app/dashboard/rankings/page.tsx
  - web/frontend/src/app/dashboard/predictions/page.tsx
  - web/frontend/src/features/nfl/components/stat-cards.tsx
  - web/frontend/src/features/nfl/components/prediction-cards.tsx
  - .planning/phases/62-design-ux-polish/AUDIT-FINAL.md
autonomous: false
requirements:
  - DSGN-01
  - DSGN-03

must_haves:
  truths:
    - "Motion primitives from 62-04 are applied to pages 1-5 (page entrance, card hover, list stagger)"
    - "Prediction edge badges have a subtle reveal/shimmer when rendered above the edge threshold"
    - "A re-run of the design audit scores every one of the 11 pages at > 7/10 overall"
    - "Any page still below 7 has an explicit waiver or remediation task before SHIP"
  artifacts:
    - path: ".planning/phases/62-design-ux-polish/AUDIT-FINAL.md"
      provides: "Post-polish audit with per-page scores, side-by-side with AUDIT-BASELINE"
      contains: "11 page sections, numeric scores, baseline→final delta"
  key_links:
    - from: "AUDIT-FINAL.md"
      to: ".planning/phases/62-design-ux-polish/AUDIT-BASELINE.md"
      via: "per-page delta table"
      pattern: "baseline.*final|delta"
---

<objective>
Close the loop. Apply motion primitives to the pages 62-04 deliberately skipped (pages 1-5, owned by 62-03 during token normalization), then re-run the design audit across all 11 pages and confirm every page scores above 7/10. This is the SHIP gate for DSGN-01 and the final piece of DSGN-03.

Purpose: The phase goal says "The website looks and feels like a premium product on any device." DSGN-01 requires audit > 7/10 on all pages. This plan is the measurement + final polish iteration that proves we got there.
Output: Motion applied to pages 1-5; final audit document (AUDIT-FINAL.md) with scores and baseline delta; a human-verify checkpoint confirming the site feels right.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-design-ux-polish/AUDIT-BASELINE.md
@.planning/phases/62-design-ux-polish/MOBILE-AUDIT.md
@.planning/phases/62-design-ux-polish/62-02-SUMMARY.md
@.planning/phases/62-design-ux-polish/62-03-SUMMARY.md
@.planning/phases/62-design-ux-polish/62-04-SUMMARY.md
@.planning/phases/62-design-ux-polish/62-05-SUMMARY.md
@web/frontend/src/lib/motion-primitives.ts
@web/frontend/src/lib/design-tokens.ts
@.claude/agents/design-engineer.md

### Why motion-on-pages-1-5 lives here, not in 62-04

Plan 62-03 owns file-level edits to pages 1-5 (token normalization). To avoid same-wave file conflicts, plan 62-04 explicitly deferred motion wiring on those 5 pages to this plan. By Wave 4, 62-03 has merged and motion-primitives.ts exists.

### Scope split

This plan has three distinct tasks:
1. Apply motion to pages 1-5 (overview, accuracy, projections, rankings, predictions)
2. Re-run audit across all 11 pages, produce AUDIT-FINAL.md, score all ≥ 7
3. Human-verify checkpoint: user browses the live deploy (or local) and confirms premium feel

### Guardrails
- Do not modify token scale or motion primitive durations in this plan — they are locked.
- If a page scores < 7 in the re-audit, create a follow-up task inside THIS plan's action (not a new plan) — but only if the fix is ≤ 3 files and does not require more than a polish tweak. Anything bigger → flag as gap_closure candidate for a phase 62.1.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply motion to pages 1-5</name>
  <files>
web/frontend/src/app/dashboard/page.tsx
web/frontend/src/app/dashboard/accuracy/page.tsx
web/frontend/src/app/dashboard/projections/page.tsx
web/frontend/src/app/dashboard/rankings/page.tsx
web/frontend/src/app/dashboard/predictions/page.tsx
web/frontend/src/features/nfl/components/stat-cards.tsx
web/frontend/src/features/nfl/components/prediction-cards.tsx
  </files>
  <behavior>
- Overview page wraps body in <FadeIn>; stat-cards use <Stagger> so cards cascade in
- Accuracy page fades in; charts fade in with slight delay
- Projections + Rankings pages: page fades in; table header row has no motion (to avoid jitter), but filter/sort controls get <PressScale>
- Predictions page: card grid uses <Stagger>; each <PredictionCard> uses <HoverLift>; edge-tier badges get a subtle reveal (opacity 0→1 + y 4→0 over MOTION.base) when edge ≥ medium threshold
- prefers-reduced-motion respected (via motion-primitives contract from 62-04)
  </behavior>
  <action>
Spawn the `design-engineer` subagent (Opus 4.6) with the `animate` + `polish` skills. Required behavior:

1. Read AUDIT-BASELINE.md DSGN-03 gap list for pages 1-5.
2. For each of the 5 page files, wrap root JSX in `<FadeIn>` (import from `@/lib/motion-primitives`).
3. `stat-cards.tsx`: wrap the card grid in `<Stagger step={0.04}>`; wrap each card in `<FadeIn>` (or let Stagger handle children if its children convention supports it — design-engineer chooses, document in SUMMARY).
4. `prediction-cards.tsx`: wrap the grid in `<Stagger>`; each card in `<HoverLift>`; the edge badge in a conditional <FadeIn> when `edge >= 1.5` (medium tier).
5. Build, typecheck, lint.

Constraint: pages 3-4 (projections, rankings) host data tables owned by 62-03/05 — do NOT edit table internals here. Motion is applied only at the page shell + feature wrappers listed above.
  </action>
  <verify>
    <automated>cd web/frontend &amp;&amp; (command -v bun &gt;/dev/null &amp;&amp; bun run build || npm run build) 2&gt;&amp;1 | tee /tmp/build06a.log | tail -10; grep -q "error" /tmp/build06a.log &amp;&amp; exit 1; grep -l "motion-primitives" web/frontend/src/app/dashboard/page.tsx web/frontend/src/app/dashboard/accuracy/page.tsx web/frontend/src/app/dashboard/projections/page.tsx web/frontend/src/app/dashboard/rankings/page.tsx web/frontend/src/app/dashboard/predictions/page.tsx 2&gt;/dev/null | wc -l | xargs -I{} test {} -eq 5</automated>
  </verify>
  <done>All 5 page files import from motion-primitives. Build passes. Stat-cards and prediction-cards use <Stagger>. Edge badges reveal for medium/high tier.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run design audit and produce AUDIT-FINAL.md</name>
  <files>
.planning/phases/62-design-ux-polish/AUDIT-FINAL.md
  </files>
  <action>
Spawn the `design-engineer` subagent (Opus 4.6) with the `audit` + `critique` skills.

The agent must:
1. Read AUDIT-BASELINE.md for baseline scores.
2. Re-audit all 11 dashboard pages using the same rubric (8 dimensions, 0-10 scale, weighted overall).
3. Build + run the site locally (`cd web/frontend && bun run dev`) OR audit against the deployed URL.
4. Produce `.planning/phases/62-design-ux-polish/AUDIT-FINAL.md` in this format:

```yaml
---
audit_version: final
pages_audited: 11
live_url: https://frontend-jet-seven-33.vercel.app
rubric: design-engineer v1 (8 dimensions)
ship_gate: DSGN-01 requires all pages > 7/10
---
```

### Delta Summary
| Page | Baseline | Final | Delta | > 7? |
|------|---------|-------|-------|------|
| /dashboard | X.X | X.X | +X.X | PASS/FAIL |
| ... (11 rows) |

### Per-page sections
(Same format as AUDIT-BASELINE.md, plus a "What changed" subsection citing which plan(s) improved which dimension.)

### DSGN-02/03/04 verification
- DSGN-02: Typography / color / spacing consistency — PASS/FAIL with evidence
- DSGN-03: Motion on key user actions — PASS/FAIL with list of interactions verified
- DSGN-04: Mobile 375px usability — PASS/FAIL with cross-reference to MOBILE-AUDIT.md

### Ship gate decision
If all 11 pages > 7: "READY FOR SHIP"
If any page ≤ 7: list remediation items. If remediation is a 1-3 file polish tweak, apply it inline (in the files allowed by this plan) and re-score. If remediation is larger, mark as PHASE 62.1 gap_closure candidate.

5. If remediation is applied, run build and re-record scores.
  </action>
  <verify>
    <automated>test -f .planning/phases/62-design-ux-polish/AUDIT-FINAL.md &amp;&amp; grep -c "^### Page\|^| /dashboard" .planning/phases/62-design-ux-polish/AUDIT-FINAL.md | xargs -I{} test {} -ge 11 &amp;&amp; grep -q "Ship gate decision" .planning/phases/62-design-ux-polish/AUDIT-FINAL.md</automated>
  </verify>
  <done>AUDIT-FINAL.md exists with delta table, per-page sections, DSGN-02/03/04 verification, and explicit SHIP/NOT-SHIP decision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of premium feel on real devices</name>
  <what-built>
- Token system (62-02) + consistency pass (62-03, 62-04)
- Motion primitives (62-04) + motion applied across all 11 pages (62-04 + this plan)
- Mobile adaptations (62-05)
- Final audit scored > 7/10 per page (this plan Task 2)
  </what-built>
  <how-to-verify>
1. Open the live or preview deploy:
   - If Vercel preview, pull latest branch → Vercel auto-deploys → open the preview URL
   - Otherwise run locally: `cd web/frontend && bun run dev` then visit http://localhost:3000/dashboard
2. On **desktop (1440×900)**, visit each of the 11 pages in this order and check:
   - /dashboard (overview) — stat cards cascade in; charts render; no layout jitter
   - /dashboard/accuracy — page fades in; charts render; no hardcoded styling visible
   - /dashboard/projections — table renders; sort/filter interactive; row hover feels right
   - /dashboard/rankings — same as projections
   - /dashboard/predictions — card grid staggers in; edge badges reveal on medium+/high tier cards; hover lifts cards
   - /dashboard/advisor — chat widget fades in; send a message ("who are the top 5 RBs"); message animates in; typing indicator visible during response
   - /dashboard/draft — draft UI consistent with rest of site; button press scales
   - /dashboard/lineups — lineup cards stagger in; drag/drop (if present) still works
   - /dashboard/matchups — offense/defense columns render; data present (if matchup data exists)
   - /dashboard/news — articles stagger in; sentiment badges animate on tier change
   - /dashboard/players — search result list staggers; detail panel fades in on select
3. On **mobile (iPhone SE or Chrome devtools 375×667)**, repeat the same 11 pages and check:
   - No horizontal scroll on <body>
   - Sidebar opens as a drawer via hamburger
   - Data tables usable (either scroll with sticky col OR stacked-card)
   - Chat widget usable: input visible, messages fit
   - All buttons tappable without mis-taps
4. Toggle `prefers-reduced-motion: reduce` in OS settings (or Chrome devtools Rendering panel → Emulate CSS media feature) and confirm motion is removed (not slow/weird — simply absent).
5. Open AUDIT-FINAL.md and verify the SHIP decision is "READY FOR SHIP" with all 11 pages > 7/10.
  </how-to-verify>
  <resume-signal>Type "approved" when premium feel confirmed on desktop + mobile + reduced-motion. Otherwise list issues and they'll be addressed in a phase 62.1 gap_closure plan.</resume-signal>
</task>

</tasks>

<verification>
- `cd web/frontend && bun run build` succeeds
- AUDIT-FINAL.md shows all 11 pages > 7/10 with positive delta vs AUDIT-BASELINE.md
- MOBILE-AUDIT.md PASS status intact (no regressions from this plan)
- Human-verify checkpoint approved
</verification>

<success_criteria>
- DSGN-01 satisfied: audit score > 7/10 on every page (measured, recorded in AUDIT-FINAL.md)
- DSGN-03 satisfied: motion applied across all 11 pages, consistent durations/easings, reduced-motion honored
- Phase 62 goal met: "The website looks and feels like a premium product on any device."
</success_criteria>

<output>
After completion, create `.planning/phases/62-design-ux-polish/62-06-SUMMARY.md` with:
- Final per-page scores table (baseline → final deltas)
- List of motion interactions wired on pages 1-5 (this plan's Task 1)
- Any remediation applied in Task 2 inline
- Human-verify outcome (approved / deferred items)
- Confirmation that DSGN-01, DSGN-02, DSGN-03, DSGN-04 are all satisfied
</output>
