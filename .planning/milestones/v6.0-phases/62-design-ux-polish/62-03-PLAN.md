---
phase: 62-design-ux-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 62-01
  - 62-02
files_modified:
  - web/frontend/src/components/layout/page-container.tsx
  - web/frontend/src/components/layout/header.tsx
  - web/frontend/src/components/layout/app-sidebar.tsx
  - web/frontend/src/components/ui/heading.tsx
  - web/frontend/src/features/nfl/components/stat-cards.tsx
  - web/frontend/src/features/nfl/components/accuracy-dashboard.tsx
  - web/frontend/src/features/nfl/components/accuracy-chart.tsx
  - web/frontend/src/features/nfl/components/mae-chart.tsx
  - web/frontend/src/features/nfl/components/projections-table
  - web/frontend/src/features/nfl/components/rankings-table
  - web/frontend/src/features/nfl/components/prediction-cards.tsx
  - web/frontend/src/app/dashboard/page.tsx
  - web/frontend/src/app/dashboard/accuracy/page.tsx
  - web/frontend/src/app/dashboard/projections/page.tsx
  - web/frontend/src/app/dashboard/rankings/page.tsx
  - web/frontend/src/app/dashboard/predictions/page.tsx
autonomous: true
requirements:
  - DSGN-02

must_haves:
  truths:
    - "The shared layout primitives (page-container, header, sidebar, Heading) use design tokens only"
    - "Pages 1-5 (overview, accuracy, projections, rankings, predictions) show zero typography drift vs AUDIT-BASELINE findings"
    - "Spacing between sections, cards, and table rows on pages 1-5 matches the shared token scale"
    - "No hardcoded text sizes, colors, or magic-number margins remain in the touched files"
  artifacts:
    - path: "web/frontend/src/components/layout/page-container.tsx"
      provides: "Token-compliant shared shell"
    - path: "web/frontend/src/components/ui/heading.tsx"
      provides: "Single heading component used across pages with consistent hierarchy"
  key_links:
    - from: "pages 1-5"
      to: "web/frontend/src/lib/design-tokens.ts + tokens.css"
      via: "CSS classes backed by tokens, or TYPE_SCALE / SPACE imports"
      pattern: "tokens\\.css|design-tokens"
---

<objective>
Apply the DSGN-02 consistency pass to the first 5 of 11 pages: overview, accuracy, projections, rankings, predictions — plus the shared layout primitives every page inherits (page-container, header, sidebar, Heading). After this plan, these pages should exhibit zero typography/spacing drift when cross-referenced.

Purpose: DSGN-02 requires consistency across all 11 pages. Rather than one mega-plan touching 11 pages (context budget and file-conflict risk), this plan owns pages 1-5 + shared layout. Plan 62-04 owns pages 6-11 plus the motion layer.
Output: Token-compliant layout primitives and 5 pages. No functional change — purely visual consistency.
</objective>

<execution_context>
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgesmith/repos/nfl_data_engineering/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-design-ux-polish/AUDIT-BASELINE.md
@.planning/phases/62-design-ux-polish/62-02-SUMMARY.md
@web/frontend/docs/design-tokens.md
@web/frontend/src/styles/tokens.css
@web/frontend/src/lib/design-tokens.ts
@.claude/agents/design-engineer.md
@web/frontend/src/components/layout/page-container.tsx

This plan consumes the `## Priority Targets for Phase 62 Plans > DSGN-02` list in AUDIT-BASELINE.md produced by 62-01.

Pages in scope for 62-03 (5 pages + shared layout):
1. `/dashboard` → `src/app/dashboard/page.tsx` + `stat-cards.tsx`, `mae-chart.tsx`, `accuracy-chart.tsx`
2. `/dashboard/accuracy` → `src/app/dashboard/accuracy/page.tsx` + `accuracy-dashboard.tsx`
3. `/dashboard/projections` → `src/app/dashboard/projections/page.tsx` + `projections-table/`
4. `/dashboard/rankings` → `src/app/dashboard/rankings/page.tsx` + `rankings-table/`
5. `/dashboard/predictions` → `src/app/dashboard/predictions/page.tsx` + `prediction-cards.tsx`

Shared layout (applies to all 11 pages, but owned by this plan):
- `src/components/layout/page-container.tsx`
- `src/components/layout/header.tsx`
- `src/components/layout/app-sidebar.tsx`
- `src/components/ui/heading.tsx`

File ownership: plan 62-04 must not touch any file listed in this plan's `files_modified`. Pages 6-11 and motion wiring belong to 62-04.

### Guardrails

- Never introduce pixel values. Use Tailwind classes that resolve to the token scale, or `style={{ fontSize: 'var(--fs-body)' }}` for inline cases.
- Preserve every API surface: props, named exports, public behavior. This plan is visual only.
- Do not change data fetching, routing, or business logic.
- Respect the multi-theme system: all colors must resolve through existing theme tokens (`bg-background`, `text-foreground`, etc.) — do not introduce hex/rgb values.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize shared layout primitives to tokens</name>
  <files>
web/frontend/src/components/layout/page-container.tsx
web/frontend/src/components/layout/header.tsx
web/frontend/src/components/layout/app-sidebar.tsx
web/frontend/src/components/ui/heading.tsx
  </files>
  <behavior>
- Heading component renders h1/h2/h3 with fs/lh bound to --fs-h1/h2/h3 tokens
- Page-container title, description, and body gap use --space-* tokens
- Header bar height resolves to a token (not a magic number)
- Sidebar spacing between nav items uses --space-3
- No raw text-xl/text-2xl/text-[N]px class in any of the four files after the change (Tailwind shorthand OK if it maps to token-scale values defined in tokens.css)
  </behavior>
  <action>
Spawn the `design-engineer` subagent (Opus 4.6) with the `polish` + `typeset` + `layout` skills. Required behavior:

1. Read AUDIT-BASELINE.md's layout/typography drift findings for the shared shell.
2. Refactor `page-container.tsx`, `header.tsx`, `app-sidebar.tsx`, `heading.tsx` to:
   - Use `--fs-*`, `--lh-*`, `--space-*` tokens (either via inline CSS vars, or via Tailwind arbitrary values resolving to tokens, e.g. `text-[length:var(--fs-body)]`).
   - Replace any `text-2xl`, `text-3xl`, `text-sm` class that clashes with the token scale with the explicit token.
   - Normalize gap/padding/margin to `--space-*` values.
3. Preserve all props and exports. `<PageContainer>` still accepts `scrollable`, `pageTitle`, `pageDescription`, `isLoading`, etc.
4. Update `Heading` to accept a `level: 1|2|3` prop and apply the correct `--fs-h{level}` and `--lh-h{level}` token. If the current signature differs, preserve backward compatibility (additive props only).
5. Build and typecheck.

Constraint: Do not change the visual composition (no sections moved, removed, or added). Only normalize token usage.
  </action>
  <verify>
    <automated>cd web/frontend &amp;&amp; (command -v bun &gt;/dev/null &amp;&amp; bun run build || npm run build) 2&gt;&amp;1 | tail -20; ! grep -E "text-(xs|sm|base|lg|xl|2xl|3xl|4xl)" web/frontend/src/components/layout/page-container.tsx web/frontend/src/components/layout/header.tsx web/frontend/src/components/ui/heading.tsx 2&gt;/dev/null</automated>
  </verify>
  <done>Build passes. page-container/header/heading contain no raw Tailwind text-size utilities that bypass the token scale; all typography references either --fs-* or classes explicitly mapped to them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply tokens across pages 1-5 and their feature components</name>
  <files>
web/frontend/src/features/nfl/components/stat-cards.tsx
web/frontend/src/features/nfl/components/accuracy-dashboard.tsx
web/frontend/src/features/nfl/components/accuracy-chart.tsx
web/frontend/src/features/nfl/components/mae-chart.tsx
web/frontend/src/features/nfl/components/projections-table
web/frontend/src/features/nfl/components/rankings-table
web/frontend/src/features/nfl/components/prediction-cards.tsx
web/frontend/src/app/dashboard/page.tsx
web/frontend/src/app/dashboard/accuracy/page.tsx
web/frontend/src/app/dashboard/projections/page.tsx
web/frontend/src/app/dashboard/rankings/page.tsx
web/frontend/src/app/dashboard/predictions/page.tsx
  </files>
  <behavior>
- Every page renders identically in composition but with token-backed spacing and typography
- Card padding uses --space-4 or --space-5 consistently; stat cards align across overview and accuracy pages
- Table headers use --fs-sm + --lh-sm and consistent letter-spacing
- Prediction card edge badges inherit existing semantic colors (no hardcoded hex introduced)
- No new visual elements, no removed ones
  </behavior>
  <action>
Spawn the `design-engineer` subagent (Opus 4.6) with the `polish` + `typeset` + `layout` skills. Required behavior:

1. Read AUDIT-BASELINE.md's DSGN-02 gap list for these specific 5 pages.
2. For each file in `files_modified` (excluding the layout primitives handled in Task 1):
   - Replace raw `text-*`, `p-*`, `m-*`, `gap-*` classes that deviate from the spacing/typography token scale with token-backed alternatives.
   - Preserve color tokens (`bg-background`, `text-foreground`, `border-border`, `text-muted-foreground`) — they already route through theme.css.
   - Remove one-off inline styles like `style={{ fontSize: '18px' }}` in favor of `var(--fs-*)` or matching Tailwind class.
   - Ensure `Heading` is used for page titles (not raw `<h1 class='text-2xl ...'>`).
3. For `projections-table/` and `rankings-table/` directories, audit every .tsx file. Table row height, header padding, and sort-header typography must use tokens.
4. For `prediction-cards.tsx`, edge badges retain their semantic color meaning (green/amber/red for high/medium/low) but draw from existing theme tokens — introduce a small `edgeTier()` helper returning tailwind classes if not already present. Do not hardcode hex.
5. Run `bun run build` (or `npm run build`) and verify it passes.
6. Run the project's lint: `bun run lint` (or `npm run lint`). Fix any newly introduced warnings.

Constraint: zero new dependencies. Zero changes to props, exports, hooks, or data fetching. This plan is visual-normalization only. If the design-engineer wants to improve information architecture (layout shifts, new sections), it MUST defer to 62-06 re-audit — not do it here.
  </action>
  <verify>
    <automated>cd web/frontend &amp;&amp; (command -v bun &gt;/dev/null &amp;&amp; bun run build || npm run build) 2&gt;&amp;1 | tee /tmp/build03.log | tail -10; grep -q "error" /tmp/build03.log &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>Build passes; lint passes; all 5 pages render; AUDIT-BASELINE.md's DSGN-02 drift items for pages 1-5 have been addressed (verified by grep that no hardcoded text-size / pixel-margin remains outside token scale).</done>
</task>

</tasks>

<verification>
- `cd web/frontend && bun run build` succeeds
- Visual spot-check (described for 62-06 re-audit): open each of pages 1-5 in the browser and confirm no layout regressions
- `git diff --stat` shows only the 15 files in files_modified touched, with no unintended file changes
</verification>

<success_criteria>
- DSGN-02 satisfied for pages 1-5 and the shared shell: all typography, spacing, and radius values route through the token scale
- Layout primitives are the single place a future change to the base scale propagates from
- Zero functional regression, zero new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/62-design-ux-polish/62-03-SUMMARY.md` with:
- List of drift items from AUDIT-BASELINE.md that were addressed
- Any drift items intentionally deferred and why
- Before/after screenshots of overview + projections pages (optional, link if captured)
</output>
