---
phase: 04-documentation-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/NFL_DATA_DICTIONARY.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "Data dictionary has an entry for every data type in DATA_TYPE_REGISTRY (15 top-level types)"
    - "Each sub-type has its own section (NGS Passing/Rushing/Receiving separate; PFR pass/rush/rec/def separate)"
    - "Each entry includes source notes: nfl-data-py function name, season range, known quirks"
    - "Column specs are auto-generated from Parquet schemas for locally available data types"
  artifacts:
    - path: "docs/NFL_DATA_DICTIONARY.md"
      provides: "Complete Bronze data type reference with column specs for all 15+ types"
      min_lines: 1200
      contains: "NGS Passing"
  key_links:
    - from: "docs/NFL_DATA_DICTIONARY.md"
      to: "src/config.py"
      via: "DATA_TYPE_SEASON_RANGES referenced for season availability"
      pattern: "DATA_TYPE_SEASON_RANGES|season.*range"
    - from: "docs/NFL_DATA_DICTIONARY.md"
      to: "src/nfl_data_adapter.py"
      via: "nfl-data-py function names documented per table"
      pattern: "import_|fetch_"
---

<objective>
Update the NFL Data Dictionary to cover all 15+ Bronze data types with actual column specs, source notes, and season availability.

Purpose: The current dictionary only covers Games and Plays (with 3 "planned" stubs). After Phases 1-3, there are 15 data types in the registry. The dictionary should be the single source of truth for Bronze schemas.

Output: Comprehensive `docs/NFL_DATA_DICTIONARY.md` with entries for all data types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation-update/04-CONTEXT.md
@.planning/phases/04-documentation-update/04-RESEARCH.md

@docs/NFL_DATA_DICTIONARY.md
@src/config.py
@src/nfl_data_adapter.py
@scripts/bronze_ingestion_simple.py
@src/nfl_data_integration.py
@tests/test_advanced_ingestion.py
@tests/test_pbp_ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-generate column specs from local Parquet files</name>
  <files>docs/NFL_DATA_DICTIONARY.md</files>
  <action>
    Write a temporary script (or inline Python) to extract schemas from all locally available Parquet files in `data/bronze/`. Use `pyarrow.parquet.read_schema()` on the most recent file per data type (by filename timestamp). For each data type with local files (schedules, player_weekly, player_seasonal, snap_counts, injuries, rosters), capture: column name, pyarrow type, nullable flag.

    For data types WITHOUT local files (pbp, ngs, pfr_weekly, pfr_seasonal, qbr, depth_charts, draft_picks, combine, teams), gather column info from:
    - `PBP_COLUMNS` in `src/config.py` for PBP (103 columns)
    - Mock DataFrames in `tests/test_advanced_ingestion.py` for NGS, PFR, QBR, depth_charts
    - Mock DataFrames in `tests/test_pbp_ingestion.py` for PBP validation columns
    - `NFLDataAdapter` method signatures for draft_picks, combine, teams
    - `validate_data()` required columns in `src/nfl_data_integration.py`
    - Note these as "representative columns" since full schemas require actual ingestion

    Save extracted schema data for use in Task 2.
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "
import pyarrow.parquet as pq
import os
count = 0
for root, dirs, files in os.walk('data/bronze'):
    for f in files:
        if f.endswith('.parquet'):
            schema = pq.read_schema(os.path.join(root, f))
            count += 1
print(f'Successfully read schemas from {count} parquet files')
assert count >= 20, f'Expected 20+ files, got {count}'
"</automated>
  </verify>
  <done>Schema data extracted from all 6 locally available data types. Column info gathered from config/tests/adapter for remaining 9 types.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite NFL_DATA_DICTIONARY.md with all Bronze data types</name>
  <files>docs/NFL_DATA_DICTIONARY.md</files>
  <action>
    Rewrite `docs/NFL_DATA_DICTIONARY.md` with the following structure:

    **Document structure:**
    1. Title + intro explaining this is the schema reference for all Bronze data types
    2. Table of Contents listing all data types
    3. Per-data-type sections (see below)
    4. Keep existing Silver and Gold layer sections (update if needed but preserve)

    **Per data type section format** (per user decision -- same depth as existing Games table):
    ```
    ### [Data Type Name]

    **Source:** `nfl-data-py` function `import_xyz()` via `NFLDataAdapter.fetch_xyz()`
    **Seasons:** YYYY-YYYY (from DATA_TYPE_SEASON_RANGES)
    **S3 Path:** `s3://nfl-raw/[bronze_path]/season=YYYY/`
    **Local Path:** `data/bronze/[bronze_path]/season=YYYY/`
    **Known Quirks:** [any gotchas, e.g., "use import_seasonal_rosters not import_rosters"]

    | Column | Type | Nullable | Description | Example |
    |--------|------|----------|-------------|---------|
    | col1   | string | No | ... | ... |
    ```

    **Data types to document (one section per sub-type per user decision):**

    Locally verified (full column specs from Parquet):
    - Schedules (Games)
    - Player Weekly Stats
    - Player Seasonal Stats
    - Snap Counts
    - Injuries
    - Rosters

    From config/tests (representative columns, note "full schema via ingestion"):
    - Play-by-Play (PBP) -- use PBP_COLUMNS from config.py
    - NGS Passing, NGS Rushing, NGS Receiving (3 separate sections)
    - PFR Weekly Passing, PFR Weekly Rushing, PFR Weekly Receiving, PFR Weekly Defense (4 sections)
    - PFR Seasonal Passing, PFR Seasonal Rushing, PFR Seasonal Receiving, PFR Seasonal Defense (4 sections)
    - QBR Weekly, QBR Seasonal (2 sections)
    - Depth Charts
    - Draft Picks
    - Combine
    - Teams

    **For locally available types:** Include ALL columns from Parquet schema with types and descriptions. Write meaningful descriptions based on NFL domain knowledge.

    **For non-local types:** Document columns from test mocks and config. Add note: "Representative columns shown; full schema available after running `python scripts/bronze_ingestion_simple.py --data-type [type] --season YYYY`"

    **Per user decision:** S3 paths as primary reference. Include source notes (nfl-data-py function name, season range, quirks). One entry per sub-type.
  </action>
  <verify>
    <automated>source venv/bin/activate && python -c "
content = open('docs/NFL_DATA_DICTIONARY.md').read()
required_types = ['Schedules', 'Player Weekly', 'Player Seasonal', 'Snap Counts', 'Injuries', 'Rosters', 'Play-by-Play', 'NGS Passing', 'NGS Rushing', 'NGS Receiving', 'PFR Weekly', 'PFR Seasonal', 'QBR', 'Depth Charts', 'Draft Picks', 'Combine', 'Teams']
missing = [t for t in required_types if t not in content]
assert not missing, f'Missing data types: {missing}'
assert 'nfl-data-py' in content.lower() or 'nfl_data' in content, 'Missing source notes'
assert 's3://nfl-raw' in content, 'Missing S3 paths'
assert content.count('###') >= 15, f'Expected 15+ sections, got {content.count(chr(35)*3)}'
print(f'Data dictionary verified: {len(required_types)} types found, {content.count(chr(10))} lines')
"</automated>
  </verify>
  <done>NFL_DATA_DICTIONARY.md has entries for all 15+ Bronze data types. Each entry has source notes (nfl-data-py function, season range, quirks). S3 paths are primary references. Sub-types (NGS, PFR) each have their own section. Column specs are auto-generated for local types and representative for non-local types.</done>
</task>

</tasks>

<verification>
- Data dictionary has sections for all 15 top-level data types (24+ with sub-types)
- Each section includes: source function, season range, S3 path, local path, column table
- Column tables have: Column, Type, Nullable, Description, Example
- No duplicate column specs with other docs (prediction model will cross-reference this)
- `python -m pytest tests/ -x -q` full suite still passes
</verification>

<success_criteria>
- Every data type in DATA_TYPE_REGISTRY has a dictionary entry
- Sub-types documented separately (NGS x3, PFR weekly x4, PFR seasonal x4, QBR x2)
- Source notes include nfl-data-py function name and known quirks
- S3 paths are canonical references with local paths as fallback
- Locally available types have complete column specs from actual Parquet schemas
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-update/04-02-SUMMARY.md`
</output>
