---
phase: 03-advanced-stats-context-data
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_advanced_ingestion.py
autonomous: true
requirements:
  - ADV-01
  - ADV-02
  - ADV-03
  - ADV-04
  - ADV-05
  - CTX-01
  - CTX-02
  - VAL-03

must_haves:
  truths:
    - "NGS adapter fetch works for passing, rushing, and receiving stat types"
    - "PFR weekly adapter fetch works for pass, rush, rec, def sub-types"
    - "PFR seasonal adapter fetch works for pass, rush, rec, def sub-types"
    - "QBR adapter fetch works for both weekly and seasonal frequency"
    - "Depth charts adapter fetch returns a DataFrame"
    - "Draft picks adapter fetch returns a DataFrame"
    - "Combine adapter fetch returns a DataFrame"
    - "validate_data() catches missing required columns for each new type"
    - "At least 1 test per new data type exists (7+ tests total)"
  artifacts:
    - path: "tests/test_advanced_ingestion.py"
      provides: "Test coverage for all 7 new data types + validation"
      min_lines: 150
  key_links:
    - from: "tests/test_advanced_ingestion.py"
      to: "src/nfl_data_adapter.py"
      via: "mock _import_nfl, call fetch_* methods"
      pattern: "patch.*NFLDataAdapter.*_import_nfl"
    - from: "tests/test_advanced_ingestion.py"
      to: "src/nfl_data_integration.py"
      via: "call validate_data() with valid/invalid DataFrames"
      pattern: "validate_data"
    - from: "tests/test_advanced_ingestion.py"
      to: "scripts/bronze_ingestion_simple.py"
      via: "test _build_method_kwargs for QBR frequency"
      pattern: "_build_method_kwargs"
---

<objective>
Create test suite covering all 7 new data types (NGS, PFR weekly, PFR seasonal, QBR, depth charts, draft picks, combine) plus validate_data() extensions.

Purpose: VAL-03 requires minimum 1 test per new data type. These tests mock nfl-data-py to avoid API dependency and verify the adapter + validation wiring works correctly. They also serve as regression tests for the QBR frequency fix from Plan 01.

Output: tests/test_advanced_ingestion.py with 10+ tests organized by data type.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-advanced-stats-context-data/03-RESEARCH.md
@.planning/phases/03-advanced-stats-context-data/03-01-SUMMARY.md

@tests/test_pbp_ingestion.py
@src/nfl_data_adapter.py
@src/nfl_data_integration.py
@scripts/bronze_ingestion_simple.py

<interfaces>
<!-- Test pattern from test_pbp_ingestion.py — executor should follow this exactly. -->

```python
from unittest.mock import patch, MagicMock
import pandas as pd
from src.nfl_data_adapter import NFLDataAdapter

@patch.object(NFLDataAdapter, "_import_nfl")
def test_fetch_ngs_returns_dataframe(mock_import_nfl):
    mock_nfl = MagicMock()
    mock_nfl.import_ngs_data.return_value = pd.DataFrame({...})
    mock_import_nfl.return_value = mock_nfl

    adapter = NFLDataAdapter()
    df = adapter.fetch_ngs([2024], stat_type="passing")
    assert not df.empty
```

<!-- Adapter method -> nfl-data-py function mapping (from nfl_data_adapter.py): -->
- fetch_ngs -> import_ngs_data(seasons, stat_type)
- fetch_pfr_weekly -> import_weekly_pfr(s_type, seasons)
- fetch_pfr_seasonal -> import_seasonal_pfr(s_type, seasons)
- fetch_qbr -> import_qbr(seasons, frequency)
- fetch_depth_charts -> import_depth_charts(seasons)
- fetch_draft_picks -> import_draft_picks(seasons)
- fetch_combine -> import_combine_data(seasons)

<!-- Verified required columns from RESEARCH.md: -->
- NGS common: season, season_type, week, player_display_name, player_position, team_abbr, player_gsis_id
- PFR weekly common: game_id, season, week, team, pfr_player_name, pfr_player_id
- PFR seasonal common: player, team, season, pfr_id
- QBR: season, season_type, qbr_total, pts_added, epa_total, qb_plays
- Depth charts: season, club_code, week, position, full_name, gsis_id
- Draft picks: season, round, pick, team, pfr_player_name, position
- Combine: season, player_name, pos, school, ht, wt
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test file with adapter fetch tests for all 7 data types</name>
  <files>tests/test_advanced_ingestion.py</files>
  <behavior>
  - TestNGSIngestion: fetch_ngs returns non-empty DataFrame for each stat_type (passing, rushing, receiving) -- 1 parametrized test or 3 individual tests
  - TestPFRWeeklyIngestion: fetch_pfr_weekly returns non-empty DataFrame for each s_type (pass, rush, rec, def) -- 1 parametrized test
  - TestPFRSeasonalIngestion: fetch_pfr_seasonal returns non-empty DataFrame for each s_type -- 1 parametrized test
  - TestQBRIngestion: fetch_qbr works for frequency="weekly" AND frequency="seasonal" -- 2 tests
  - TestDepthChartsIngestion: fetch_depth_charts returns non-empty DataFrame -- 1 test
  - TestDraftPicksIngestion: fetch_draft_picks returns non-empty DataFrame -- 1 test
  - TestCombineIngestion: fetch_combine returns non-empty DataFrame -- 1 test
  </behavior>
  <action>
  Create `tests/test_advanced_ingestion.py` following the exact mock pattern from `tests/test_pbp_ingestion.py`:

  1. Use `@patch.object(NFLDataAdapter, "_import_nfl")` to mock the nfl-data-py module
  2. Set up mock return values with DataFrames containing the required columns from RESEARCH.md
  3. Call the adapter fetch method and assert:
     - DataFrame is not empty
     - The underlying nfl-data-py function was called (assert_called_once)
  4. For NGS: test all 3 stat_types (passing, rushing, receiving). Use `@pytest.mark.parametrize` for conciseness.
  5. For PFR weekly: test all 4 s_types. Use parametrize.
  6. For PFR seasonal: test all 4 s_types. Use parametrize.
  7. For QBR: test both weekly and seasonal frequency explicitly (2 separate tests to verify the frequency fix from Plan 01).
  8. For depth_charts, draft_picks, combine: 1 test each.

  Also add:
  - TestValidation class: 2 tests
    - test_validate_data_accepts_valid_ngs: Create a DataFrame with all required NGS columns, call validate_data("ngs"), assert is_valid=True
    - test_validate_data_rejects_missing_columns: Create a DataFrame missing a required column for each of the 7 types, call validate_data(), assert is_valid=False for each

  - TestQBRFrequencyKwargs class: 1 test
    - test_qbr_frequency_from_args: Import _build_method_kwargs, create args with frequency="seasonal", assert kwargs["frequency"] == "seasonal"

  Use `sys.path.insert(0, ...)` at top for project root (same as test_pbp_ingestion.py).
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_advanced_ingestion.py -v</automated>
  </verify>
  <done>tests/test_advanced_ingestion.py exists with 10+ tests. All tests pass. Coverage: 1+ test per data type (ADV-01 through ADV-05, CTX-01, CTX-02), validation tests (VAL-01 via test), QBR frequency test (ADV-04 via test). Full test suite (python -m pytest tests/ -v) remains green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_advanced_ingestion.py -v` -- all tests pass
- `python -m pytest tests/test_advanced_ingestion.py -v --tb=short 2>&1 | grep -c "PASSED"` -- at least 10 passes
- `python -m pytest tests/ -v` -- full suite still green (existing 71 tests + new tests)
</verification>

<success_criteria>
1. tests/test_advanced_ingestion.py exists with organized test classes
2. At least 1 test per new data type (7 data types = 7+ tests minimum)
3. QBR tests cover both weekly and seasonal frequency
4. Validation tests confirm validate_data() works for new types
5. All tests pass without hitting real nfl-data-py API (fully mocked)
6. Full test suite (71 existing + new) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced-stats-context-data/03-02-SUMMARY.md`
</output>
