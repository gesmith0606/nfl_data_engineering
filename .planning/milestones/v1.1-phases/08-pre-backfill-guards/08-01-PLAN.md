---
phase: 08-pre-backfill-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - requirements.txt
  - tests/test_infrastructure.py
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03

must_haves:
  truths:
    - "Injuries ingestion for season 2025 is rejected as invalid by validate_season_for_type()"
    - "Injuries ingestion for season 2024 is accepted as valid"
    - "GITHUB_TOKEN is documented in .env with a comment noting nfl-data-py does NOT use it"
    - "nfl_data_py and numpy pins in requirements.txt have inline comments explaining why"
  artifacts:
    - path: "src/config.py"
      provides: "Injury season cap at 2024 via static lambda"
      contains: 'lambda: 2024'
    - path: "requirements.txt"
      provides: "Pinned dependency comments"
      contains: "pinned:"
    - path: "tests/test_infrastructure.py"
      provides: "Injury cap test + updated max-year test"
      contains: "test_injury_season_capped_at_2024"
  key_links:
    - from: "src/config.py"
      to: "scripts/bronze_ingestion_simple.py"
      via: "validate_season_for_type() already wired at line 312"
      pattern: "validate_season_for_type"
---

<objective>
Harden config, pin dependencies, and document rate-limit protection before bulk backfill begins.

Purpose: Prevent known failure modes (injuries 2025 crash, dependency breakage, rate limiting) before Phases 9-10 run bulk ingestion across 10 years of data.
Output: Updated config.py (injury cap), commented requirements.txt, GITHUB_TOKEN in .env, passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pre-backfill-guards/08-CONTEXT.md
@.planning/phases/08-pre-backfill-guards/08-RESEARCH.md

@src/config.py
@requirements.txt
@tests/test_infrastructure.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/config.py:
```python
DATA_TYPE_SEASON_RANGES: Dict[str, Tuple[int, Callable[[], int]]] = {
    # ... 15 data types, each (min_year, max_year_callable) ...
    "injuries": (2009, get_max_season),  # THIS LINE CHANGES
}

def validate_season_for_type(data_type: str, season: int) -> bool:
    # Already wired into bronze_ingestion_simple.py -- no changes needed there
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cap injury season range and update tests</name>
  <files>src/config.py, tests/test_infrastructure.py</files>
  <behavior>
    - test_injury_season_capped_at_2024: validate_season_for_type("injuries", 2024) returns True, validate_season_for_type("injuries", 2025) returns False, validate_season_for_type("injuries", 2009) returns True
    - test_validate_edge_max_year (updated): iterates all DATA_TYPE_SEASON_RANGES EXCEPT types with static caps (injuries), asserts max year is valid for each
  </behavior>
  <action>
1. In tests/test_infrastructure.py, add test_injury_season_capped_at_2024 to the TestDynamicSeasonValidation class:
   - Assert validate_season_for_type("injuries", 2024) is True
   - Assert validate_season_for_type("injuries", 2025) is False
   - Assert validate_season_for_type("injuries", 2009) is True (min bound still works)

2. Update test_validate_edge_max_year to skip static-cap types:
   - Define static_cap_types = {"injuries"} at top of test
   - Add `if dtype in static_cap_types: continue` before the assertion
   - Update docstring to say "types with dynamic bounds"

3. Run tests -- injury test MUST FAIL (RED phase, config not changed yet).

4. In src/config.py line 201, change:
   `"injuries": (2009, get_max_season),`
   to:
   `"injuries": (2009, lambda: 2024),  # nflverse discontinued injury data after 2024`

5. Run tests again -- all MUST PASS (GREEN phase).
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && source venv/bin/activate && python -m pytest tests/test_infrastructure.py -v -x</automated>
  </verify>
  <done>validate_season_for_type("injuries", 2025) returns False; validate_season_for_type("injuries", 2024) returns True; all existing tests still pass including updated max-year test</done>
</task>

<task type="auto">
  <name>Task 2: Pin comments in requirements.txt and GITHUB_TOKEN in .env</name>
  <files>requirements.txt, .env</files>
  <action>
1. In requirements.txt, add inline comments to lines 38-39:
   - `nfl_data_py==0.3.3    # pinned: archived Sept 2025, last stable release`
   - `numpy==1.26.4         # pinned: numpy 2.x breaks pandas 1.5.3 (ABI incompatibility)`
   - Optionally add a brief comment block near the top of requirements.txt explaining the pinning strategy (Claude's discretion from CONTEXT.md).

2. In .env, add GITHUB_TOKEN entry with comment:
   - `# GitHub token for API rate limits (5000 req/hr vs 60 unauthenticated)`
   - `# Note: nfl-data-py v0.3.3 does NOT use this token for its downloads --`
   - `# it calls pandas.read_parquet(url) which makes unauthenticated HTTP requests.`
   - `# This token protects gh CLI and GitHub Actions workflows.`
   - `GITHUB_TOKEN=${value_from_existing_GITHUB_PERSONAL_ACCESS_TOKEN_in_.env}`
   - Keep the existing GITHUB_PERSONAL_ACCESS_TOKEN line for backward compatibility.

3. Verify: grep requirements.txt for the pin comments, grep .env for GITHUB_TOKEN.
  </action>
  <verify>
    <automated>cd /Users/georgesmith/repos/nfl_data_engineering && grep -E "nfl_data_py.*pinned|numpy.*pinned" requirements.txt && grep "^GITHUB_TOKEN=" .env</automated>
  </verify>
  <done>requirements.txt has inline comments on nfl_data_py and numpy pins; .env has GITHUB_TOKEN with documentation comment noting nfl-data-py limitation</done>
</task>

</tasks>

<verification>
1. Full test suite passes: `python -m pytest tests/ -v`
2. Injury cap works: `python -c "from src.config import validate_season_for_type; print(validate_season_for_type('injuries', 2025))"` prints False
3. Pin comments visible: `grep -n 'pinned:' requirements.txt` shows two lines
4. GITHUB_TOKEN set: `grep '^GITHUB_TOKEN=' .env` returns a line
</verification>

<success_criteria>
- validate_season_for_type("injuries", 2025) returns False (graceful skip)
- validate_season_for_type("injuries", 2024) returns True (last valid year)
- All 71+ tests pass including new injury cap test and updated max-year test
- requirements.txt has inline comments explaining nfl_data_py and numpy pins
- .env has GITHUB_TOKEN with honest documentation about nfl-data-py limitation
</success_criteria>

<output>
After completion, create `.planning/phases/08-pre-backfill-guards/08-01-SUMMARY.md`
</output>
