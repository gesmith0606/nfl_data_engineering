# v7.1 Draft Season Readiness — SHIPPED 2026-04-26

**Goal:** Deliver draft-season-critical features before fantasy draft season opens — LLM-primary sentiment extraction, external projections comparison, Sleeper league integration, and v7.0 tech debt cleanup.

**Outcome:** All 5 phases shipped. 27/27 requirements code-complete; 22 fully validated by tests; 5 operator-mediated checkpoints (Phase 72 Railway audit + Phase 73 first cron) deferred to natural production cycles.

## Stats
- Phases: 5 (71–75)
- Plans: 21 total (Phase 71: 5, Phase 72: 4 + 1 checkpoint, Phase 73: 5, Phase 74: 1 inline, Phase 75: 1 inline)
- Commits: ~72
- Files modified: ~108
- LOC delta: +17,616 / -237
- New tests: ~165 (sentiment 75 → 211, plus router/Silver/frontend)

## Phases

### Phase 71: LLM-Primary Extraction
**Goal:** Convert `src/sentiment/processing/extractor.py` from rule-primary to Claude-primary so offseason content (drafts/trades/coaching) produces signals.
**Result:** PASSED. 5/5 LLM reqs ✓. Benchmark: rule=28 → claude=156 (5.57× lift). Cost projection: $1.57/wk warm-cache. 165 sentiment tests passing.
**Plans:** 5 (schema, fixtures, batched extractor + cost log, pipeline wiring, CLI + GHA + benchmark)

### Phase 72: Event Flag Expansion + Non-Player Attribution
**Goal:** Add 7 draft-season flags (drafted/rumored/coaching/trade buzz/holdout/cap cut/rookie buzz); hybrid non-player routing.
**Result:** Code-complete (4/5 plans + audit checkpoint). EVT-01/02/03 ✓. EVT-04/05 require Railway-live audit (operator action — see 72-05-CHECKPOINT.md).
**Plans:** 4 + 1 operator-mediated checkpoint

### Phase 73: External Projections Comparison
**Goal:** Side-by-side ESPN + Sleeper + Yahoo (FantasyPros consensus proxy) projections vs ours.
**Result:** Code-complete. EXTP-01/02/03/04 ✓. EXTP-05 cron configured; first run pending. New endpoint `/api/projections/comparison` + frontend comparison view.
**Plans:** 5 (Bronze ingesters, Silver consolidator, API endpoint, frontend view, GHA cron)
**Key file:** `src/sleeper_http.py` shared HTTP helper introduced (D-01 LOCKED, enforced via structural test).

### Phase 74: Sleeper League Integration
**Goal:** Username-based Sleeper auth → league import → roster view → personalized advisor.
**Result:** PASSED. All 4 SLEEP reqs ✓. New `/dashboard/leagues` route + `getUserRoster` advisor tool. 10 router tests passing.
**Plans:** 1 (inline backend + frontend + advisor tool)

### Phase 75: v7.0 Tech Debt Cleanup
**Goal:** Clear all 8 tech debt items rolled forward from v7.0 audit.
**Result:** PASSED. All 8 TD items closed. TD-07 structural test enforces TD-01 (no `--no-verify` or `--amend` in auto-rollback).
**Plans:** 1 (inline TD-01 through TD-08)

## Key Architectural Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Claude-primary as new mode (`claude_primary`), preserve `auto`/`rule`/`claude` | Back-compat + per-doc soft fallback | ✓ Good — production extraction unblocked |
| Anthropic prompt-caching with cached player roster | Cost target <$5/wk at 80 docs/day | ✓ Good — measured $1.57/wk warm-cache |
| FakeClaudeClient with SHA-256 replay | Deterministic CI; no live API | ✓ Good — 165 sentiment tests offline |
| Roster-provider determinism contract (`lambda: []`) | Locks fixture SHA reproducibility | ✓ Good — Phase 72 fixture re-record clean |
| Hybrid non-player attribution (coach+team→rollup, reporter→non_player_news) | Preserves coach context in team rows; reporter byline preserved separately | ✓ Good — 17 routing tests passing |
| NewsItem schema asymmetry resolved via Phase 72 Schema Note (7 new flags only in event_flags list) | Single inflection point; back-compat for every existing client | ✓ Good — locked by 14 schema tests |
| `src/sleeper_http.py` shared helper (D-01 LOCKED) | All Sleeper HTTP through one chokepoint; no third-party deps | ✓ Good — structural test enforces; reused in Phases 73 + 74 |
| Long-format Silver schema for external projections | Future 5th source = column-free addition | ✓ Good — pivot at API layer |
| `yahoo_proxy_fp` source label | Provenance transparent — preserves swap-to-OAuth path for v8.0 | ✓ Good — exposed in source_labels API surface |
| Username-only Sleeper auth (no OAuth) | Multi-user persistence deferred to v8.0; cookie session sufficient | ✓ Good — 10 router tests passing |

## Operator Action Items (post-merge)
1. Push origin main when ready.
2. Phase 72-05: Railway-live audit per `.planning/phases/72-event-flag-expansion/72-05-CHECKPOINT.md` (6-step workflow). Closes EVT-04/05.
3. Phase 73 cron: Tuesday 14:00 UTC + Sunday 12:00 UTC first run. Closes EXTP-05.
4. Sleeper league live test: connect a real Sleeper username on `/dashboard/leagues`; confirm advisor `getUserRoster` returns lineup.

## Audit
Full milestone audit: `.planning/v7.1-MILESTONE-AUDIT.md`.
