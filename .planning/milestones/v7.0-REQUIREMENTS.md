# v7.0 Production Stabilization — Requirements Archive

**Archived:** 2026-04-24
**Coverage:** 32/32 mapped | 23/32 satisfied in code | 9/32 pending external ops
**Milestone audit:** `.planning/v7.0-MILESTONE-AUDIT.md`
**Archive roadmap:** `.planning/milestones/v7.0-ROADMAP.md`

## Legend

- `[x]` — Delivered, tested, verified by code
- `[o]` — Code complete; end-to-end live verification pending external setup (GitHub UI, Railway observation, cron observation)
- `[ ]` — Not completed (none in v7.0)

## HOTFIX (Phase 66 — P0 Deployment Hotfixes)

- [o] **HOTFIX-01**: `ANTHROPIC_API_KEY` is set in Railway environment and readable by the news extractor process. *Set by user 2026-04-22; live-curl verification of `/api/health` flag pending.*
- [x] **HOTFIX-02**: Railway Docker image bundles `data/bronze/schedules/` so `/api/teams/current-week` returns 200, not 503. *Shipped in commit `c4c1640`.*
- [x] **HOTFIX-03**: Railway Docker image bundles `data/bronze/players/rosters/` (or equivalent) so `/api/teams/{team}/roster` returns 200, not 503. *Same commit.*
- [x] **HOTFIX-04**: Predictions page frontend sends `season` and `week` query params to `/api/predictions` (or backend defaults them server-side), eliminating the 422. *Both: Phase 66-02 server-side defaulting + 66-03 frontend nuqs hook.*
- [x] **HOTFIX-05**: Lineups page frontend sends required query params to `/api/lineups`, eliminating the 422. *Same dual mechanism.*
- [x] **HOTFIX-06**: `/api/predictions`, `/api/lineups`, `/api/teams/ARI/roster` all return 200 with well-shaped payloads when hit with no query string (graceful defaulting). *Verified by 12 tests in `tests/web/test_graceful_defaulting.py`.*

## ROSTER (Phase 67 — Roster Refresh v2)

- [x] **ROSTER-01**: Script handles released players by marking them as FA. *Commit `5fd1b65`; 3 unit tests.*
- [x] **ROSTER-02**: Script handles traded/moved players with timestamped audit log entry. *`5fd1b65`; 2 unit tests.*
- [x] **ROSTER-03**: Script writes corrections to Bronze `data/bronze/players/rosters_live/`. *`5fd1b65`.*
- [x] **ROSTER-04**: Audit log surfaced via GitHub Actions artifact upload. *Commit `f09f57f`.*
- [o] **ROSTER-05**: Kyler Murray correctly reflects his current team. *Code delivers correct behavior; live canary awaits daily cron run against Sleeper.*
- [x] **ROSTER-06**: Daily cron calls rewritten script and fails loudly on errors. *`f09f57f` removed silent `|| echo`; Phase 69 added `contents: write` + `issues: write` permissions.*

## SANITY (Phase 68 — Sanity-Check v2)

- [x] **SANITY-01**: Probes `/api/predictions`, fails on 422 / empty-when-expected. *`_probe_predictions_endpoint` + 4 tests in `test_sanity_check_v2_probes.py`.*
- [x] **SANITY-02**: Probes `/api/lineups`, fails on shape issues. *`_probe_lineups_endpoint` + 4 tests.*
- [x] **SANITY-03**: Probes `/api/teams/{team}/roster` for top-10 teams, fails on 503. *`_probe_team_rosters_sampled` + 3 tests.*
- [x] **SANITY-04**: Validates `/api/news/team-events` CONTENT (≥20/32 teams), not just `len==32`. *`_validate_team_events_content` with 17/19/20 thresholds.*
- [x] **SANITY-05**: Roster drift check vs Sleeper canonical for top-50 PPR players; CRITICAL on mismatch. *`_check_roster_drift_top50` + 6 drift tests + Kyler Murray canary.*
- [x] **SANITY-06**: Extractor freshness check (latest Silver sentiment within 48h). *`_check_extractor_freshness` (CRITICAL >48h, WARN 24-48h).*
- [x] **SANITY-07**: Asserts `ANTHROPIC_API_KEY` set when `ENABLE_LLM_ENRICHMENT=true`. *`_assert_api_key_when_enrichment_enabled`; never logs value.*
- [x] **SANITY-08**: Deploy gate invokes `--check-live` as BLOCKING step. *`deploy-web.yml` `live-gate-blocking` job with `needs: [deploy-frontend, deploy-backend]`, no `continue-on-error`.*
- [o] **SANITY-09**: Post-deploy smoke promoted to blocking with auto-rollback on failure. *`auto-rollback` job wired + 20 YAML structural tests guard invariants; first real live rollback proof deferred to natural failure event.*
- [x] **SANITY-10**: DQAL-03 carry-overs (negative-projection clamp, rookie ingestion, rank-gap) asserted. *3 `_check_dqal_*` functions + 11 DQAL tests; live-surfacing 2 real DQAL CRITICALs on current Gold data.*

## SENT (Phase 69 — Sentiment Backfill)

- [o] **SENT-01**: Extractor runs against accumulated Bronze. *Workflow hotfixes landed (season decoupling + permissions); extraction itself blocked on GitHub Secret `ANTHROPIC_API_KEY` + Variable `ENABLE_LLM_ENRICHMENT=true` not yet set.*
- [o] **SENT-02**: `/api/news/team-events` shows `event_flags` + `total_articles > 0` for ≥20 of 32 teams. *Blocked on SENT-01.*
- [o] **SENT-03**: `/api/news/feed` returns populated `sentiment`, `event_flags`, `player_id`, `summary`. *Blocked on SENT-01.*
- [o] **SENT-04**: News page renders real headlines + sentiment context. *UI was delivered in Phase 61; waiting for Phase 69 data.*
- [o] **SENT-05**: Advisor audit flips 4 news tools WARN → PASS. *Blocked on SENT-01.*

## FE (Phase 70 — Frontend Empty/Error States)

- [x] **FE-01**: Predictions page friendly empty state with `data_as_of`. *`prediction-cards.tsx` EmptyState integration.*
- [x] **FE-02**: Lineups page friendly empty state with week/season context. *`lineup-view.tsx` EmptyState integration.*
- [x] **FE-03**: Matchups page 503 offseason fallback. *`matchup-view.tsx` with preseason preview below EmptyState; no error styling.*
- [x] **FE-04**: News page empty state + sentiment null-safety (no dangling chips). *`news-feed.tsx` + `player-news-panel.tsx` with `typeof sentiment === 'number' && summary.trim().length > 0` guard.*
- [x] **FE-05**: All 4 pages surface `meta.data_as_of`. *Via `useWeekParams().dataAsOf` (predictions/lineups), `predData.generated_at` (matchups), TanStack Query `feedUpdatedAt` (news).*

## Coverage

- Total: 32 requirements
- Code satisfied: 23 (72%)
- Pending external ops: 9 (28%)
- Unsatisfied: 0

## Pending External Operations (human_needed → passed path)

1. `ANTHROPIC_API_KEY` as **GitHub Secret** (distinct from Railway env var) — at https://github.com/gesmith0606/nfl_data_engineering/settings/secrets/actions
2. `ENABLE_LLM_ENRICHMENT=true` as **GitHub Variable** — at https://github.com/gesmith0606/nfl_data_engineering/settings/variables/actions
3. Re-trigger `gh workflow run daily-sentiment.yml -f season=2025 -f week=17` (and W18)
4. Observe daily cron run for Kyler Murray canary (ROSTER-05)
5. Run 6 live verification curls from `66-VERIFICATION.md` (HOTFIX-01)
6. Run `scripts/audit_advisor_tools.py --live https://nfldataengineering-production.up.railway.app` (SENT-05)
7. Deliberate or observed live rollback event to prove SANITY-09

Estimated total operational effort: ~30-60 min.
