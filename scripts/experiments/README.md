# Archived experiment & one-off scripts

Stale experiment and connectivity-check scripts moved out of `scripts/` during
the 2026-07-07 repo cleanup. None are referenced by workflows, skills, or
active code — kept for the historical record (see `.planning/` phase docs that
cite them). Active experimentation lives in `scripts/experiment_heuristic_lab.py`.
