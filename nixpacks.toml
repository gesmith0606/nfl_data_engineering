[phases.setup]
nixPkgs = ["python39", "gcc", "libpq"]

[phases.install]
cmds = ["pip install -r web/requirements.txt"]

[start]
cmd = "python -c \"import os; port=os.environ.get('PORT','8000'); import uvicorn; uvicorn.run('web.api.main:app', host='0.0.0.0', port=int(port))\""
