# Bronze Layer Data Inventory

> This document is auto-generated by `scripts/generate_inventory.py`.
> To refresh after new ingestions, run: `python scripts/generate_inventory.py --output docs/BRONZE_LAYER_DATA_INVENTORY.md`

**Generated:** 2026-03-08
**S3 Bucket:** `s3://nfl-raw/`
**Local Mirror:** `data/bronze/`
**Total Files:** 31
**Total Size:** 6.89 MB

## Current Data Summary

| Data Type | Files | Size (MB) | Seasons | Columns | Last Updated |
|-----------|-------|-----------|---------|---------|--------------|
| games | 6 | 0.31 | 2020-2025 | 50 | 2026-03-06 |
| players/injuries | 5 | 0.64 | 2020-2024 | 18 | 2026-03-06 |
| players/rosters | 5 | 2.77 | 2020-2024 | 39 | 2026-03-06 |
| players/seasonal | 5 | 0.69 | 2020-2024 | 60 | 2026-03-06 |
| players/snap_counts | 5 | 1.15 | 2020-2024 | 18 | 2026-03-06 |
| players/weekly | 5 | 1.33 | 2020-2024 | 55 | 2026-03-06 |

## Available but Not Yet Ingested

The following data types are supported by `bronze_ingestion_simple.py` and registered in `DATA_TYPE_SEASON_RANGES` (see `src/config.py`) but have not been ingested locally:

| Data Type | Available Seasons | Ingestion Command |
|-----------|-------------------|-------------------|
| pbp | 1999-present | `python scripts/bronze_ingestion_simple.py --season YYYY --week W --data-type pbp` |
| ngs | 2016-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type ngs` |
| pfr_weekly | 2018-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type pfr_weekly` |
| pfr_seasonal | 2018-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type pfr_seasonal` |
| qbr | 2006-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type qbr` |
| depth_charts | 2001-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type depth_charts` |
| draft_picks | 2000-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type draft_picks` |
| combine | 2000-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type combine` |
| teams | 1999-present | `python scripts/bronze_ingestion_simple.py --season YYYY --data-type teams` |

## Storage Architecture

- **Canonical storage:** `s3://nfl-raw/` (AWS S3, us-east-2 region)
- **Local development mirror:** `data/bronze/` -- used as primary read source with S3 as fallback
- **S3 key pattern:** `dataset/season=YYYY/week=WW/filename_YYYYMMDD_HHMMSS.parquet`
- **Read convention:** Always use `download_latest_parquet()` from `src/utils.py` -- never scan full prefix
- Files are partitioned by season (and week where applicable) and timestamped to preserve full history

## How to Refresh

```bash
# Regenerate this inventory from current local data
python scripts/generate_inventory.py --output docs/BRONZE_LAYER_DATA_INVENTORY.md

# Print inventory to stdout without overwriting
python scripts/generate_inventory.py

# Scan S3 instead of local (requires valid AWS credentials)
python scripts/generate_inventory.py --s3

# Scan a custom directory
python scripts/generate_inventory.py --base-dir /path/to/bronze
```

## Data Pipeline

1. **Fetch:** NFL data from nfl-data-py API via `scripts/bronze_ingestion_simple.py`
2. **Validate:** Data quality and completeness checks via `NFLDataFetcher.validate_data()`
3. **Store:** Write to local `data/bronze/` in Parquet format with season/week partitioning
4. **Upload (optional):** Push to `s3://nfl-raw/` with `--s3` flag

---

**Data Freshness:** Current as of generation date
**Update Frequency:** Manual (on-demand via CLI scripts)
**Quality Status:** Validated and production-ready
